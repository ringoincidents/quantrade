# 토스증권 Open API — 주문/거래내역 스펙 조사 (2026-08-05)

> **범위**: 조사 문서. 코드 구현 없음. `autoexec.place_sell_order()`를 실제로 채우는 작업,
> `portfolio_pnl_history.json`(총손익 추적) 신설 작업 둘 다 **이 문서의 범위 밖**이며
> 다음 방향성 세션에서 이 조사 내용을 재확인한 뒤 별도로 진행한다 — 지시받은 대로 조사만
> 하고 실제 연결은 하지 않았다.
>
> **출처와 신뢰도에 대한 중요한 유보 사항**: 이 세션의 네트워크 정책상 `openapi.tossinvest.com`,
> `developers.tossinvest.com` 두 도메인 모두 프록시 단계에서 `403`으로 차단되어 있어(조직
> egress 정책, Toss 서버 쪽 문제 아님) **공식 문서를 이 세션에서 직접 열어 대조하지 못했다.**
> 대신 GitHub에 공개된 서드파티 CLI 프로젝트(`JungHoonGhae/tossinvest-cli`)가 미러링해둔
> `docs/migration/openapi.latest.json`(제목 "토스증권 Open API", `version: "1.2.9"`)을
> 가져와 분석했다 — 이 저장소가 `real_portfolio_sync.py`에서 이미 확인한 `version 1.2.5`보다
> 신버전이다. 파일 자체의 sha256(`fa844be22a3ab471052a75ef8015087fdcf5973d30f80306d35c5ebc09287968`)가
> 그 프로젝트의 스냅샷 메타데이터(`.openapi-snapshot.json`)에 기록된 값과 정확히 일치해
> **다운로드 과정에서 변조되지 않았다는 것**은 확인했지만, 이 미러가 애초에 Toss 서버에서
> 정확히 받아온 게 맞는지는 **독립적으로 검증할 수 없었다.** 즉 "제3자 미러 기준 신뢰도"이지
> "1급 소스 확인"이 아니다. **실제 구현에 들어가기 전에 프록시 우회/화이트리스트 등으로
> `openapi.tossinvest.com`에 직접 접속해 이 문서의 스키마와 대조하거나, 사용자가 공식 문서
> 페이지를 직접 캡처해서 제공하는 절차가 반드시 필요하다** — `real_portfolio_sync.py` 구현
> 때 계좌/보유종목 API를 실제 스펙으로 확정했던 것과 동일한 검증 수준이 주문 API에도
> 필요하다.

---

## 1. 지침 1 — 주문 API 조사

### 1.1 인증

기존 `real_portfolio_sync.py`와 **완전히 동일한 인증 구조**다. 추가 스코프나 별도 승인 절차는
스펙상 보이지 않는다.

- `POST /oauth2/token` — OAuth 2.0 Client Credentials Grant (기존 구현 그대로)
- 모든 요청에 `Authorization: Bearer {access_token}` 헤더
- 계좌/주문처럼 자산을 다루는 API는 추가로 `X-Tossinvest-Account: {accountSeq}` 헤더 필요
  (기존 구현의 `account_seq` 파라미터와 동일한 값 재사용 가능)
- `securitySchemes.oauth2ClientCredentials.flows.clientCredentials.scopes`가 **빈 객체 `{}`**
  — 문서상 별도 주문 전용 스코프 개념이 없다(다만 1급 소스로 확인 못 했으므로 실제 발급받은
  client_id/secret에 주문 권한이 별도로 붙어 있는지는 토스 개발자센터에서 직접 확인 필요).

### 1.2 관련 엔드포인트 전체 목록

| 메서드 | 경로 | 용도 |
|---|---|---|
| POST | `/api/v1/orders` | 주문 생성 |
| GET | `/api/v1/orders` | 주문 목록 조회 (status=OPEN\|CLOSED) |
| GET | `/api/v1/orders/{orderId}` | 주문 상세 조회 |
| POST | `/api/v1/orders/{orderId}/cancel` | 주문 취소 |
| POST | `/api/v1/orders/{orderId}/modify` | 주문 정정(가격/수량) |
| GET | `/api/v1/sellable-quantity` | 매도가능수량 조회 (주문 전 사전 점검용) |
| GET | `/api/v1/commissions` | 계좌 수수료율 조회 |
| GET/POST/DELETE | `/api/v1/conditional-orders*` | 조건부 주문 (스탑로스 등, 이 조사 범위 밖) |

`autoexec.execute()`가 지금 하드코딩하고 있는 매도 전용·즉시체결 요구를 채우려면 최소
`POST /api/v1/orders`(주문 생성) + `GET /api/v1/orders/{orderId}`(체결 확인)면 된다.
취소/정정은 매도 자동실행 유스케이스에서 당장 필요하지 않을 수 있다.

### 1.3 주문 생성 (`POST /api/v1/orders`) 요청 스키마

`autoexec.place_sell_order(symbol, quantity)`가 채워야 할 실제 필드. `oneOf` 두 가지 방식 중
`autoexec.py`의 현재 설계(정수 수량 매도)는 **수량 기반 방식**만 필요하다.

**수량 기반 (OrderCreateQuantityBased) — 우리가 쓸 방식:**

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `symbol` | string | ✅ | KRX 6자리 숫자 / US 영문 티커 |
| `side` | enum `BUY`\|`SELL` | ✅ | `autoexec`는 항상 `SELL`만 |
| `orderType` | enum `LIMIT`\|`MARKET` | ✅ | 아래 1.6 참고 — 자동실행에 뭘 쓸지가 실제 설계 결정 사항 |
| `quantity` | string(decimal) | ✅ | KR은 정수만. `autoexec.py`가 이미 `int`로 계산해두므로 문자열 변환만 하면 됨 |
| `price` | string(decimal) | LIMIT일 때만 필수 | KR: 호가 단위 준수 필요(안 맞으면 400, 올바른 호가가 에러 응답에 옴) |
| `clientOrderId` | string, 최대 36자 `[a-zA-Z0-9_-]` | 선택 | **멱등키.** 10분간 유효, 동일 값 재요청 시 이전 주문 결과 재반환 |
| `confirmHighValueOrder` | boolean, 기본 `false` | 조건부 | 1억원 이상 주문 시 `true` 아니면 `400 confirm-high-value-required` |
| `timeInForce` | enum `DAY`\|`CLS`, 기본 `DAY` | 선택 | KR 매도는 `DAY`만 해당 |

응답(`OrderResponse`): `{"orderId": "...", "clientOrderId": "..." | null}` — 체결 여부는
별도로 `GET /api/v1/orders/{orderId}`를 폴링해서 확인해야 한다(생성 응답 자체엔 체결 정보가
없다).

### 1.4 주문 상세/체결 조회 (`GET /api/v1/orders/{orderId}`)

`Order` 스키마 핵심 필드: `status`(`OrderStatus` — 아래 1.7), `execution` 객체 —
`filledQuantity`, `averageFilledPrice`, `filledAmount`, `commission`, `tax`, `filledAt`,
`settlementDate`. **부분체결/거부 시에도 `filledQuantity`로 실제 체결량을 확인해야 하며,
`status`만 보고 "실행됨/안됨"을 이분법으로 판단하면 안 된다** — 예:
`CANCELED`/`REJECTED` 상태에서도 `execution.filledQuantity > 0`일 수 있다(부분체결 후 취소).

### 1.5 취소/정정

- `POST /api/v1/orders/{orderId}/cancel` — 본문 없음(빈 객체). 이미 체결된 주문은
  409(`already-filled`) 등으로 거부.
- `POST /api/v1/orders/{orderId}/modify` — `orderType` 필수, KR은 `quantity`도 필수(정수).
  US는 가격만 정정 가능, `quantity` 보내면 400.
- 두 작업 모두 새 `orderId`를 응답에 발급하며 **원주문 orderId와 다르다** — 상태 추적 시
  주의 필요(원주문 id로 조회하면 되는지, 새 id로 다시 조회해야 하는지는 1급 소스로
  재확인 필요한 지점).

### 1.6 실제 구현 전 결정해야 할 사항 (조사로 드러난 열린 질문)

이 항목들은 **이 세션이 판단하지 않는다** — 다음 방향성 세션이 확정할 대상으로 남겨둔다.

1. **`orderType`을 `MARKET`으로 할지 `LIMIT`으로 할지.** `autoexec.py`의 세 규칙(집중도/
   손실지속/목표가)은 지금 목표 매도금액/수량만 계산하고 가격을 지정하지 않는다. `MARKET`은
   가격 슬리피지 위험, `LIMIT`은 미체결 위험(그리고 KR은 호가단위 준수 필요) — 트레이드오프이며
   이 조사가 대신 고를 수 없다.
2. **`clientOrderId` 멱등키 전략.** `autoexec.py`가 이미 `approval_id`(`{rule}_{symbol}_{날짜}`
   형식)를 갖고 있어 자연스러운 후보이지만, 멱등키 유효기간이 10분뿐이라 "승인 대기 등록 시점"과
   "실제 `/autoexec_approve` 처리 시점"이 10분 이상 벌어지면 재사용할 수 없다 — 재승인 재시도
   흐름을 어떻게 설계할지 결정 필요.
3. **1억원 이상 주문 확인 플래그(`confirmHighValueOrder`).** `CONCENTRATION_MAX_SELL_PCT`
   상한이 있어도 평가액이 큰 종목이면 1회 매도가 1억을 넘을 수 있다 — 이 플래그를 무조건
   `true`로 보낼지, 별도 승인 스텝을 추가할지는 안전장치 설계 판단이다.
4. **부분체결 처리.** `execute()`가 지금은 "성공/실패"만 다루는데, 실제 주문은 부분체결로
   끝날 수 있다(`PARTIAL_FILLED`) — `autoexec_state.json`/`autoexec_log.json`에 부분체결
   수량을 어떻게 기록할지 스키마 확장이 필요하다.
5. **체결 확인을 동기 폴링으로 할지, 별도 워크플로로 할지.** `check_updates.py`의
   `handle_autoexec_approve()`는 지금 한 번의 텔레그램 응답 안에서 끝나야 하는데, 시장가가
   아닌 지정가 주문은 즉시 체결되지 않을 수 있다 — "승인 = 주문 접수"와 "승인 = 체결 완료"는
   다른 의미이고 텔레그램 메시지 문구도 달라져야 한다.

### 1.7 참고: 에러 코드 / 상태값 전체

- `OrderStatus`: `PENDING`, `PENDING_CANCEL`, `PENDING_REPLACE`, `PARTIAL_FILLED`, `FILLED`,
  `CANCELED`, `REJECTED`, `CANCEL_REJECTED`, `REPLACE_REJECTED`, `REPLACED`
  (스펙 자체가 "클라이언트는 unknown code를 허용하도록 구현해야 한다"고 명시 — 향후 상태값이
  추가될 수 있다는 뜻)
- 확인된 4xx 에러 코드: `account-header-required`, `invalid-request`(필드별 `data.field`로
  세분화됨 — 호가단위 불일치 시 `nearestPrices` 후보까지 응답에 포함), `confirm-high-value-required`,
  `already-canceled`/`already-filled`/`already-modified`/`already-processing`/`already-rejected`,
  `cancel-restricted`, `order-hours-closed`, `us-modify-quantity-not-supported`,
  `amount-order-outside-regular-hours`, `fractional-quantity-outside-regular-hours`,
  `fractional-quantity-scale-exceeded`, `max-order-amount-exceeded`(30억원 초과, 확인 플래그와
  무관하게 무조건 거부)
- Rate limit 그룹: `ORDER`(생성/취소/정정), `ORDER_HISTORY`(목록/상세 조회), `ORDER_INFO`
  (매도가능수량/수수료) — 그룹별 구체적 수치(초당/분당 제한)는 이번 조사에서 확인하지 못했다.

---

## 2. 지침 2 관련 조사 — 실현손익/거래내역/배당/이자 엔드포인트

**결론부터: 이 스펙(27개 경로 전수 확인)에는 사용자가 요청한 세 가지 중 하나도 직접 제공하는
엔드포인트가 없다.**

- **`/api/v1/trades`는 계좌 거래내역이 아니다.** 이름 때문에 헷갈리기 쉬운데, 실제로는
  "종목별 최근 체결 내역 조회"라는 **공개 시세 데이터**(호가창처럼 누구나 볼 수 있는 정보,
  최대 50건, 당일 한정)다. 응답 필드가 `price`/`volume`/`timestamp`/`currency`뿐이고 계좌 식별자,
  주문 방향, 손익 개념 자체가 없다 — `X-Tossinvest-Account` 헤더도 요구하지 않는다.
- **실현손익을 직접 반환하는 엔드포인트 없음.** 가장 가까운 것은 `GET /api/v1/orders?status=CLOSED`
  (주문 이력, `execution.averageFilledPrice`/`filledAmount`/`commission`/`tax` 포함)인데, 이건
  **체결 정보일 뿐 손익이 아니다** — 매도 체결가와 매수 시점 평균단가를 우리가 직접 매칭해서
  계산해야 한다. 문제는:
  - Toss가 내부적으로 어떤 회계 방식(FIFO/평균단가/이동평균)을 쓰는지 스펙에 안 나와 있어
    우리 계산과 Toss 내부 계산이 다를 수 있다.
  - `real_portfolio_sync.py`가 2026-08-01부터 시작했으므로 **그 이전 매도 이력의 매수원가를
    복원할 방법이 없다** — 그 이전 실현손익은 원천적으로 계산 불가능하다.
- **배당금 조회 엔드포인트 없음.** 27개 경로 어디에도 dividend/배당 관련 항목이 없다.
- **이자(예수금 이자 등) 조회 엔드포인트 없음.** 마찬가지로 없다.

### 2.1 이게 지침 2에 의미하는 것

사용자가 요청한 "총손익 = 실현손익(판매수익+배당금+이자 누적) + 미실현손익"을 **문면 그대로
구현할 방법이 이 Open API 안에는 없다.** 가능한 것과 불가능한 것을 나누면:

| 구성요소 | 가능 여부 | 방법 |
|---|---|---|
| 미실현손익 | ✅ 가능 | `GET /api/v1/holdings`(이미 `real_portfolio_sync.py`가 수집 중, `profitLoss.rate`) — 종목별 평가손익을 합산하면 됨 |
| 실현손익(매매) | ⚠️ 부분적으로만 가능, 정확도 낮음 | `GET /api/v1/orders?status=CLOSED`의 체결 기록을 우리가 직접 매칭해 계산. **2026-08-01 이전 매도 이력은 원가 정보가 없어 계산 불가**, 그 이후 것도 Toss 내부 회계방식과 어긋날 수 있음 |
| 배당금 | ❌ 불가능 | 해당 엔드포인트가 스펙에 없음 |
| 이자 | ❌ 불가능 | 해당 엔드포인트가 스펙에 없음 |

즉 "판매수익+배당금+이자"를 다 채운 정확한 실현손익 숫자를 만들 수 없고, 만들 수 있는 건
①미실현손익(정확)과 ②2026-08-01 이후 매도분의 근사 실현손익(부정확할 수 있음) 정도다.
배당금·이자를 빈칸으로 두고 "총손익"이라는 이름을 붙이면 실제보다 낮은 숫자로 사용자를
오도할 위험이 있다 — `rule_trigger_report.py`가 이미 확립한 "빈 칸이 틀린 숫자보다 낫다"
원칙에 비춰보면, 이 상태로 조용히 근사치를 만들어 넣는 것보다 이 한계를 먼저 사용자에게
보고하는 편이 맞다고 판단해 **`portfolio_pnl_history.json` 신설과
`real_portfolio_sync.py` 훅 연결은 보류했다.**

CLAUDE.md의 "Track B/실계좌 관련 파일(`real_portfolio_sync.py` 포함) 변경은 사전
방향성 세션 승인 필요 — 이 세션도 예외 아님" 원칙과도 맞물려 있어, 이 부분은 코드로
먼저 만들어보고 나중에 보고하는 대신 지금 여기서 먼저 확인을 구한다.
