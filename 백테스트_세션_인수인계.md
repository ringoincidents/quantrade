# 백테스트 세션 인수인계 (2026-08-08 작성)

새 세션이 이 저장소에서 백테스트 관련 지시를 받았을 때, 과거 대화를 다시 훑지
않고 바로 이행할 수 있도록 현재 상태만 압축해 둔 문서. **CLAUDE.md를 대체하지
않는다** — CLAUDE.md가 정본이고, 특히 최상단 "v3.2 전환" 절과 "세션 시작
체크리스트"는 반드시 먼저 읽을 것. 이 문서는 그 안에서 백테스트에 필요한 부분만
빠르게 찾도록 돕는 색인이다.

## 0. 제일 먼저 할 것

```
git fetch origin main && git log --oneline origin/main -10
```

이 저장소는 여러 세션이 병렬로 작업한다. 이 문서에 적힌 "현재 상태"가 이미
낡았을 수 있다.

## 1. 지금 예측(BUY/SELL 방향 판단)은 꺼져 있다

`PREDICTION_ENABLED = false` (`analyze_lib.py`). 두 신호원이 모두 §2.1 통계
게이트를 통과하지 못해서다:

| 신호원 | 파일/로직 | 게이트 결과 |
|---|---|---|
| 가격 신호 | `backtest.py` — MA20/60 골든크로스 + ADX≥25, RSI/BB는 진입타이밍 필터로 강등 | **미통과** |
| 뉴스 방향판단 | `news_event_experiment.py`(실시간) / `news_event_backtest.py`(과거 재현) — `ask_news_event_judgment` | **미통과, 다수클래스 baseline보다도 낮음** |

**"이번엔 될 것 같다"는 이유로 아무거나 재활성화하지 않는다.** 재개하려면 다시
§2.1 게이트를 통과해야 한다. 아래는 각 트랙의 구체적인 파일·명령·현재 수치다.

## 2. Phase 1 — 가격 신호 백테스트 (`backtest.py`)

- 실행: `python backtest.py` (CLI) 또는 `backtest.yml`(workflow_dispatch)
- 출력: `backtest_report.json`
- 신호: `analyze_lib.is_golden_cross` + `calc_adx` (MA20/60 골든크로스, ADX≥25),
  RSI≥75 또는 상단 밴드 위면 진입 스킵. **라이브 스캐닝(`entry_score`)과
  의도적으로 분리돼 있다** — 여기서 검증된 규칙을 `entry_score`로 승격하는 건
  별도의 의식적 단계다(계획서 v3 "전략 검증이 아키텍처보다 먼저다").
- 매도(청산)는 규칙 기반 근사치(하드손절/RSI과열/보유기간의 2배 타임스톱) —
  과거 Claude 판단을 재생할 수 없어서(비용+비결정성) 쓰는 알려진 단순화.
- 비용 모델: `TRADING_COSTS`(수수료+슬리피지) + KRX 매도 시 `sell_tax_pct`(증권거래세).
- 70/30 train/validation 분할, 상승장/하락장/횡보장(추세 60일 수익률 기준)
  레짐별 집계, buy&hold 벤치마크(레짐 태깅 포함) 동반 비교.
- **고정된 성공 기준**(`SUCCESS_CRITERIA`, 사후 조정 금지):
  ```
  min_trades: 30, sharpe_meaningful: 1.0, sharpe_review: 0.5, mdd_limit_pct: -20
  ```
  MDD는 참고용일 뿐 통과/실패 판정에서 제외된다(`MDD_CAVEAT`,
  `POSITION_WEIGHT`=10% 상수 스케일링 근사치 — 실제 동시보유/분산효과 미반영).
- **`evaluate_gate()`가 §2.1 전체 판정을 `report["gate"]`에 묶는다**: 훈련/검증
  각 30건↑, 검증 샤프≥1.0, 훈련/검증 동일부호(과최적화 징후), 훈련/검증 모두
  buy&hold 대비 우위. **현재 저장된 `backtest_report.json` 결과:
  `gate_passed: false`** — 30건 기준 2개만 충족, 나머지 4개(검증샤프,
  훈련/검증 동일부호, 훈련·검증 각각 buy&hold 대비 우위) 전부 미충족.
- `--max-hold-multiplier`/`--rsi-exit`: 청산 규칙 완화 실험용, "1회만 시도,
  반복 튜닝 금지" 전제로 추가된 플래그. 이미 한 번 쓴 것으로 기록돼 있다 —
  다시 쓰기 전에 CLAUDE.md/커밋 로그에서 그 결과부터 확인할 것.
- 유니버스: `--crypto` 생략 시 `get_all_krw_markets()[:80]`, `--krx` 생략 시
  `KRX_MARKET_CAP_TOP`(정적 스냅샷), lookback 기본 1500(주식)/5000(크립토).

## 3. Phase 2 — 뉴스 방향판단 (두 트랙, 절대 병합 금지)

두 트랙은 `news_event_calibration_analysis.py --log-file <파일>`로 **각각
따로만** 분석한다. 병합/공동분석 금지 — `_meta.log_file`/`_meta.track`이
출력에 찍혀 어느 쪽인지 항상 구분된다.

### 3-1. 실시간 트랙 (arbiter — 최종 판단 기준)

- `news_event_experiment.py` → `news_event_calibration_log.json`, 매일 cron
- D+1/D+5/D+20 결과가 실제로 올 때까지 기다려야 하는 트랙. **건드리지 말 것**
  (CLAUDE.md/이전 지시 다수에서 반복 확인된 제약).

### 3-2. 과거 재현 트랙 (leading indicator — 대체재 아님)

- `news_event_backtest.py` → `news_event_backtest_log.json`,
  `news_event_backtest.yml`(workflow_dispatch 전용)
- 선정 기준은 수집 전에 `Phase2_과거뉴스백테스트_설계.md`에 사전 고정:
  2026-02-01~07-31, 5일 간격 판단일(25개), `KRX_MARKET_CAP_TOP` 전수, 랜덤
  샘플링 없음.
- 룩어헤드 방지: (1) Claude API에 `tools` 파라미터 없음(웹서치 등 접근 불가),
  (2) RSS `pubDate`를 GMT→KST 변환해 판단일 이전 기사만 인정 — 검증
  실패시 조용히 통과가 아니라 표본 0건으로 떨어지게 설계.
- **현재 저장된 `news_event_backtest_log.json`: 706건 전수 수집 완료.**
  `stats`: `pubdate_rejected=636`, `no_headlines=344`, `judge_failed=0`,
  `no_price=0`, `fetch_failed=637`.
- **결과(문서화됨, `Phase2_과거뉴스백테스트_설계.md` §8): 게이트 미통과.**
  고확신군조차 D+1/D+5/D+20 세 window 전부 다수클래스 baseline 미달
  (D+1 −8.82p / D+5 −3.04p / D+20 −5.53p), 방향별 적중률이 기저비율과
  사실상 동일(D+1 호재 40.0% vs 기저 40.1%) — 방향 판단에 정보가 없다는
  결론.
- **§4-1 전제, 반드시 같이 인용할 것**: 이 트랙 단독 통과는 §2.1 게이트
  통과로 인정하지 않는다. 판단모델이 그 시기 뉴스로 훈련됐다면 결과를
  "기억"해낼 수 있고, 도구 차단으로는 이 오염을 막을 수 없다 — 실시간
  트랙에는 없는 문제. (이번 경우는 미통과라 이 전제가 실제로 문제된 건
  아니지만, 통과 케이스가 나오면 반드시 이 문장과 함께 보고할 것.)
- promptB 실험 관련: 이전 세션에서 470건 judge_failed로 재수집 필요 상태로
  남았을 수 있음 — 새 지시가 ECE 비교를 요구하면 먼저 현재 상태 확인.

## 4. Phase 3 — 펀더멘털 신호 (스펙만 존재, 미착수)

- `Phase3_펀더멘털신호_스펙.md` — PER/PBR/EV-EBITDA 저평가, 부채비율 개선,
  영업이익 흑자전환 등. 판단 시점은 분기 실적 발표 후. Phase 1 백테스트
  프레임(train/val 분할, 레짐 태깅, buy&hold 비교, 고정 성공기준) 재사용
  전제로 설계.
- **착수는 방향성 세션 승인 후.** 데이터 소스(재무제표 조회 API/스크레이핑)가
  아직 결정 안 됨 — 이게 착수의 블로커.
- 지시가 "Phase 3 시작"이면: 스펙 문서부터 읽고, 데이터 소스 결정이 이미
  됐는지(방향성 세션에서) 먼저 확인. 안 됐으면 스펙 문서에 명시된 대로
  코드 세션 판단으로 데이터 소스를 정하지 말 것.

## 5. 백테스트 세션이 절대 하지 말아야 할 것 (CLAUDE.md 발췌)

- **성공 기준 사후 조정 금지.** `SUCCESS_CRITERIA`(Phase 1), Fisher's
  유의성 α구간(Phase 2, `SIGNIFICANCE_ALPHA_STRONG/WEAK`), 최소표본
  `MIN_BUCKET_SAMPLES=10`은 전부 결과를 보기 전에 고정됐다. "이번엔 결과가
  아쉬우니 기준을 조금 완화" 하지 않는다.
- **게이트 판정(충족/미충족)을 이 문서나 CLAUDE.md에 새로 써넣지 않는다.**
  이미 기록된 과거 판정(위 §2, §3-2의 "미통과")은 인용 가능하지만, 새로운
  실행 결과에 대한 통과/미통과 판정은 방향성 세션 몫이다 — 코드 세션은
  "결과 숫자가 이렇다"까지만 기록한다.
- **`news_event_calibration_log.json`(실시간 트랙)에 손대지 않는다.**
- **`PREDICTION_ENABLED`를 켜지 않는다.** 백테스트 결과가 좋게 나와도
  마찬가지 — 재활성화는 방향성 세션 승인 사안.
- **백그라운드/폴링 스크립트에 `git checkout`/`switch` 넣지 않는다** —
  포그라운드 작업트리와 공유되어 브랜치가 임의 시점에 갈아치워진 사고가
  실제로 있었다(CLAUDE.md 세션 시작 체크리스트 참조).
- 백테스트 워크플로가 상태 파일을 커밋할 때 `daily.yml`/`poll.yml`과
  레이스가 날 수 있는 파일(`portfolio.json` 등)은 건드리지 않는다 —
  `backtest_report.json`/`news_event_backtest_log.json`은 자기 워크플로만
  커밋하므로 레이스 없음, 이 구분을 유지할 것.

## 6. 새 지시 유형별 즉시 참조

- **"백테스트 다시 돌려줘/파라미터 바꿔서"** → §2. 유니버스/lookback 플래그
  변경은 허용, `SUCCESS_CRITERIA` 변경은 방향성 세션 승인 필요.
- **"뉴스 트랙 결과 더 모아줘/기간 넓혀줘"** → §3-2. 선정 기준(윈도우,
  간격, 유니버스)은 사전 고정이라 바꾸면 재등록 필요 — 그냥 이어서 모으는
  건 기존 `--exec-from`/`--exec-until` 범위 필터로 가능(전체 판단일 목록은
  안 바뀜, 실행 구간만 나눔).
- **"게이트 통과했는지 판정해줘"** → 숫자는 보고하되, "통과/미통과"
  단정은 방향성 세션에 넘긴다고 명시.
- **"Phase 3 시작"** → §4. 데이터 소스 결정 여부부터 확인.
- **"결과 요약 문서/보고서 만들어줘"** → 기존 `Phase2_과거뉴스백테스트_설계.md`
  §8, `세션보고서_2026-08-04.md` 형식 참고. 판정 문구 대신 사실 나열 톤 유지.
