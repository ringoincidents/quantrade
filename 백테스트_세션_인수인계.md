# 백테스트 상태 보고서 — 2026-08-08

> 이 문서는 **현재 상태 요약**이다. 결정을 내리지 않으며, 게이트 판정이나 재개
> 가능 여부 같은 판정성 서술은 담지 않는다(CLAUDE.md 세션 시작 체크리스트
> "사실을 적고 판정을 내리지 말 것"). 새 세션이 백테스트 관련 지시를 바로
> 이행할 수 있도록 세 트랙(Phase 1/2/3)의 현재 수치만 압축했다. CLAUDE.md를
> 대체하지 않는다 — 특히 최상단 "v3.2 전환" 절이 정본이다.

---

## 1. 한눈에

| 트랙 | 신호 | 상태 | 최근 결과 |
|---|---|---|---|
| Phase 1 | 가격(MA골든크로스+ADX) | 백테스트 완료, 게이트 미통과 | `backtest_report.json` `gate_passed: false` |
| Phase 2-실시간 | 뉴스 방향판단 | 진행 중 (D+1/D+5/D+20 대기) | 건드리지 말 것 |
| Phase 2-과거재현 | 뉴스 방향판단(재현) | 전수 수집 완료 | 게이트 미통과, baseline 이하 |
| Phase 3 | 펀더멘털 | 스펙만, 미착수 | 데이터소스 미결정이 블로커 |

```
PREDICTION_ENABLED = false   # 두 신호원 모두 §2.1 미통과로 비활성
```

**재개는 "이번엔 될 것 같다"는 판단으로 하지 않는다.** 다시 §2.1 게이트를
통과해야 한다는 조건은 CLAUDE.md에 고정돼 있다.

---

## 2. Phase 1 — 가격 신호 백테스트

파일: `backtest.py` (CLI/`backtest.yml`) → `backtest_report.json`

신호는 라이브 스캐닝(`entry_score`)과 **의도적으로 분리**돼 있다 — MA20/60
골든크로스 + ADX≥25가 메인 트리거, RSI≥75/상단밴드 위는 진입 스킵 필터로만
사용. 매도는 규칙 기반 근사치(하드손절/RSI과열/보유기간 2배 타임스톱) — 과거
Claude 판단을 재생할 수 없어서 쓰는 알려진 단순화.

### 고정 성공 기준 (`SUCCESS_CRITERIA`, 사후 조정 금지)

```
min_trades: 30, sharpe_meaningful: 1.0, sharpe_review: 0.5, mdd_limit_pct: -20
```

### 현재 저장된 게이트 결과

| 기준 | 통과 |
|---|---|
| 거래 30건 이상(훈련) | ✅ |
| 거래 30건 이상(검증) | ✅ |
| 검증구간 샤프 ≥ 1.0 | ❌ |
| 훈련/검증 동일부호(과최적화 징후 없음) | ❌ |
| Buy&Hold 대비 우위(훈련) | ❌ |
| Buy&Hold 대비 우위(검증) | ❌ |

**`gate_passed: false`** — 6개 중 2개만 충족.

MDD는 참고용이며 통과/실패 판정에서 제외(`MDD_CAVEAT`, `POSITION_WEIGHT`=10%
상수 스케일링 근사치 — 실제 동시보유/분산효과 미반영).

`--max-hold-multiplier`/`--rsi-exit`(청산 완화 실험)는 "1회만 시도, 반복
튜닝 금지" 전제로 이미 한 번 쓰인 것으로 기록돼 있음 — 재사용 전 결과부터
확인.

---

## 3. Phase 2 — 뉴스 방향판단 (두 트랙, 병합 금지)

`news_event_calibration_analysis.py --log-file <파일>`로 트랙별 개별 분석만
수행. `_meta.log_file`/`_meta.track`으로 항상 구분.

### 3-1. 실시간 트랙 — 최종 판단 기준, 불가침

`news_event_experiment.py` → `news_event_calibration_log.json`, 매일 cron.
D+1/D+5/D+20 실제 결과를 기다리는 중. **이 파일에는 손대지 않는다.**

### 3-2. 과거 재현 트랙 — 선행지표, 대체재 아님

파일: `news_event_backtest.py`(workflow_dispatch 전용) →
`news_event_backtest_log.json`

선정 기준은 수집 전 사전 고정(`Phase2_과거뉴스백테스트_설계.md`):
2026-02-01~07-31, 5일 간격 25개 판단일, `KRX_MARKET_CAP_TOP` 전수, 랜덤
샘플링 없음. 룩어헤드 방지: Claude API `tools` 없음 + RSS `pubDate` GMT→KST
하드 검증(실패 시 표본 0건).

**현재 저장된 수집 현황 (706건, 전수 완료)**

| pubdate_rejected | no_headlines | judge_failed | no_price | fetch_failed |
|---|---|---|---|---|
| 636 | 344 | 0 | 0 | 637 |

**결과 (`Phase2_과거뉴스백테스트_설계.md` §8)**

| window | 고확신군 | baseline | 격차 |
|---|---|---|---|
| D+1 | 46.28% | 55.10% | **-8.82p** |
| D+5 | 48.48% | 51.52% | **-3.04p** |
| D+20 | 48.62% | 54.15% | **-5.53p** |

6개 셀 전부 baseline 미달, 방향별 적중률이 기저비율과 사실상 동일(D+1 호재
40.0% vs 기저 40.1%) — 방향 판단에 정보가 없다는 결론.

**§4-1 전제 (통과 결과가 나오면 반드시 같이 인용)**: 이 트랙 단독 통과는
§2.1 게이트 통과로 인정하지 않는다. 판단모델이 그 시기 뉴스로 훈련됐다면
결과를 기억해낼 수 있고, 도구 차단으로는 이 오염을 막지 못한다 — 실시간
트랙에는 없는 문제.

promptB 실험: 470건 `judge_failed`로 재수집 필요 상태로 남아 있을 수 있음
— ECE 비교 지시가 오면 먼저 현재 상태 재확인.

---

## 4. Phase 3 — 펀더멘털 신호

파일: `Phase3_펀더멘털신호_스펙.md` (스펙만, 착수 전)

PER/PBR/EV-EBITDA 저평가, 부채비율 개선, 영업이익 흑자전환. 판단 시점은
분기 실적 발표 후. Phase 1 프레임(train/val 분할, 레짐 태깅, buy&hold 비교,
고정 성공기준) 재사용 전제.

**블로커**: 데이터 소스(재무제표 조회/스크레이핑) 미결정. 착수는 방향성
세션 승인 후 — 코드 세션 판단으로 데이터 소스를 정하지 않는다.

---

## 5. 지금 필요한 것 / 하지 말아야 할 것

| # | 항목 |
|---|---|
| 1 | `SUCCESS_CRITERIA`/Fisher's α구간/`MIN_BUCKET_SAMPLES`는 결과를 본 뒤 조정 금지 |
| 2 | 새 실행 결과의 통과/미통과 **판정**은 방향성 세션 몫 — 코드 세션은 숫자까지만 |
| 3 | `news_event_calibration_log.json`(실시간 트랙) 불가침 |
| 4 | `PREDICTION_ENABLED` 재활성화 금지 — 결과가 좋아도 방향성 세션 승인 사안 |
| 5 | 백그라운드/폴링 스크립트에 `git checkout`/`switch` 넣지 않기 |
| 6 | Phase 3 착수 전 데이터소스 결정 여부 확인 |

---

## 부록 — 지시 유형별 즉시 참조

| 지시 | 참조 |
|---|---|
| "백테스트 다시 돌려줘/파라미터 바꿔서" | §2 — 유니버스/lookback 변경 가능, `SUCCESS_CRITERIA`는 승인 필요 |
| "뉴스 트랙 결과 더 모아줘/기간 넓혀줘" | §3-2 — 선정 기준 고정, `--exec-from`/`--exec-until`로 구간만 나눠 이어서 수집 |
| "게이트 통과했는지 판정해줘" | 숫자는 보고, 통과/미통과 단정은 방향성 세션에 넘긴다고 명시 |
| "Phase 3 시작" | §4 — 데이터 소스 결정 여부부터 확인 |
| "결과 요약 문서 만들어줘" | 이 문서 형식 유지, 판정 문구 대신 사실 나열 톤 |
