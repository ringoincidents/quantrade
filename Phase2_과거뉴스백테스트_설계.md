# Phase 2 과거 뉴스 백테스트 트랙 — 선정 기준 사전 고정 문서

> **이 문서는 데이터를 수집하기 전에 쓴다.** 선정 기준을 먼저 못박고 그 다음에 돌린다 —
> 순서가 반대면(돌려보고 기준을 정하면) "유리한 사례만 골랐다"는 의심을 구조적으로
> 반박할 수 없다. `backtest.py`의 `SUCCESS_CRITERIA`,
> `news_event_calibration_analysis.py`의 유의성 판정기준과 같은 사전 고정 원칙.
>
> **작성 시점**: 2026-08-02 (실제 수집 실행 전)
> **구현**: `news_event_backtest.py` — 아래 기준은 전부 코드 상수로 박혀 있고,
> 수집된 데이터셋 파일에 실행 시 사용한 값이 그대로 기록된다(사후 대조 가능).

---

## 1. 목적과 실시간 트랙과의 관계

실시간 파이프라인(`news_event_experiment.py`)은 매일 조금씩 표본을 쌓는 방식이라
최초 코호트 D+20 도달이 2026-08-21이고, 캘리브레이션 판정에 쓸 만한 표본이 모이려면
그 뒤로도 몇 달이 더 걸린다. 과거(2026-02~07) 뉴스는 결과가 이미 나와 있으므로
D+20을 기다릴 필요 없이 즉시 대조할 수 있다 — 그래서 **병행 트랙**으로 추가한다.

- **실시간 트랙은 그대로 유지하고 중단하지 않는다.** `news_event_experiment.yml`
  (매일 09:30 UTC)도, `news_event_calibration_log.json`도 이 작업에서 건드리지 않는다.
- 두 트랙은 **별도 데이터셋 파일로 완전히 분리**한다(§5). 섞이면 나중에 어떤 숫자가
  과거 백테스트에서 나온 건지 실시간에서 나온 건지 구분할 수 없어진다.
- **두 트랙의 숫자는 서로 대체 가능하지 않다.** §4의 한계(특히 4-1 학습데이터 오염)
  때문에 과거 백테스트 결과는 실시간 트랙의 선행 지표/참고치일 뿐이고,
  §2.1 게이트 판정을 과거 백테스트만으로 대신할 수 없다.

## 2. 선정 기준 (사전 고정 — 실행 전 확정, 사후 변경 금지)

**무작위 추출이 아니라 전수(全數) 방식을 택했다.** 무작위는 시드를 고정해도 "왜 하필
그 시드냐"가 남지만, 전수는 고를 여지 자체가 없어서 선정 편향을 원천 차단한다.

| 항목 | 고정값 | 근거 |
|---|---|---|
| 대상 유니버스 | `backtest.KRX_MARKET_CAP_TOP` (38종목) 전수 | 실시간 트랙과 **동일한** 유니버스. 이 실험을 위해 새로 고른 종목이 하나도 없다 |
| 대상 기간 | 2026-02-01 ~ 2026-07-31 | 지시된 범위 그대로 |
| 판단일 간격 | 거래일 기준 5일마다(`JUDGMENT_DATE_STRIDE = 5`) | 주 1회 꼴. 매 거래일 전수는 API 비용이 과하고, 인접일 헤드라인이 중복돼 표본 독립성만 떨어진다 |
| 거래일 기준 | 005930(삼성전자) 일봉 달력 | 최대 유동성 종목이라 거래정지로 달력이 비는 일이 사실상 없음 |
| 헤드라인 조회 창 | 판단일 직전 3일(`HEADLINE_LOOKBACK_DAYS = 3`) | 실시간 트랙이 "오늘 시점 최근 뉴스"를 보는 것과 같은 위치 |
| 처리 순서 | 판단일 오름차순 → 유니버스 정의 순서 | 도중에 중단돼도 남는 건 "날짜 순 앞부분"이라 결과와 무관한 편향 없는 부분집합 |
| 판단 대상 필터 | 해당 창에 헤드라인이 1건 이상 있는 (종목, 날짜) 쌍 **전부** | 실시간 트랙의 필터(`if not headlines: continue`)와 **완전히 동일**. 사건 유형·종목·결과로 거르는 조건은 **없다** |

**결과를 보고 거르는 조건은 하나도 없다.** 수익률·방향·확신도·사건유형 어느 것으로도
표본을 제외하지 않는다. 판단이 파싱 실패했거나 가격 데이터가 없는 경우만 제외되며,
그 건수는 데이터셋에 `skipped` 카운트로 남는다.

`--stride`/`--limit`/`--window-start`/`--window-end` CLI 옵션이 있지만 **기본값이 위 표의
고정값**이고, 실행에 실제로 쓰인 값은 데이터셋의 `selection_criteria` 블록에 그대로
기록된다 — 나중에 누가 기준을 바꿔 돌렸는지 파일만 봐도 드러난다.

## 3. 룩어헤드(미래 정보 유입) 차단 — 3중 방어

판단 시점 이후의 정보가 판단에 새어 들어가면 이 실험은 통째로 무효다. 3겹으로 막는다.

**3-1. 외부 도구 접근 차단 (지시사항 2)**
`ask_backtest_event_judgment()`는 Anthropic API를 호출할 때 `tools` 파라미터를 아예
넘기지 않는다 — web_search를 비롯한 어떤 도구도 모델에게 주어지지 않으므로 모델은
헤드라인 텍스트 외의 정보를 조회할 수단이 물리적으로 없다. 코드에 이 사실을 단언하는
주석과 함께, 요청 본문에 `tools` 키가 없음을 self-test에서 검증한다.

**3-2. 헤드라인 발행일 강제 검증 (가장 중요)**
Google News RSS 검색에 `after:`/`before:` 날짜 연산자를 넣지만, **그 필터가 실제로
동작한다고 신뢰하지 않는다.** 반환된 각 항목의 `pubDate`를 직접 파싱해 판단 창
(판단일 -3일 ~ 판단일) 밖의 항목은 **전부 버린다**. 날짜 연산자가 무시되는 상황에서
2월 판단에 8월 기사가 섞여 들어가는 게 이 실험의 최악의 실패 모드인데, 이 검증이
있으면 그 경우 표본이 그냥 0건에 수렴할 뿐(안전한 실패) 오염된 데이터가 쌓이지 않는다.
버려진 건수는 `pubdate_rejected`로 집계해 필터 동작 여부를 사후에 확인할 수 있게 한다.

> **실측 검증 (2026-08-02, 150건 수집분)**: 이 방어층이 실제로 필요했다.
> `rejected_samples`에 남은 폐기 기사 40건의 발행일이 **전부 판단일 +1일**이었다 —
> 즉 Google은 `before:` 경계를 넘긴 "판단일 다음날" 기사를 그대로 돌려주고 있고,
> pubDate 검증이 없었다면 그 기사들이 판단 프롬프트에 들어가 D+1 수익률을 미리
> 알려주는 꼴이 됐을 것이다. 검증 통과분 391건은 판단일 대비 offset이 전부
> {-3, -2, -1, 0}에 들어갔고 양수 offset은 0건이다(창 위반 0건).

**3-3. 프롬프트에 가격/결과 정보 미포함**
실시간 트랙과 동일하게 헤드라인 원문만 준다. 판단일 종가, 이후 수익률, 심지어 "이건
과거 데이터"라는 사실조차 프롬프트에 넣지 않는다 — 모델이 회고적 상황임을 알면
답변 태도가 달라질 수 있다.

## 4. 알려진 한계 — 이 트랙이 실시간 트랙을 대체할 수 없는 이유

**4-1. 학습데이터 오염 (구조적으로 해결 불가)**
판단 모델이 2026-02~07 기간을 학습데이터로 포함하고 있다면, 헤드라인만 보고도 그
사건이 어떻게 끝났는지 "기억"할 수 있다. **§3의 도구 차단은 이 문제를 전혀 막지 못한다**
(도구를 안 써도 파라미터 안에 이미 들어있는 지식이므로). 이건 회고적 백테스트에만
존재하고 실시간 트랙에는 존재하지 않는 위험이며, 이 트랙의 캘리브레이션이 실시간보다
좋게 나올 경우 "모델이 잘 판단했다"와 "모델이 이미 알고 있었다"를 구분할 방법이 없다.
- 완화책: 판단에 쓴 모델 ID를 데이터셋에 기록(`judge_model`)하고, 결과 해석 시 항상
  이 한계를 함께 인용한다. 실시간 트랙 표본이 쌓이면 두 트랙의 캘리브레이션을 비교해
  격차 자체를 오염 정도의 간접 지표로 볼 수 있다.
- **따라서 과거 백테스트에서 "강한신호"가 나와도 그것만으로 §2.1 게이트 통과를
  선언하지 않는다.**

**4-2. 뉴스 검색의 현재 시점 편향**
Google News RSS는 지금 시점에 색인·랭킹된 기사를 돌려준다. 당시엔 크게 다뤄졌지만
지금 색인에서 빠진 기사는 안 나오고, 반대로 나중에 중요해진 사건의 기사가 과대
대표될 수 있다. 당시 실제로 보였을 뉴스 분포와 같지 않다.

**4-3. 유니버스 생존 편향**
`KRX_MARKET_CAP_TOP`은 2026-08 시점의 대형주 스냅샷이다. 2~7월 사이 대형주에서
이탈한 종목은 빠져 있고 새로 진입한 종목은 들어와 있다 — `backtest.py`가 이미 안고
있는 한계와 같은 성격.

**4-4. 헤드라인의 당일 주가 정보 포함**
"급등"·"신고가" 같은 표현이 헤드라인에 있으면 당일 방향이 새어 들어간다. 다만 이건
실시간 트랙도 똑같이 안고 있는 한계라 두 트랙 간 비교 자체는 공정하다. D+1에 더
크게, D+20에 더 작게 작용한다.

**4-5. 기간 말미는 아직 미도래**
오늘이 2026-08-02이므로 판단일이 2026-07-13 이후인 건은 D+20이 아직 오지 않았다.
해당 outcome은 `null`로 남고, 나중에 재실행하면 채워진다(실시간 트랙의 `fill_outcomes`와
같은 방식). 이 구간은 회고가 아니라 사실상 전향적 표본이라 4-1 오염에서 자유롭다.

## 5. 데이터 분리 (지시사항 4)

| | 실시간 트랙 | 과거 백테스트 트랙 |
|---|---|---|
| 수집 스크립트 | `news_event_experiment.py` | `news_event_backtest.py` |
| 데이터 파일 | `news_event_calibration_log.json` | `news_event_backtest_log.json` |
| 워크플로 | `news_event_experiment.yml` (매일 자동) | `news_event_backtest.yml` (**`workflow_dispatch` 전용, 스케줄 없음**) |
| 레코드 `track` 필드 | (없음 = 실시간) | `"backtest"` |
| 대시보드 표시 | Phase 2 진행상황 카드 | 표시 안 함(이 작업 범위 밖) |

두 파일은 서로 다른 스크립트가 서로 다른 워크플로에서만 쓰므로 커밋 레이스도 없다.
분석은 같은 `news_event_calibration_analysis.py`에 `--log-file`로 대상 파일을 지정해서
돌린다 — 분석 로직은 공유하되 데이터는 절대 합치지 않는다. 출력 JSON 최상단
`_meta.log_file`에 어느 데이터셋을 분석한 건지 항상 박힌다.

## 6. 실행

```bash
# 로컬(네트워크 되는 환경): 소규모로 먼저 확인
python news_event_backtest.py --limit 20

# 전체 수집 — GitHub Actions에서 수동 실행(workflow_dispatch)
#   Actions > News Event Backtest (Historical) > Run workflow

# 수집된 과거 데이터셋 분석
python news_event_calibration_analysis.py --log-file news_event_backtest_log.json

# 실시간 트랙 분석(기존 그대로, 영향 없음)
python news_event_calibration_analysis.py
```

**네트워크 제약 메모**: 이 설계·구현이 이뤄진 샌드박스에서는 `news.google.com`과
`api.finance.naver.com`이 정책상 차단(403)돼 있어 실제 수집을 로컬에서 검증하지
못했다. 로직 검증은 네트워크를 타지 않는 self-test(`--self-test`)로 했고, 실제 수집
동작은 Actions 실행 로그로 확인해야 한다 — 특히 §3-2의 `pubdate_rejected` 카운트와
실제 수집 건수를 첫 실행 후 반드시 눈으로 확인할 것(날짜 필터가 통째로 무시되면
수집 0건으로 나타난다).

---

## 7. 1차 수집 결과 (2026-08-02, 150건 / 판단일 7개)

**아직 사전 고정 기준(§2)의 전수 수집이 아니다** — `--limit 150`으로 날짜 오름차순
앞부분만 받아서 2026-02-02 ~ 2026-03-20의 판단일 7개(전체 25개 중)에 몰려 있다.
아래 숫자는 파이프라인 검증 결과이지 §2.1 판정이 아니다.

| window | 고확신 승률(n=91) | 저확신 승률(n=42) | 고확신>저확신 | p(단측) | 판정 | ECE |
|---|---|---|---|---|---|---|
| D+1 | 45.05% | 30.95% | ✅ | 0.0878 | 약한신호 | 0.313 |
| D+5 | 47.25% | 40.48% | ✅ | 0.2944 | 유의미하지 않음 | 0.303 |
| D+20 | 31.87% | 42.86% | ❌ 역전 | 0.9225 | 유의미하지 않음 | 0.375 |

**읽을 때 주의할 점:**
- **ECE 0.30~0.38은 심한 과신**이다. 고확신군 평균 확신도가 ~78인데 실제 적중률은
  45%(D+1)/32%(D+20)다. 적중률이 세 window 모두 50%를 밑돈다.
- **방향 판단이 강하게 호재 편향**: 150건 중 호재 111 / 중립 17 / 악재 22. 반면
  수집 구간의 실제 D+1은 150건 중 양수가 53건(35%)뿐이었다 — 하락 구간에서
  계속 호재를 외친 셈이고, 이게 낮은 적중률의 주된 성분으로 보인다.
- **날짜 편중이 심하다**: 판단일이 7개뿐이라 같은 날 판단들은 같은 장세를 공유해
  사실상 독립표본이 아니다. 실제로 D+1 양수 비율이 날짜별로 22/23(2/02)에서
  0/23(3/06), 0/18(3/20)까지 갈린다. 전수 수집(25개 판단일, 4~7월 포함)을
  마치기 전에는 이 숫자로 결론을 내면 안 된다.
- 그럼에도 **사전 고정 기준을 결과를 보고 바꾸지 않는다**(§2.1 판정기준은
  `news_event_calibration_analysis.py` 상단에 못박혀 있음). D+20이 역전으로 나온
  것도 그대로 기록한다.

**남은 작업**: `--limit` 없이 전수 수집을 마치면 판단일 25개(2~7월 전 구간)로
날짜 편중이 크게 완화된다. 그때 다시 분석하고, 그 결과도 §4-1(학습데이터 오염)
때문에 실시간 트랙을 대체하지 못한다는 전제는 동일하게 적용한다.

---

## 8. 전수 수집 결과 (2026-08-02)

*(수집 완료 후 채움 — 아래 §8-0 전제는 결과와 무관하게 미리 못박는다)*

### 8-0. 결과를 보기 전에 확정하는 전제 (§4-1 재명시)

**전수 수집을 마쳐도 이 트랙만으로 계획서 v3.1 §2.1 게이트 통과를 선언하지 않는다.**
표본이 150건에서 늘어나 통계적 검정력이 올라가더라도, 그것이 해결하는 것은 §4-2~4-4
(표본 편중·생존 편향)의 일부일 뿐이고 **§4-1 학습데이터 오염은 표본을 아무리 늘려도
줄지 않는다.** 판단 모델(`judge_model`)이 2026-02~07을 학습했다면 헤드라인만으로
결말을 "기억"할 수 있고, 도구 차단(§3-1)도 pubDate 검증(§3-2)도 이 경로는 막지 못한다
— 둘 다 *외부 조회*를 막는 장치이지 *파라미터에 이미 들어있는 지식*을 막는 장치가
아니기 때문이다.

따라서 결과가 어느 쪽으로 나오든 해석은 다음과 같이 비대칭적이다:

| 전수 결과 | 해석 |
|---|---|
| 캘리브레이션이 **성립**(고확신>저확신, 유의) | "모델이 잘 판단했다"와 "모델이 이미 알고 있었다"를 **구분할 수 없다** → 게이트 통과 근거로 쓰지 않음. 실시간 트랙에서 재현되는지 확인해야 비로소 의미가 생김 |
| 캘리브레이션이 **불성립** | 오염이 있었다면 오히려 유리하게 나왔어야 하는데도 안 나온 것이므로, 이 방향의 증거는 상대적으로 신뢰할 만하다(다만 §4-2~4-4 한계는 남음) |

**판정 권한은 실시간 트랙에 있다.** 이 트랙은 선행 지표이고, 실시간 트랙
(`news_event_calibration_log.json`)은 이 작업 내내 중단 없이 계속 돈다.

### 8-1. 수집 결과 (전수 완료)

- **706건 / 판단일 25개 전부 (2026-02-02 ~ 2026-07-31)** — 사전 고정 기준대로 전수 완료.
  `selection_criteria.defaults_used: true`, `judgment_dates_count: 25`.
- **룩어헤드 최종 검증**: 판단에 쓰인 기사 **2205건 전부**의 KST 발행일을 대조한 결과
  판단 창 위반 **0건**. offset 분포 `{-3: 449, -2: 479, -1: 593, 0: 684}` — 양수(미래
  기사) 0건. 반면 창 밖이라 폐기한 기사가 636건 있었다(§3-2 방어층이 계속 일했다는 뜻).
- **outcome 충족도**: D+1/D+5 각 668/706(94.6%), D+20 606/706(85.8%). 미충족분은
  전부 판단일 7/16·7/24·7/31 건으로 §4-5대로 D+20이 아직 안 왔다 — 이 구간은 회고가
  아니라 사실상 전향적 표본이라 §4-1 오염에서 자유롭다.
- 수집 과정: Google News RSS 레이트리밋 때문에 호출 유예를 0.2초→1.5초로 올리고
  실행을 3구간으로 나눠 돌렸다. **분할은 판단일 선정을 바꾸지 않는다**(전체 기간에서
  뽑은 판단일 중 처리 구간만 고르는 `--exec-from/--exec-until` 방식, self-test [8]로 검증).

### 8-2. 4개 축 비교 — 7개 판단일(150건) vs 전수(706건)

| window | 구분 | 고확신 | 저확신 | 방향 | p(단측) | 판정 | ECE |
|---|---|---|---|---|---|---|---|
| D+1 | 7개 판단일 | 45.05% (n=91) | 30.95% (n=42) | 정상 | 0.0878 | 약한신호 | 0.313 |
| D+1 | **전수** | 46.28% (n=363) | 37.70% (n=183) | 정상 | **0.0343** | **강한신호** | 0.291 |
| D+5 | 7개 판단일 | 47.25% (n=91) | 40.48% (n=42) | 정상 | 0.2944 | 유의미하지 않음 | 0.303 |
| D+5 | **전수** | 48.48% (n=363) | 45.90% (n=183) | 정상 | 0.3159 | 유의미하지 않음 | 0.250 |
| D+20 | 7개 판단일 | 31.87% (n=91) | 42.86% (n=42) | **역전** | 0.9225 | 유의미하지 않음 | 0.375 |
| D+20 | **전수** | 48.62% (n=325) | 42.69% (n=171) | **정상** | 0.1224 | 유의미하지 않음 | 0.251 |

**축1 — 50% 미만 패턴: 유지된다.** 6개 셀(고확신/저확신 × 3 window) 전부 여전히 50%
미만이다. 가장 높은 D+20 고확신도 48.62%로 동전던지기에 못 미친다. 표본이 4.7배로
늘어도 이 패턴은 안 깨졌다.

**축2 — D+20 역전: 해소됐다.** 기준선의 역전(31.87% < 42.86%)은 **날짜 편중 아티팩트**였다.
7개 판단일이 전부 2~3월(하락 구간)에 몰려 있어서 생긴 현상이고, 25개 판단일로 넓히자
정상 방향(48.62% > 42.69%)으로 돌아왔다. 다만 p=0.1224로 여전히 유의하지 않다.

**축3 — ECE: 개선됐지만 여전히 심각하다.** 0.313/0.303/0.375 → 0.291/0.250/0.251.
수치는 내려갔지만 의미는 그대로다 — 고확신군 평균 확신도가 78.1인데 실제 적중률은
46.3%(D+1)/48.6%(D+20)로 **약 30%p 괴리**다. 표본이 늘어 극단값이 희석된 것이지
모델이 덜 과신하게 된 게 아니다.

**축4 — 방향 편향: 완화됐지만 유지된다.** 호재 74.0%→66.9%, 중립 11.3%→18.0%,
악재 14.7%→15.2%. 여전히 3분의 2가 호재다. 그런데 수집 구간의 실제 D+1 양수 비율은
39.5%(중앙값 -0.79%)로 하락 우위 장세였다. 방향별 적중률을 보면 원인이 분명하다 —
**호재 판단 적중률 40.0%(177/443) vs 악재 판단 적중률 58.3%(60/103)**. 즉 모델은
악재를 짚을 때는 평균 이상인데, 호재를 과다 생산하면서 전체 적중률을 끌어내리고 있다.

### 8-3. 판정 — §8-0에서 미리 정한 대로

D+1이 **강한신호(p=0.0343)** 로 나왔다. §8-0의 비대칭 해석표를 그대로 적용한다:

> 캘리브레이션이 **성립**하는 경우 → "모델이 잘 판단했다"와 "모델이 이미 알고 있었다"를
> **구분할 수 없다** → 게이트 통과 근거로 쓰지 않음.

따라서 **이 결과로 계획서 v3.1 §2.1 게이트 통과를 선언하지 않는다.** 판단 모델
(`claude-sonnet-4-6`)이 2026-02~07을 학습했을 가능성이 남아 있고, 도구 차단(§3-1)도
pubDate 검증(§3-2)도 그 경로는 막지 못한다. 표본을 150건에서 706건으로 늘린 것은
§4-2~4-4(날짜 편중)를 완화했을 뿐 §4-1은 전혀 건드리지 못했다 — 실제로 이번에
해소된 D+20 역전이 바로 날짜 편중 쪽 문제였다는 점이 그 구분을 잘 보여준다.

**덧붙여, 강한신호가 나왔다고 해서 이 모델이 쓸 만하다는 뜻도 아니다.** "고확신군이
저확신군보다 낫다"는 상대적 진술이고, 절대 수준은 여전히 세 window 모두 50% 미만에
ECE 0.25~0.29다. 상대적 서열은 있는데 절대 정확도와 확신도 보정이 둘 다 실패한 상태다.

**판정 권한은 실시간 트랙에 있다.** 이 트랙은 선행 지표로만 쓰고, 실시간 트랙
(`news_event_calibration_log.json`, 최초 코호트 D+20 = 2026-08-21)이 중단 없이 계속
돈다. 두 트랙 수치가 갈리면 그 격차 자체가 §4-1 오염 정도의 간접 지표가 된다.
