# QuanTrade 상태 보고서 — 2026-08-04

> 이 문서는 **현재 상태 요약**이다. 결정을 내리지 않으며, 게이트 판정이나 Track B
> 충족 여부 같은 판정성 서술은 담지 않는다(CLAUDE.md 세션 시작 체크리스트).

---

## 1. 한눈에

| 스위치 | 값 | 의미 |
|---|---|---|
| `PREDICTION_ENABLED` | `false` | 매수/매도 방향 판단 중단 (v3.2) |
| `RULE_BASED_AUTOEXEC_ENABLED` | `false` | 규칙 기반 자동실행 미활성 (판정만) |
| `AI_SUGGESTION_DRY_RUN` | `true` | 실계좌 AI 제안 dry-run |

**주문을 넣는 코드는 저장소에 없다.** `autoexec.place_sell_order()`는 미구현이며,
호출되면 `OrderLayerUnavailable`을 던지고 "실행 불가"로 로깅된다. 위 플래그를
`true`로 바꿔도 현재는 주문이 나가지 않는다.

### 자동 실행 중인 워크플로

| 워크플로 | 주기 | 하는 일 |
|---|---|---|
| `daily.yml` | 매일 11:00 UTC | 하드 손절 + 규칙 위반 점검 (예측 없음) |
| `poll.yml` | 15분마다 | 텔레그램 명령 수신 |
| `sync_real.yml` | 하루 4회 | 실계좌 **조회** 전용 동기화 |
| `news_event_experiment.yml` | 매일 09:30 UTC | 실시간 캘리브레이션 트랙 (검증용, 계속) |
| `news_event_cards.yml` | 매일 09:50 UTC | 뉴스 사건 설명 카드 (예측 아님) |
| `portfolio_report.yml` | 일요일 23:10 UTC | 주간 포트폴리오 리포트 |

수동 실행 전용: `backtest.yml`, `news_event_backtest.yml`,
`news_event_prompt_experiment.yml`

---

## 2. v3.2 전환 — 완료

AI 역할을 **알파 생성 → 리스크 가드레일 + 뉴스 설명**으로 재정의했다.

**근거**: 가격 신호와 뉴스 방향 판단이 §2.1 게이트 미통과. 특히 뉴스 방향 판단은
**단순 다수클래스 baseline보다도 적중률이 낮았다.**

| window | 고확신군 | baseline | 격차 |
|---|---|---|---|
| D+1 | 46.28% | 55.10% | **-8.82p** |
| D+5 | 48.48% | 51.52% | **-3.04p** |
| D+20 | 48.62% | 54.15% | **-5.53p** |

고확신·저확신 6개 셀 중 baseline을 넘는 것이 하나도 없었고, 방향별 적중률이
기저비율과 사실상 동일했다(D+1 호재 40.0% vs 기저 40.1%). **방향 판단에 정보가
없다**는 뜻이다.

**바뀐 것**
- 예측 경로 차단 (코드는 전부 보존 — 삭제 아님, 재개 시 §2.1 재통과 필요)
- 이전에 쌓인 대기 건이 `/approve`로 뒤늦게 체결되는 구멍도 함께 막음
- 대시보드 승인 카드 → "연구 종료" 라벨, 상호작용 완전 제거 (버튼 0개)
- 신규 카드 2종을 **런타임 가드로** 분리: 예측성 필드가 섞이면 렌더 거부 + 콘솔 경고
- 뉴스 파이프라인 재사용 → 사실 설명만 생성 (스키마에 `direction`/`confidence` 없음)

---

## 3. 규칙 기반 자동실행 — 1·2단계 완료, 3단계 막힘

방향성 세션 2026-08-04 정식 승인. 지시된 순서를 지켜 진행 중.

### ✅ 1단계 — 킬스위치 테스트

`/autoexec_stop` → 상태 영속 → 재시작 후에도 유지 → ON 상태에서 실행 0건, 차단
로그 4건 → 멱등성 → `/autoexec_start`로만 해제. **전항목 통과.**

### ✅ 2단계 — 안전장치 검증

- 전량 로깅: 3규칙 × 3종목 = 9건이 발동/미발동 사유와 함께 기록
- 유예기간: 당일 재발동 차단 → 7일 경과 후 자동 해제
- 매수 경로 부재: `sell` 외 action 6종 거부, 네임스페이스에 매수 심볼 0개
- 목표가 AI 미개입: AI 관련 심볼 0개, 미입력 시 발동 0건(기본값 생성 안 함)

### ⛔ 3단계 — 토스 주문 API 스펙 없음

저장소에 연동된 엔드포인트는 `/accounts`, `/holdings`, `/buying-power`,
`/exchange-rate`, `/oauth2/token` 다섯 개뿐. 주문 엔드포인트·요청 스키마·주문유형
enum·멱등키 처리 중 **아무것도 알려져 있지 않다.** 실계좌 주문에서 이것들을
추측으로 채울 수 없어 미구현으로 두었다.

### 실계좌 판정 결과 (플래그 false)

```
⏸️ [집중도리밸런싱] SCHD 54주 — 비활성(판정만)
   비중 60.78%가 기준 30% 초과 — 초과분 1,325,633원,
   1회 상한 785,286원 적용해 785,286원 매도
```

### ⚠️ dry-run 생략에 대해

사후통보·로깅·킬스위치·레이트리밋은 전부 *잘못된 실행이 일어난 뒤*에 작동한다.
수량·종목을 잘못 계산하는 버그 자체를 실행 전에 잡는 건 dry-run뿐이다.

**가정이 아니라 실제로 일어났다**: 토스 API가 `quantity`/`current_price`를
**문자열**(`'1'`, `'347500'`)로 주는데 self-test 픽스처는 정수여서 통과하고
있었고, 실계좌 데이터에서 수량 계산이 크래시했다. 크래시가 아니라 조용히 틀린
수량이었다면 사후통보는 "54주 매도했습니다"라고 알려줄 뿐이었을 것이다.
`_num()` 강제 변환과 문자열 픽스처 회귀 테스트로 막았다.

결정을 뒤집자는 게 아니라 기록이다.

---

## 4. 포트폴리오 리포트 — 완료, 확인 1건 대기

실계좌 현황 계산 + 사전 정의 규칙 판정. 읽기 전용, 실행 버튼 없음.

```
총자산 4,286,697원 · 현금 0.0% · 보유 8종목
자산군: US 70.4% / KR 29.6%
📐 규칙 해당 1건 [초안 기준]
   · [집중도] SCHD 비중이 총자산의 60.93%로 기준 30% 이상
```

### 배분 로드맵 — 숫자가 60만원 갈린다

| 계급 | 선언 `months` | 진급시점 역산 | 구간 |
|---|---|---|---|
| 일병 | 5개월 | **6개월** | 2026-07 ~ 2027-01 |
| 상병 | 6개월 | 6개월 | 2027-01 ~ 2027-07 |
| 병장 | 4개월 | **3개월** | 2027-07 ~ 2027-09 |

합은 둘 다 15개월인데 단가가 다른 계급 사이에서 한 달이 옮겨가 **총액이
16,950,000원 vs 16,350,000원**으로 갈린다(병장 145만 − 일병 85만 = 60만).

어느 쪽도 조용히 이기게 두지 않았다. `month_basis`로 명시 선택(기본
`declared` — 주신 숫자를 임의로 안 바꿈), 불일치는 어느 기준을 골라도 항상 표시.
화면에서도 일병이 2026-11에 끝나는데 상병이 2027-01에 시작해 **빈 두 달이
눈에 보인다.**

전역일은 입대 2026-04 + 18개월(육군 기준)로 2027-09을 넣었다 — **가정이다.**

---

## 5. Phase 3 펀더멘털 신호 — 스펙만, 착수 전

분기 실적 발표 기준 신호를 세 번째 후보로 검토. 다음 방향성 세션 승인 후 착수.

- Phase 1 프레임 재사용 경계를 표로 명시 (판정부는 손대지 않음)
- 표본 산술 확인: 38종목 × 4분기 × 5년 = 760 → 하위 30분위면 훈련 160/검증 68
- **최대 위험 = point-in-time 문제**: 오늘 조회한 재무제표는 과거 시점에 알 수
  있던 값이 아닐 수 있다(정정공시/재작성/공시지연). 결과를 좋게 만드는 방향으로
  작동해서, 좋은 결과가 나와도 신뢰할 수 없게 된다
- **블로커**: 펀더멘털 데이터 소스가 저장소에 하나도 없다. 새 외부 커넥터 추가는
  CLAUDE.md 조항에 걸려 승인 필요

---

## 6. 정정 — 프롬프트 A/B 실험은 전수 완료가 아니다

앞선 보고에서 "전수 재판단(706건) 완료"라고 했는데 **부정확했다.**

```
promptB 수집: 236건 / 706건 (33%)
judge_failed: 470건
```

워크플로는 성공으로 끝났지만 판단 파싱이 **470건 실패**했다. 프롬프트 B가 길고
지시가 많아 모델이 순수 JSON을 벗어난 응답을 냈을 가능성이 높다. 원인은 아직
확인하지 않았다.

**영향은 제한적이다** — v3.2에서 방향 판단 자체를 접었으므로 이 실험은 참고
기록에 가깝다. 다만 이 데이터로 ECE 비교를 하려면 실패분 재수집이 먼저다.

---

## 7. 지금 필요한 것

| # | 항목 | 없으면 막히는 것 |
|---|---|---|
| 1 | **토스 주문 API 스펙** | 자동실행 3단계 진행 불가 |
| 2 | **`month_basis` 결정** (`declared` / `dates`) | 로드맵 총액 60만원 미확정 |
| 3 | **`target_prices.json` 목표가** | 목표가 부분익절 규칙 미동작 (현재 0건 등록) |
| 4 | 임계값 확정 (30% / -50% / 60일) | 현재 전부 `provisional: true` |
| 5 | 종목→자산군 매핑 | 목표 배분 대비 현재 갭 계산 불가 |
| 6 | Phase 3 데이터 소스 결정 | Phase 3 착수 불가 |

### 권장

**킬스위치를 실제로 한 번 눌러보시길.** 코드 레벨에서만 검증했다. `poll.yml`이
도는 실제 환경에서 `/autoexec_stop` → `/autoexec_status` → `/autoexec_start`
왕복을 확인해두는 게 좋다 — 눌러본 적 없는 킬스위치는 있다고 보기 어렵다.

---

## 부록 — 이번 세션 주요 커밋

| 커밋 | 내용 |
|---|---|
| `91ca809` | v3.2 전환 병합 (4커밋) |
| `eec042d` | 세션 규칙: 백그라운드 스크립트 브랜치 전환 금지 |
| `68ad65d` | Phase 3 펀더멘털 스펙 |
| `508aac3` | 포트폴리오 리포트 생성기 |
| `2ed8c76` | 수입 스케줄/배분표 실제값 반영 |
| `3efd2f3` | 자동실행 킬스위치 + 안전장치 |
| `f49fb9a` | 복무 날짜 반영, 개월수 불일치 노출 |
