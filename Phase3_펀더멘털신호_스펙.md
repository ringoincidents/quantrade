# Phase 3 펀더멘털 신호 — 설계 스펙 (착수 전)

> **이 문서는 스펙이다. 구현도, 백테스트 착수도 아니다.**
> 실제 착수는 다음 방향성 세션(claude.ai) 승인 후에 한다. 이 문서의 목적은
> 승인 회의에서 판단할 수 있도록 **무엇을 어떻게 검증할지, 그리고 무엇이 아직
> 막혀 있는지**를 미리 정리해두는 것이다.
>
> **작성 시점**: 2026-08-03 (데이터 수집 전, 코드 작성 전)
> **선행 문서**: `CLAUDE.md` v3.2 절, `Phase2_과거뉴스백테스트_설계.md`(사전 고정
> 방법론의 선례), `backtest.py`(재사용할 Phase 1 프레임)

---

## 0. v3.2와의 관계 — 먼저 못박아둘 것

v3.2에서 **예측(BUY/SELL 방향 판단)은 비활성**이다. 가격 신호와 뉴스 방향 판단이
둘 다 §2.1 게이트를 통과하지 못했기 때문이다. Phase 3은 그 결정을 뒤집는 게
아니라, **세 번째 신호원 후보를 같은 게이트에 걸어보는 연구**다.

따라서:

- **이 스펙이 승인돼도 예측이 재활성되는 게 아니다.** 백테스트를 돌릴 권한이
  생기는 것뿐이다.
- **백테스트를 통과해도 자동으로 재활성되지 않는다.** v3.2 재개 조건은 "§2.1
  게이트 통과"이고, 통과 여부와 별개로 *재활성 여부*는 방향성 세션의 결정이다.
  두 단계를 하나로 합치지 않는다.
- 통과하지 못하면 Phase 1·2와 같은 처리를 한다 — 코드/데이터를 남기고 실행
  경로만 끊는다(v3.2 보존 원칙).

## 1. 왜 펀더멘털인가 — 시간 제약과의 관계

기존 두 신호원은 **일 단위 판단**을 전제했다. 사용자는 평일 06–09시에만 PC를
쓸 수 있어(군 복무 중, `CLAUDE.md` 프로젝트 컨텍스트) 일 단위 신호는 운영 자체가
빠듯했다. 펀더멘털 신호는 **분기 실적 발표 시점에만 판단**하므로 판단 빈도가
연 4회 × 종목수로 떨어진다 — 시간 제약이 사실상 사라진다.

이건 "정확도가 더 높을 것 같아서"가 아니라 **운영 가능성**이 주된 근거다.
정확도는 게이트가 판정한다. 이 문서는 그 판정을 미리 앞당겨 쓰지 않는다.

## 2. 후보 지표

지시받은 후보군을 세 갈래로 정리한다. **어느 것을 최종 채택할지는 §6의 사전
고정 단계에서 확정하며, 데이터를 본 뒤에 고르지 않는다.**

### 2-1. 저평가 스크리닝 (밸류에이션 수준)

| 지표 | 정의 | 비고 |
|---|---|---|
| PER | 시가총액 / 지배주주 순이익(TTM) | 적자면 정의 불가 → 결측 처리(§4-3) |
| PBR | 시가총액 / 지배주주 지분 | 금융/지주는 업종 특성상 구조적으로 낮음 |
| EV/EBITDA | (시총 + 순차입금) / EBITDA | 자본구조 차이를 중화. 계산에 부채·현금·감가상각 필요 |

**단일 절대 임계값(예: PER < 10)은 쓰지 않는다.** 업종별로 정상 범위가 달라
절대 컷은 특정 업종만 계속 뽑는 결과가 된다. **동일 시점 유니버스 내 백분위**로
정규화한 뒤 순위를 쓴다(예: 하위 30분위).

### 2-2. 재무 개선 추세 (레벨이 아니라 변화)

| 지표 | 정의 |
|---|---|
| 부채비율 개선 | 부채총계/자기자본이 직전 N분기 대비 하락 추세 |
| 영업이익 흑자전환 | 직전 분기 영업이익 < 0 → 당분기 ≥ 0 |
| 영업이익률 개선 | 영업이익/매출액의 전년동기 대비 증가 |

§2-1이 "싼가"를 본다면 §2-2는 "나아지고 있는가"를 본다. 저평가만 보면 계속 싼
채로 있는 종목(value trap)을 잡을 위험이 있어 개선 추세를 같이 요구하는 구성이
자연스럽지만, **두 갈래를 AND로 걸지 OR로 걸지, 가중합으로 갈지는 §6에서 확정**한다.

### 2-3. 애니플러스 분석에서 쓴 지표 — 대조 필요 ⚠️

지시에 "지난번 애니플러스 분석에서 쓴 지표들 참고"가 있었는데, **그 분석 문서는
이 저장소에 없다**(외부 문서). 따라서 위 목록은 지시문에 명시된 지표
(PER/PBR/EV-EBITDA, 부채비율 개선, 영업이익 흑자전환)만으로 구성했고, 그 분석에서
실제로 쓴 지표와 대조하지 못했다.

**승인 회의 전에 그 문서를 주시면 목록을 맞추겠다.** 없는 내용을 추측해서 채우면
"어떤 지표를 왜 골랐는가"의 근거가 흐려지고, 사전 고정의 의미도 약해진다.

## 3. 판단 시점 — 분기 실적 발표 기준

**판단 시점 = 분기 보고서 공시일 + K 거래일** (K는 §6에서 고정, 기본안 K=1).

- 기준을 "분기말"이 아니라 **"공시일"** 로 잡는 게 핵심이다. 3월 말 결산 수치는
  5월 중순에야 공시되므로, 분기말 기준으로 판단하면 **아직 공개되지 않은 정보로
  거래하는 셈**이 된다(§4-1).
- K=1인 이유: 공시 당일 종가는 공시 시각(장중/장후)에 따라 반영 여부가 갈린다.
  하루 미루면 그 모호함이 사라진다. K=0과 K=1 중 어느 쪽인지도 사전 고정 대상이다.
- 보유 기간은 **다음 분기 공시까지**를 기본으로 한다(약 60~65거래일). Phase 1처럼
  하드 손절과 시간 스톱을 병행할지는 §6에서 확정.

## 4. 검증을 무효화할 수 있는 위험 — 착수 전에 해결해야 할 것들

Phase 2에서 Google의 `before:` 연산자가 판단일 다음날 기사를 돌려주던 문제를
`pubDate` 검증으로 막았던 것과 **같은 성격의 위험**이 펀더멘털에는 더 크게 있다.

### 4-1. Point-in-time 문제 (최대 위험) 🔴

오늘 조회한 재무제표는 **오늘 시점의 값**이다. 과거 어느 시점에 실제로 알 수
있었던 값과 다를 수 있다:

- **정정공시/재작성(restatement)**: 회계 오류나 기준 변경으로 과거 수치가 사후에
  바뀐다. 지금 보이는 2023년 영업이익이 2023년 당시 공시된 값이 아닐 수 있다.
- **공시 지연**: 위 §3의 이유.
- **분기 데이터의 소급 조정**: 연결 범위 변경, 중단사업 재분류 등.

이게 오염되면 **"미래를 알고 고른 저평가 종목"** 이 되어 백테스트가 통째로 무효다.
그리고 이건 결과를 좋게 만드는 방향으로 작동하므로, 좋은 결과가 나와도 신뢰할 수
없게 된다.

**요구사항**: 데이터 소스가 *공시 시점 기준 스냅샷(point-in-time)*을 제공하는지
확인하고, 제공하지 않는다면 **최소한 각 수치의 공시일(report date)을 함께 저장**해
판단 시점 이후 정보가 섞이지 않았음을 사후 검증할 수 있어야 한다. Phase 2에서
`headline_dates`를 남겨 2205건 전량을 대조했던 것과 같은 방식.

### 4-2. 유니버스 생존 편향 🔴 (Phase 1·2보다 심각)

`KRX_MARKET_CAP_TOP`은 **2026-08 시점의 대형주 스냅샷**이다. 이걸 과거 구간에
그대로 적용하면 그 사이 상장폐지·피인수·대형주에서 탈락한 종목이 전부 빠진다.

가격 신호에서도 문제였지만 **저평가 전략에서는 치명적**이다. "PER이 낮았는데
지금도 대형주로 살아남은 회사"만 남으면, 저평가 종목을 샀을 때의 실제 위험
(가치 함정, 파산)이 표본에서 제거된다. 저평가 전략의 성과가 구조적으로 부풀려진다.

**요구사항**: 시점별 유니버스(각 판단 시점의 시가총액 상위 N)를 재구성할 수
있어야 한다. 불가능하다면 **그 한계를 결과에 반드시 병기**하고, 게이트 통과
여부와 무관하게 "생존 편향 미보정" 상태임을 명시한다.

### 4-3. 결측·정의 불가 처리

적자 기업의 PER, 자본잠식 기업의 PBR은 정의되지 않는다. **결측을 어떻게 다루는지가
결과를 좌우한다** — 조용히 제외하면 "적자 기업을 피한" 효과가 신호의 성과로
둔갑한다.

**요구사항**: 결측은 제외가 아니라 **명시적 범주**로 다룬다(예: 백분위 계산에서
제외하되 그 건수를 기록하고, 결측 종목만 따로 성과를 집계). 사전 고정 대상.

### 4-4. 업종 편중

저평가 스크리닝은 특정 업종(금융·지주·경기민감)에 몰리는 경향이 있다. 성과가
신호 때문인지 그 업종의 그 시기 성과 때문인지 구분되지 않는다.

**요구사항**: Phase 1의 국면별 분해(`by_regime_strategy`)와 같은 방식으로
**업종별 분해**를 리포트에 포함한다.

## 5. Phase 1 프레임 재사용 — 새로 만들지 않는다

지시대로 검증 뼈대는 기존 것을 그대로 쓴다. 아래는 **재사용 대상과 교체 대상의
경계**다.

| 구성요소 | 위치 | Phase 3에서 |
|---|---|---|
| 성공 기준 | `backtest.py` `SUCCESS_CRITERIA` (min_trades 30, sharpe≥1.0, MDD 참고용) | **그대로 재사용** |
| 게이트 판정 | `evaluate_gate()` (훈련/검증 각 30건, 검증 샤프≥1.0, 훈련/검증 같은 부호, B&H 대비 우위) | **그대로 재사용** |
| 훈련/검증 분리 | 날짜 기준 70/30 (`split_index = int(len(candles) * 0.7)`) | **방식 재사용**, 분리 축만 캔들 인덱스 → 판단 시점(공시일)으로 |
| 성과 지표 | `compute_metrics()` (승률/평균수익/샤프 유사) | **그대로 재사용** |
| MDD | `compute_portfolio_mdd()` (날짜축 동시보유 시뮬레이션) | **그대로 재사용** — 분기 리밸런싱은 동시보유가 더 많아 오히려 이 구현이 필요 |
| 매매비용 | `analyze_lib.TRADING_COSTS["krx"]` (수수료 0.015%, 슬리피지 0.1%, 매도세 0.18%) | **그대로 재사용** |
| Buy&Hold 대조 | `buy_hold_trades()` / `strategy_vs_buy_hold` | **그대로 재사용** |
| baseline 대조 | Phase 2에서 쓴 "단순 다수클래스" 방식 | **개념 재사용**, 펀더멘털판 baseline은 §6에서 정의(예: 유니버스 동일가중 보유) |
| 진입 신호 | `entry_score`/`is_golden_cross`/`calc_adx` | **교체 대상** — 이 자리에 펀더멘털 스코어가 들어간다 |
| 청산 규칙 | 하드 손절 / RSI 과열 / 시간 스톱 | **재검토 대상** — 분기 보유가 기본이라 시간 스톱 정의가 달라진다 |

**새로 만들 것은 신호 계산부와 데이터 적재부뿐이다.** 판정·집계·비용·벤치마크는
손대지 않는다. 손대면 Phase 1 결과와 비교가 불가능해지고, 기준을 결과에 맞춰
바꿨다는 의심을 받을 수 있다.

## 6. 사전 고정 대상 목록 (착수 전 확정, 이후 변경 금지)

`Phase2_과거뉴스백테스트_설계.md` §2와 같은 원칙 — **데이터를 보기 전에** 아래를
전부 확정하고 코드 상수로 박은 뒤 수집을 시작한다.

1. 채택 지표와 결합 방식 (§2-1/§2-2 중 무엇을, AND/OR/가중합 중 어느 방식으로)
2. 스크리닝 컷 (백분위 몇 % 이하, 유니버스 내 상대 기준)
3. 판단 시점 오프셋 K (공시일 + K 거래일, 기본안 1)
4. 보유 기간 / 청산 규칙 (다음 분기 공시까지 + 하드 손절 적용 여부)
5. 대상 기간과 유니버스 (그리고 시점별 유니버스 재구성 가능 여부)
6. 결측 처리 규칙 (§4-3)
7. baseline 정의 (유니버스 동일가중 보유 등)
8. 훈련/검증 분리 날짜

**결과를 보고 위 항목을 바꾸지 않는다.** 바꿔야 한다면 별도로 논의하고 명시적으로
기록한다(`SUCCESS_CRITERIA` 주석과 같은 원칙).

## 7. 표본 수 타당성 — 게이트를 만족할 수 있는가

`SUCCESS_CRITERIA["min_trades"] = 30`을 **훈련/검증 각각** 충족해야 한다.

```
유니버스 38종목 × 연 4분기 = 152 판단이벤트/년
5년 구간(예: 2021~2025)      = 760 판단이벤트
스크리닝 하위 30분위 통과     ≈ 228 거래
70/30 분리                    ≈ 훈련 160 / 검증 68
```

**산술적으로는 여유가 있다.** 다만 컷을 더 좁히거나(하위 10분위 → ~76거래 →
훈련 53/검증 23으로 **검증 30건 미달**), 구간을 3년으로 줄이면 바로 빠듯해진다.
§6-2(스크리닝 컷)와 §6-5(기간)는 이 산술을 보고 정해야 하며, **거꾸로 결과가 잘
나오는 컷을 찾아 조정하는 건 금지**다.

## 8. 미해결 블로커 — 승인 회의에서 결정해야 할 것

### 8-1. 펀더멘털 데이터 소스가 없다 🔴

현재 저장소의 데이터 소스는 가격뿐이다(Upbit / Naver 일봉 / Yahoo·stooq).
**PER·PBR·EV/EBITDA·재무제표를 주는 연결은 하나도 없다** — 코드 검색 결과 관련
함수·상수·호출이 전무하다.

즉 Phase 3은 **새로운 외부 데이터 커넥터 추가를 전제**한다. 그런데 `CLAUDE.md`
작업 원칙에 이런 조항이 있다:

> 게이트(v3.1 §2.1) 미통과 상태에서는 실시간 외부 데이터 연결 함수를 세션 내
> 자체 판단으로 추가/재활성화하지 말 것 — 방향성 세션에서 먼저 승인

따라서 **데이터 소스 선정과 연결은 이 세션에서 진행하지 않는다.** 승인 회의에서
결정할 사항으로 남긴다. 검토 대상 후보(각각 확인 필요):

- **DART 공시 API**(금융감독원): 원본 공시 기반이라 공시일이 정확하고
  point-in-time에 가장 가깝다. 다만 재무제표 파싱 부담이 크다.
- **네이버 금융 재무 페이지**: 이미 일봉에 쓰고 있는 소스라 접근은 쉽지만,
  **현재 시점 값만 제공**할 가능성이 높아 §4-1을 해결하지 못할 수 있다.
- **토스증권 Open API**: 현재 연동은 계좌 조회 전용이며, 재무 데이터 제공 여부
  미확인. 제공하더라도 실계좌 관련 파일을 건드리게 되므로 별도 승인 대상.

**어느 것을 쓰든 §4-1(공시일 저장)을 만족하지 못하면 이 트랙은 성립하지 않는다.**
데이터 소스 결정 = Phase 3의 성패를 가르는 결정이라고 보는 게 맞다.

### 8-2. 시점별 유니버스 재구성 가능 여부

§4-2를 해결하려면 과거 각 시점의 시총 상위 종목을 알아야 한다. 이것도 데이터
소스에 달려 있다. 불가능하면 "생존 편향 미보정"을 명시하고 진행할지, 아니면
트랙 자체를 보류할지가 승인 회의 판단 사항이다.

### 8-3. 애니플러스 분석 문서 (§2-3)

지표 목록 대조에 필요하다.

## 9. 착수 시 산출물 (승인 후)

승인되면 아래를 만든다. 지금은 만들지 않는다.

| 파일 | 역할 |
|---|---|
| `Phase3_펀더멘털신호_사전고정.md` | §6 항목을 확정해 못박는 문서 (수집 전 작성) |
| `fundamental_data.py` | 데이터 적재 + 공시일 저장 (§4-1 검증 포함) |
| `fundamental_backtest_log.json` | 수집 데이터셋 (기존 데이터셋과 분리) |
| `backtest_fundamental.py` | 신호 계산부. 판정·집계는 `backtest.py`에서 import |
| `.github/workflows/` 신규 1건 | `workflow_dispatch` 전용 |

**기존 파일 중 수정 대상은 없다.** `backtest.py`의 판정부는 import해서 쓰고
고치지 않는다(§5).

---

## 요약 — 승인 회의에서 답해야 할 질문

1. 펀더멘털 데이터 소스를 무엇으로 할 것인가, 그리고 그 소스가 **공시일을 함께
   제공**하는가(§8-1). → 이게 안 되면 나머지는 의미 없다.
2. 시점별 유니버스 재구성이 가능한가, 안 되면 생존 편향을 안고 갈 것인가(§8-2).
3. §6의 8개 항목을 확정할 준비가 됐는가(애니플러스 지표 목록 포함).
4. 이 트랙에 쓸 API 예산/시간을 어느 정도로 잡을 것인가.

**이 문서는 어떤 결론도 내리지 않는다.** 게이트 통과 가능성에 대한 전망도 적지
않았다 — 그건 백테스트가 답할 문제이고, 미리 적어두면 나중에 결과를 그 전망에
맞춰 해석하게 된다.
