# 킬스위치 타이밍 이상 조사 — 2026-08-08

> 프록시 세션 보고용 문서. 조사 결과만 기록한다 — 판정이나 조치는 방향성 세션 몫이다
> (CLAUDE.md "State facts, don't render verdicts" 원칙). 근거는 전부 GitHub Actions
> 실행 로그, 잡 로그, git 커밋 타임스탬프에서 직접 확인한 값이며, 추론이 필요한 부분은
> 명시적으로 "추정"이라고 표시했다.

---

## 1. 한눈에

| 항목 | 결과 |
|---|---|
| 11:35~12:44 구간에 `poll.yml`이 돌았는가 | **아니오 — 72분간 실행 이력 자체가 0건** |
| 원인이 코드/로직 문제인가 | **아니오 — 실제로 돌았을 때는 8초 만에 정상 처리** |
| 이전에 문서화된 "플랫폼 지연"과 같은 원인인가 | **예 — 같은 현상의 재확인** |
| 이번 사건이 수정본(PR#15) 적용 이후인가 | **아니오 — PR#15 병합(14:42:59 KST) 전 사건** |
| CLAUDE.md에 "킬스위치 검증 완료" 기록했는가 | **아니오 — 계속 보류 중** |

---

## 2. 조사 방법

- `mcp__github__actions_list`/`get_job_logs`로 `poll.yml`의 실제 실행 이력(예약 실행 +
  수동 실행 전부)과 각 실행의 잡 로그 원문을 확인.
- `git log --format="%ad" --date=iso-strict`로 `telegram_offset.json`/`CLAUDE.md`/PR
  머지 커밋의 정확한 UTC 타임스탬프 확인 후 KST(UTC+9)로 환산.
- 시각은 전부 사용자가 보고한 KST 기준으로 통일해 표기(괄호 안 UTC 병기).

---

## 3. 항목별 조사 결과

### 3-1. `poll.yml` 실행 이력 (11:35~12:44 KST 구간)

GitHub Actions 실행 목록을 시간순으로 나열하면:

| 시각 (KST) | 트리거 | 결과 |
|---|---|---|
| 11:30:17 | `workflow_dispatch` (조사 세션의 수동 트리거) | `/autoexec_stop` 2건 처리 |
| 11:31:43 | `workflow_dispatch` (조사 세션의 수동 트리거) | 업데이트 1건 처리, **응답 전송 없음** |
| *(공백 — 이 구간에 실행 이력이 전혀 없음)* | | |
| **12:44:03** | `schedule` (최초 자연 실행) | `/autoexec_start` + `/autoexec_status` 처리·응답 |

**11:31:43 이후 12:44:03까지 72분간 `poll.yml`은 예약 실행이든 수동 실행이든 단 한 건도
없었다.** "돌았는데 응답을 못 보냈다"가 아니라 "잡 자체가 시작되지 않았다"— 실행 이력
목록 자체에 해당 구간 항목이 없다.

### 3-2. 원인 분리 — 플랫폼 지연 vs. 코드/로직

**플랫폼 지연.** 12:44:03에 잡이 실제로 시작되자, `check_updates.py` 실행부터 텔레그램
응답까지 약 8초(03:44:08 → 03:44:16 UTC) 만에 두 건(`/autoexec_start`, `/autoexec_status`)
모두 정상 처리·응답됐다:

```
ℹ️ 킬스위치는 이미 해제 상태입니다.
🤖 자동실행 상태
· 킬스위치: 해제됨
· 활성 플래그: false (판정만)
· 유예기간: 적용 중 (규칙별 1일 1회)
· 최초 활성화: 없음
· 오늘 판정 로그: 0건 (누적 24건)
```

코드가 실행 기회를 얻자마자 즉시·정확히 동작했다는 뜻이다. 로직 결함의 증거는 없다.

### 3-3. `autoexec_state.json` 커밋 히스토리

**이 시간대에는 확인할 히스토리 자체가 없다.** 상태 영속화 버그 수정(PR#15)이 병합된
시각은 **2026-08-08 14:42:59 KST**(05:42:59 UTC) — 이번에 문제된 세 시각(11:30, 11:35,
12:44) 전부 그 이전이다. 즉 이 사건은 `autoexec_state.json`이 애초에 커밋되지 않던
**수정 전** `poll.yml`에서 일어났다. 직접 겪은 사용자 사례로 기존 진단(상태 미영속화 +
스케줄 지연 이중 문제)이 재확인된 셈이지, 수정본이 실패한 사례가 아니다.

### 3-4. 기존 "플랫폼 지연" 문서화 내용과 동일 원인인가

**동일하다.** 72분 완전 공백은 CLAUDE.md에 이미 기록된 GitHub Actions `schedule` 트리거의
공식 문서화된 한계(플랫폼 부하 시 지연, 선언된 간격 보장 안 함)와 정확히 같은 패턴이다.
새로운 실패 유형이 아니라 기존 진단의 재현이다.

---

## 4. 추가로 발견된 사항 (요청 범위 밖이지만 보고)

### 4-1. "11:30 → 11:31, 약 1분, 정상" 페어링과 서버 로그 불일치

서버 로그 기준으로 11:30:17에 보낸 응답 2건은 모두 **`/autoexec_stop`(킬스위치 정지)**
관련이었고, 11:31:43에 처리된 업데이트 1건은 **응답이 전혀 전송되지 않았다.** 사용자가
그 시각에 정말 `/autoexec_status`를 보냈다면, 서버 기록상 그 요청은 무응답이었다 — "1분
만에 정상 응답"이라는 인식과 배치된다. 같은 시간대에 도착한 킬스위치 정지 확인 메시지를
상태 조회 응답으로 착각했을 가능성이 있다. 사용자 측 텔레그램 로그(실제 발신 시각·문구)
재확인 필요.

### 4-2. 세 번째 메시지("2:55") 이후 — 현재 시각 기준 라이브 상태

PR#15 병합(14:42:59 KST) 이후 확인 시점(15:04:52 KST)까지 22분간, `poll.yml`과 신설된
`autoexec_stop_fast.yml` **둘 다 단 한 번도 실행되지 않았다**(`autoexec_stop_fast.yml`은
누적 실행 0건). "2:55" 메시지가 아직 응답을 못 받았다면 이 공백이 원인일 가능성이 높다.
계속 모니터링 필요.

---

## 5. 미해결 항목

- §4-1의 페어링 불일치 — 사용자 측 원본 로그 대조 필요.
- §4-2 — PR#15 적용 이후 첫 정상 응답 사례가 아직 관측되지 않음. 관측되는 대로 별도 보고.
- CLAUDE.md "킬스위치 검증 완료" 기록은 §4-2가 확인될 때까지 계속 보류.
