# Phase 2: 뉴스 이벤트 추출 — 설계 메모 / 인수인계

2026-08-01 작성. Phase 1 백테스트 §2.1 게이트 미통과 확정, main merge 완료(`eafb570`) 이후
시작하는 별도 트랙. **이 문서 시점 기준으로 코드 구현은 없음 — 텍스트 설계안만.**

## 1. 현재 상태: get_news_headlines / _format_news

- 위치: `analyze_lib.py:391-446`
- 상태: Phase 1에서 "실험 단계 전용" 문구로 보존된 그대로, 변경 없음.
  `ask_claude_decision`/`analyze.py` 어디에도 호출부 없음 — `grep -rn "get_news_headlines\|_format_news"`로
  재확인, `analyze_lib.py`(정의부)와 `CLAUDE.md`(문서 언급) 2곳에만 존재.
- 라이브 뉴스 판단은 여전히 `get_news_sentiment`(감성 단어 카운트, `analyze_lib.py:372`)가 전담.
- **구현 세부 하나가 Phase 2 방향과 안 맞음**: `get_news_headlines`가 쓰는 Google News RSS 쿼리가
  `hl=en-US&gl=US&ceid=US:en` — 즉 영어 뉴스만 가져온다. `get_news_sentiment`도 동일 URL 패턴이라
  같은 문제. KRX 종목(삼성전자, 005930 등)의 실적발표/공시/M&A/규제 뉴스는 대부분 한국어로 나오므로,
  영어 쿼리로는 관련 기사를 거의 못 잡을 가능성이 높다 — §5-1 참고.

## 2. 이벤트 추출 설계안 (텍스트만)

### 2-1. 사건 유형 분류: 감성 점수 대신 유형 라벨

후보 유형: `실적발표` / `공시` / `M&A` / `규제` / `기타`(그 외 전부를 담는 캐치올, 분류 실패를
조용히 숨기지 않기 위해 명시적으로 남겨둠).

두 가지 분류 방식을 검토:

| | A) 키워드 규칙 | B) Claude 자체 분류 |
|---|---|---|
| 방식 | 정규식/키워드 매칭(예: "실적"/"매출"/"영업이익"→실적발표, "인수"/"합병"→M&A, "제재"/"과징금"/"금감원"→규제) | 헤드라인을 Claude에게 주고 판단(사건유형+투자판단+confidence)을 한 호출에서 같이 받음 |
| 비용 | 없음(추가 API 호출 없음) | 추가 API 호출 없음 — 어차피 판단 호출에 얹으면 됨(단, headline별이 아니라 market별 배치라면 종목당 1회) |
| 정확도 | 한국어 금융 용어 변주가 많아 브리틀함(예: "잠정실적", "컨센서스 하회" 같은 표현은 키워드 목록을 계속 늘려야 함) | 문맥 기반이라 변주에 강함, 다만 분류 근거를 같은 모델이 판단과 함께 내므로 순환 의존(같은 판단력을 같은 모델로 검증하는 셈) |
| 재현성 | 결정적(같은 입력 → 같은 출력) | 비결정적일 수 있음(temperature/모델 버전 변화) |

**권장**: B(Claude 자체 분류)를 1차로 채택. 근거: 이 프로젝트는 이미 매매 판단 자체를
Claude에게 맡기고 있어(`ask_claude_decision`), 별도 키워드 분류기를 추가로 유지보수하는 비용이
안 맞는다. 대신 캘리브레이션 결과를 볼 때 "분류 자체가 틀렸을 가능성"도 함께 열어둬야 한다 —
즉 사건유형별 지표뿐 아니라, 나중에 사람이 로그를 샘플링해서 분류가 말이 되는지 스팟체크하는
과정을 캘리브레이션 검토에 포함시키는 게 안전하다.

### 2-2. 헤드라인 원문을 프롬프트에 전달하는 방식

Phase 1에서 되돌린 시도(같은 날 `get_news_sentiment`를 `get_news_headlines`로 실제 교체)의
프롬프트 아이디어 자체는 폐기하지 않는다 — 다만 **이번엔 별도 실험 스크립트 안에서만** 쓴다.

제안하는 실험 프롬프트 구조 (기존 `ask_claude_decision`과는 별개 함수/별개 프롬프트):

```
[종목] {market} ({종목명})
[최근 헤드라인 원문]
- {headline 1}
- {headline 2}
...
(최대 5개, _format_news 포맷 재사용 가능)

아래를 JSON으로 답해줘:
{
  "event_type": "실적발표" | "공시" | "M&A" | "규제" | "기타",
  "direction": "호재" | "악재" | "중립",
  "confidence": 0~100 정수,  // 이 판단에 얼마나 확신하는지
  "reasoning": "판단 근거 한 줄"
}
```

가격 지표(RSI/이동평균 등)는 이 실험 프롬프트에 넣지 않는다 — Phase 1에서 되돌린 지시문의
취지("가격 지표가 아니라 뉴스 사건이 근거") 그대로, 이 실험은 "뉴스 단독으로 얼마나 예측력이
있는가"를 순수하게 보려는 것. 가격 지표와 결합했을 때 성능이 어떻게 바뀌는지는 이 실험이
어느 정도 성숙한 뒤의 다음 질문으로 남겨둔다.

### 2-3. Confidence 캘리브레이션 기록 방식

핵심 문제: backtest.py 사도 이미 겪은 것과 같은 제약 — **과거 시점의 AI 판단은 재현할 수 없다**
(비용·비결정성). 그래서 이 실험은 backtest처럼 과거를 되돌려 계산하는 게 아니라, **지금부터
전향적으로(prospectively) 판단을 기록하고, 시간이 지난 뒤 실제 결과와 짝짓는 방식**이어야 한다.

제안하는 2단계 로그:

1. **판단 시점 로그** (실험 스크립트를 돌릴 때마다 append):
   ```json
   {
     "id": "005930_2026-08-01_1",
     "market": "005930", "date": "2026-08-01",
     "headlines": ["...", "..."],
     "event_type": "실적발표", "direction": "호재", "confidence": 72,
     "reasoning": "...",
     "price_at_judgment": 71200,
     "outcome_window_days": null,      // 2단계에서 채움
     "outcome_return_pct": null,       // 2단계에서 채움
     "outcome_recorded_at": null
   }
   ```
2. **결과 기록** (같은 실험 스크립트를 나중에 다시 돌리거나, 별도 "결과 채우기" 모드로 실행):
   `outcome_window_days`가 지난 판단 건을 찾아 그 시점 가격을 다시 조회, `outcome_return_pct`를
   계산해 같은 레코드에 채워 넣는다(같은 `id`로 매칭 — 새 레코드를 만들지 않고 기존 레코드를
   갱신). 이렇게 해야 "판단 시점 confidence"와 "실제 결과"가 레코드 하나에 짝지어져서 나중에
   바로 신뢰도 곡선(reliability diagram: confidence 구간별 실제 적중률)을 그릴 수 있다.

캘리브레이션 지표 (충분한 표본이 쌓인 뒤):
- confidence 구간별(예: 0-40/41-60/61-80/81-100) 실제 방향 적중률 — 이상적으로는 confidence
  구간이 높을수록 적중률도 높아야 함(과신/과소신 여부 판단)
- event_type별 평균 적중률/평균 수익률 — 어떤 사건 유형이 실제로 예측력이 있는지

## 3. 실행 범위 — 분리 유지

- 별도 실험 스크립트(가칭 `news_event_experiment.py`)로만 실행. `analyze.py`/`ask_claude_decision`
  변경 없음, 이번 트랙 전체에서 마찬가지.
- 로그 파일도 별도 저장(가칭 `news_event_calibration_log.json`) — 기존 5개 상태 파일
  (`portfolio.json`/`trade_history.json`/`pending_actions.json`/`last_report.json`/
  `telegram_offset.json`)과 분리해서 `daily.yml`/`poll.yml`의 커밋 레이스에 안 걸리게 한다.
- 유니버스는 KRX 중심 유지(Phase 1에서 이미 정리된 원칙, `backtest.py`의
  `KRX_MARKET_CAP_TOP`/"[Phase 2 승인 유니버스]" 주석 그대로 재사용 가능).
- 안전장치(`HARD_STOP_LOSS`, `needs_approval()`의 승인 기준, `AUTO_TIER_WEIGHT`/
  `POSITION_WEIGHT_HARD_CAP`) — 이 실험은 매매를 전혀 실행하지 않으므로 애초에 안전장치 경로를
  타지 않음. 약화 대상 자체가 아님.
- 거래비용/슬리피지: 이 실험은 매매를 실행하지 않으니 직접 해당 없음. 다만 캘리브레이션이
  끝나고 실제 신호로 승격하는 단계가 오면(별도 논의) 그때는 `TRADING_COSTS`를 반드시 반영해야
  함 — "뉴스 반응만으로 수익률이 나오는 것 같아도 비용 반영하면 사라질 수 있다"는 점을 다음
  단계로 넘길 때 잊지 않도록 여기 남겨둠.

## 4. 이번 세션 구현 여부

**미결정 — 사용자 판단 대기.** 이 문서까지가 설계안 제시 단계이고, 코드 구현(실험 스크립트
작성)으로 이어갈지 다음 세션으로 넘길지는 사용자가 이 설계안을 검토한 뒤 결정한다. 임의로
구현을 시작하지 않았음.

## 5. 진행 중 방향성 판단이 필요한 지점 (기록만, 세션은 계속 진행)

1. **뉴스 소스 언어**: `get_news_headlines`/`get_news_sentiment` 둘 다 영어 Google News RSS
   (`hl=en-US&gl=US&ceid=US:en`)를 쓴다. KRX 종목의 실적발표/공시/M&A/규제 뉴스 대부분은
   한국어로 나오므로, 실험 스크립트는 별도로 한국어 쿼리(`hl=ko&gl=KR&ceid=KR:ko` 등)를 쓰는
   새 함수가 필요할 가능성이 높다. `get_news_sentiment`(라이브에서 여전히 쓰는 중)를 건드리는
   건 이번 범위 밖이지만, 실험용 새 함수를 만들 때 같이 정할 문제.
2. **분류 방식**: 위 2-1에서 B(Claude 자체 분류)를 권장했으나, 최종 채택은 사용자 확인 필요.
3. **Confidence 스케일**: 0-100 연속값 제안. 1-5 이산 등급 등 다른 스케일을 원하면 캘리브레이션
   버킷 설계도 같이 바뀜.
4. **결과 판정 시점(outcome window)**: 전략유형별 예상보유기간(단타≤6일/스윙≤20일/장기>20일)과
   맞출지, 사건 유형별로 다르게(예: 실적발표는 발표 익일 갭 반응이 핵심이라 짧게, M&A는 영향이
   장기라 길게) 가져갈지 미정.
5. **실행 주기**: 매일 자동(스케줄/workflow_dispatch) vs 수동 실행. 캘리브레이션에 필요한
   최소 표본(예: 사건 유형별 30건, `backtest.py`의 `SUCCESS_CRITERIA.min_trades`와 같은 기준을
   재사용할지)을 언제까지 모을지와 직결.
6. **표본 확보 기간 추정**: KRX 상위 30여 종목 기준으로 하루 몇 건의 관련 헤드라인이 나오는지
   실측이 안 된 상태 — 실험을 실제로 돌려보기 전까지는 "몇 주면 30건씩 모이는지" 추정이 어렵다.
