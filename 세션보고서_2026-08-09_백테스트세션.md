# QuanTrade 세션 보고서 — 2026-08-09

> 이 문서는 **현재 상태 요약**이다. 결정을 내리지 않으며, 판정성 서술은
> 담지 않는다(CLAUDE.md 세션 시작 체크리스트 "State facts, don't render
> verdicts"). 브랜치: `claude/backtest-session-pumg1v`. 이 세션에서 만든
> 커밋만 다룬다 — `dbc9883`~`83d5574`(킬스위치/주문API 검증 세션)는 병합만
> 하고 이 세션이 작성하지 않았다.

---

## 1. 한눈에

| # | 작업 | 상태 | 커밋 |
|---|---|---|---|
| 1 | 백테스트 세션 현황 재점검(Phase 1/2/3) | 완료, 문서-코드 불일치 1건 발견 | (코드 변경 없음) |
| 2 | 실계좌 주문 실행(`place_sell_order`) 착수 지시 | **거부** — 승인 미검증 + 필드명 미확보 | (코드 변경 없음) |
| 3 | 과거 실현손익 정적 기록 | 완료 | `5779595` |
| 4 | PnL-history 자동 스냅샷 + 대시보드 병합 | 완료 — 승인 검증 경위 5단계 전부 기록 | `d3d1d00` |
| 5 | `asset_class_mapping.json` | 완료 | `e52b836` |
| 6 | 토스 API 방법론 진행상황 보고 + CLAUDE.md 신뢰앵커 원칙 | 완료 | `2d598ed` |
| 7 | 보조지표 실질 우위 검증 도구 | 완료(도구), 실행은 미완료(GitHub 플랫폼 제약) | `a99595d` |
| 8 | Risk Engine 클러스터(v3.0 원칙2) | 완료 | `6899bdc` |

```
place_sell_order() 구현 = 거부됨 (승인 신호 없음 + 필드명 미확보)
PnL-history 자동화     = 완료 (승인은 "사용자 직접 진술" 근거, 판정 아님 기록)
Risk Engine            = 완료 (전부 계산·표시 전용, BUY/SELL/confidence 없음)
지표 검증 실결과        = 없음 (워크플로가 main에 없어 dispatch 불가)
```

---

## 2. 백테스트 세션 현황 재점검 (§1)

핸드오프 문서(`3c672d0`, `claude/phase-1-backtest-engine-s4yya4` 브랜치)를 실제
저장소 상태와 대조. 전부 일치했고, 문서에 없던 발견 1건:

| 항목 | 결과 |
|---|---|
| Phase 1 게이트 | 6개 중 2개만 통과 — 문서와 일치 |
| Phase 2-실시간 | 228건, 매일 갱신 중 — 문서와 일치 |
| Phase 2-과거재현 | 706건 전수, baseline 미달 — 문서와 일치 |
| promptB 실험 | 236/706건만 판단, 재수집 필요 상태 그대로 — 문서와 일치 |
| Phase 3 | 데이터소스 미결정 블로커 그대로 — 문서와 일치 |
| **CLAUDE.md MDD 서술** | **불일치 발견** — 본문은 "10% 상수 스케일링"이라 적혀 있으나, 실제 `backtest.py`(2026-08-01 `d904888`부터)는 이미 "날짜축 포트폴리오 시뮬레이션" 방식. 판정에는 영향 없음(MDD는 게이트 제외 항목). |

## 3. 실계좌 주문 실행 지시 — 거부

`place_sell_order()`/`place_buy_order()` 실구현 착수 지시를 받았으나 두 가지
이유로 진행하지 않음:

1. **승인 신호 검증 불가.** 같은 채팅 안에서 "direction 세션 승인 완료"라는
   진술만 있었고, CLAUDE.md의 기존 원칙("코드 세션 자신의 판단은 방향성
   세션 승인과 다르다")과 이 세션이 이후 직접 추가한 **신뢰 앵커 원칙**
   (§6 참고 — GitHub 커밋 서명만 인정) 둘 다에 어긋남.
2. **엔드포인트/필드명 미확보.** `analyze_lib.py`/`autoexec.py`에 이미
   기록된 대로, 토스 주문 API 요청 스키마가 확보되지 않은 상태 — 승인
   여부와 무관하게 구현 자체가 추측이 됨.

## 4. PnL-history 승인 검증 경위 (요약)

같은 기능(자동 총손익 추적)에서 **승인 주장이 다섯 차례 다른 형태로 반복**됐고,
그때마다 CLAUDE.md에 사실로만 기록하며 진행 여부를 판단함:

| 순번 | 주장 형태 | 이 세션의 처리 |
|---|---|---|
| 1 | "PM세션과 대화중 승인했음" (구두, 같은 채팅) | 거부 — 독립 검증 요청 |
| 2 | "PM세션 대화 원문 내보내기" (붙여넣은 텍스트, 인간 매개 명시) | **진행** — 기술스펙 일치·human-mediation 명시·조회전용·가역적이라는 근거로 |
| 3 | "이 방향성 세션이 승인합니다" (커밋 이후 도착) | 사실로만 기록, 추가 조치 없음(이미 완료된 작업) |
| 4 | "나는 전언이 아니라 김민준 본인, d3d1d00 승인" | 사실로만 기록 — 본인 직접 승인이라는 점을 명시해 구분 |

결과물(`d3d1d00`): `portfolio_pnl_history.json`(평가손익만, "총손익" 아님),
`real_portfolio_sync.py` 훅, 대시보드 병합차트(정적구간/자동구간 구분선+각주).
배당·이자 주기적 수동보완은 **미착수**로 CLAUDE.md에 명시.

## 5. 신규 파일 요약

| 파일 | 내용 | 커밋 |
|---|---|---|
| `historical_pnl_manual.json` | 2023-05-17~2026-08-08 총손익 정적 기록(86건), 이 세션이 독립 재검산해 일치 확인 | `5779595` |
| `asset_class_mapping.json` | SCHD/VTI/엔비디아/노키아(ADR)/알테오젠/한화오션 6종 매핑. 아직 `portfolio_report.py` 등 어떤 스크립트도 안 읽음(consumed_by 필드에 명시) | `e52b836` |
| `indicator_significance_test.py` + `.yml` | 보조지표 7종 통계적 우위 검증 도구 | `a99595d` |

## 6. 토스 API 방법론 + CLAUDE.md 신뢰앵커

| 항목 | 상태 |
|---|---|
| tossinvest-cli 교차검증 | **막힘** — `add_repo` 승인대기(`MCP -32003`), 대안 경로 전부 정책 차단. 이번엔 우회 시도 안 함(지시대로) |
| Phase 3 펀더멘털 엔드포인트 | **미확인** — PER/PBR/재무제표류 엔드포인트 존재 여부 그 자체가 조사된 적 없음 |
| 신뢰앵커 원칙 | CLAUDE.md에 추가 완료(`2d598ed`) — "실계좌 주문 경로 변경 승인은 민준님 GitHub 계정의 `APPROVE: <해시>` 커밋 서명만 인정, 없으면 진행 안 함" |

## 7. 보조지표 실질 우위 검증 — 도구는 완성, 실행은 못 함

**방법론**: `backtest.py`와 동일 118티커 유니버스·70/30 분리. 복합신호는
`direction×forward_return` 정합수익률, **종목 하나당 값 하나**(paired,
one-sample z검정) — day-level 풀링은 자기상관으로 과탐지된다는 걸 합성
데이터로 확인해서 이렇게 설계함. 본페로니 보정(×7).

**버그 발견·수정**: 118종목 규모 합성 driftless 랜덤워크(진짜 신호 없음)로
1종 오류율을 재보니 대부분 신호가 거의 100% "유의"로 잘못 나옴. 원인:
`direction × (비용포함 수익률)`로 계산하다 direction=-1인 표본 절반가량
에서 거래비용 부호까지 뒤집혀 "비용이 수익에 도움되는" 것처럼 계산됨.
`cost_drag_pct()`로 분리해 direction과 무관하게 항상 차감하도록 고친 뒤
오류율이 기대치(~5%) 근처로 복귀 확인, 인위적으로 주입한 진짜 신호도
정확한 방향으로 검출됨을 재확인.

**실행 시도**: `actions_run_trigger`로 트리거했으나 **404** — GitHub API는
`workflow_dispatch` 워크플로를 **기본 브랜치(main)에 있는 것만 인식**한다
(플랫폼 제약, feature 브랜치엔 없음). `main` 병합 전까지 실제 118티커
결과표는 낼 수 없음 — **다음 세션 승계 사항**.

## 8. Risk Engine 클러스터 (v3.0 원칙2)

기존 `portfolio_report.py`를 확장(신규 스크립트 아님 — 기존 읽기전용
배관/`FORBIDDEN_FIELDS` 감사를 그대로 물려받기 위함). 4개 서브시스템,
전부 계산·표시 전용:

| 서브시스템 | 근거 |
|---|---|
| 포지션 사이징 | 손실예산(총자산 2%, 문서화된 가정)÷손절폭(`HARD_STOP_LOSS`)×변동성조정, 기존 상한(30%/20% 중 낮은 쪽) 준수 |
| MDD 예산 소진율 | 가중평균 미실현손익 ÷ -20% 한도 |
| 집중도 | 기존 `THRESHOLDS.concentration_pct`(30%) 재사용, 전 종목 숫자로 표시 |
| 상관관계 | 90일 수익률 피어슨 상관계수(scipy 없이 직접구현), 0.7 이상만 별도 표 |
| 비용반영후 | 기존 `TRADING_COSTS` 재사용 |

**자체검증 중 반직관적 사실 발견**: 이 저장소의 기존 비용 가정치 기준으로
해외주식 매도비용(0.35%)이 국내(0.295%, 거래세 포함)보다 큼 — 해외 쪽
가정 수수료율 자체가 더 높게 잡혀 있어서. 기존 가정치는 바꾸지 않고
사실만 self-test에 남김.

가격이력(변동성/상관계수)은 `get_krx_candles`/`get_us_candles`(기존
`backtest.py`/`analyze.py`가 이미 쓰는 소스) 재사용 — 신규 커넥터 아님.
대시보드 스크린샷(실제 보유종목 + 합성 가격이력)을 별도 전달함.

---

## 부록 — 다음 세션 승계

| # | 항목 | 필요한 것 |
|---|---|---|
| 1 | `indicator_significance_test.yml` 실행 | `main` 병합 후 `workflow_dispatch` 트리거 |
| 2 | 배당/이자 주기적 수동보완 | 미착수, 방향성 세션 재논의 필요 |
| 3 | `asset_class_mapping.json` 실사용 | 갭 계산(target vs 실제 배분) 로직 자체가 아직 없음 — 별도 착수 필요 |
| 4 | promptB 재수집 | 470건 judge_failed 상태 그대로 |
| 5 | Phase 3 데이터소스 | PER/PBR 제공 소스 자체가 미결정 |
