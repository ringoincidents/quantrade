# Phase 2 포지션 사이징 모델 후보군 — 수학적 설계서

> **범위**: 텍스트 설계/조사 문서. 코드 구현 없음. 현재 시뮬레이션의 `AUTO_TIER_WEIGHT`(10%)/
> `POSITION_WEIGHT_HARD_CAP`(20%) 고정값 체계를 대체할 후보를 조사한다 — 어떤 모델을
> 채택할지, 언제 전환할지는 이 문서의 범위 밖이며 별도 방향성 세션에서 결정한다.
>
> **전제 조건 재확인**: Phase 1 §2.1 게이트는 미통과 상태다(가격 신호에 통계적으로 유의미한
> 우위가 없다는 뜻). 아래 모델 중 승률/손익비를 입력으로 요구하는 것(켈리)은 **유의미한 edge가
> 검증되기 전까지는 추정치가 노이즈에 불과하다** — 이 문서는 "지금 당장 켈리를 쓰자"는 제안이
> 아니라, 게이트를 통과하는 신호가 나왔을 때 바로 검토할 수 있도록 미리 정리해두는 조사다.

---

## 1. 변형 켈리 공식 (Fractional Kelly Criterion)

### 수식 정의

이산 승/패 베팅 형태(원 켈리 공식):

```
f* = p - q/b = (bp - q) / b
```

- `p`: 승률, `q = 1-p`: 패율
- `b`: 손익비 (평균 이익 / 평균 손실, 즉 `avg_win / avg_loss`)
- `f*`: 자산 대비 베팅 비중

연속 수익률 형태(트레이딩에서 더 흔히 쓰는 근사식, 수익률이 정규분포에 가깝다고 가정):

```
f* = μ / σ²
```

- `μ`: 기대 초과수익률(평균 수익률), `σ²`: 수익률 분산

**Half-Kelly / Quarter-Kelly**: `f*`를 그대로 쓰지 않고 `f*/2`, `f*/4`로 축소. 원 켈리는
장기 기하평균 성장률을 이론적으로 최대화하지만 변동성이 극심하고, `p`/`b`(또는 `μ`/`σ²`)
추정 오차에 결과가 매우 민감하게 반응한다(추정치가 조금만 낙관적이어도 실제로는 파산 위험이
커짐) — 그래서 실무에서는 절반/사분의 일로 깎아 쓰는 게 표준 관행이다.

### 장단점

| | 내용 |
|---|---|
| 장점 | 이론적으로 장기 기하평균 성장률을 최대화(원 켈리 기준). 승률/손익비가 종목·전략유형별로 다를 때 자연스럽게 차등 비중을 준다. |
| 단점 | `p`/`b`(또는 `μ`/`σ²`) 추정이 부정확하면(표본 부족, 국면 변화 등) 결과가 크게 어긋남 — 과대추정 시 큰 손실 위험. 우리 백테스트처럼 **edge 자체가 통계적으로 불확실한 상태에서는 켈리 공식이 요구하는 입력값 자체가 신뢰할 수 없다.** 음수 `f*`(edge 없음/역逆)가 나오면 공식이 성립하지 않음 — 별도 처리 필요. |

### 우리 시스템과의 연동 방식(안)

- 현재 `AUTO_TIER_WEIGHT`(10%)/`POSITION_WEIGHT_HARD_CAP`(20%)는 전략유형/자산군 무관 고정값 —
  켈리를 도입한다면 `backtest.py`의 `overall`/`by_strategy_type` 등에서 나온 실측 승률·손익비를
  `p`/`b`로 넣어 전략유형별(단타/스윙/장기) 또는 사건유형별(Phase 2 `event_type`)로 차등화된
  `f*`를 산출하는 방식이 자연스럽다.
- **안전장치와의 관계**: 켈리가 산출한 `f*`(Half/Quarter 적용 후)도 여전히
  `POSITION_WEIGHT_HARD_CAP`을 절대 상한으로 clamp한다 — 켈리 계산이 잘못돼도(추정 오차,
  버그) 하드캡 아래로는 절대 안 내려가는 이중 안전장치 구조를 유지.
- **전제**: `p`/`b` 추정치의 출처가 지금 시점엔 없다 — Phase 1 백테스트는 게이트 미통과라
  승률/손익비를 "검증된 edge"로 쓸 근거가 없고, Phase 2 캘리브레이션(ECE/유의성 검정)이
  통과해야 비로소 신뢰할 만한 입력값 후보가 생긴다.

---

## 2. 변동성 타겟팅 (Volatility Targeting / ATR-based Sizing)

### 수식 정의

**변동성 타겟팅** — 목표 변동성 대비 종목 자체 변동성으로 비중을 역산:

```
weight = target_volatility / asset_volatility
```

- `target_volatility`: 포트폴리오가 감내하려는 연환산(또는 기간환산) 변동성 목표치
- `asset_volatility`: 최근 N일 수익률 표준편차(또는 ATR 기반 근사치)를 같은 기간 단위로 환산한 값

**ATR 기반 사이징** — 진입가 대비 손절 거리(ATR 배수)와 계좌 단위 리스크로 역산:

```
position_size = (account_risk_per_trade) / (ATR_multiple × ATR)
```

- `ATR`: Average True Range(통상 14일), `ATR_multiple`: 손절을 ATR의 몇 배로 잡을지(예: 2)
- `account_risk_per_trade`: 이 거래 하나에 걸 수 있는 최대 손실액(계좌 자산의 일정 %)

### 장단점

| | 내용 |
|---|---|
| 장점 | 승률/손익비 추정이 필요 없다 — 켈리와 달리 "edge가 있다"는 가정 없이도 적용 가능, 지금처럼 아직 검증된 edge가 없는 상태에 상대적으로 더 적합. 고변동성 구간엔 자동으로 비중을 줄이고 저변동성 구간엔 늘려서, 종목별/시기별 리스크를 균등화하는 효과. `backtest.py`가 이미 ADX 계산을 위해 highs/lows/closes를 들고 있어 ATR 계산에 필요한 데이터가 이미 확보돼 있음(추가 데이터 수집 불필요). |
| 단점 | 변동성이 낮다고 방향성까지 맞다는 보장은 없음 — "안전하게 크게 베팅"이 "맞는 베팅"은 아니다. 변동성 추정 윈도우(N일)를 얼마로 잡느냐에 따라 결과가 민감하게 갈릴 수 있고, 변동성이 급변하는 구간(예: 실적발표 직후)엔 후행적으로 반응할 위험. 장기 성장률을 직접 최적화하지 않으므로 켈리보다 "이론적 최적"이라는 근거는 약함. |

### 우리 시스템과의 연동 방식(안)

- `calc_adx()`가 이미 `highs`/`lows`/`closes`를 입력받으므로, 같은 데이터로 ATR을 계산하는 건
  자연스러운 확장 — 새 데이터 소스 불필요.
- ADX(추세 강도)와 ATR(변동성 크기)은 서로 다른 축이다: 현재 신호(`is_golden_cross` +
  `ADX_TREND_THRESHOLD`)는 "추세가 있는가"만 게이트로 쓰고 비중은 안 건드리는데, ATR 기반
  사이징은 그 위에 "추세는 있는데 지금 얼마나 거칠게 움직이는가"를 비중에 반영하는 보완적
  레이어로 넣을 수 있다.
- 산출된 `weight`/`position_size`도 마찬가지로 `AUTO_TIER_WEIGHT`/`POSITION_WEIGHT_HARD_CAP`
  범위 안으로 clamp — 안전장치는 사이징 모델이 뭐가 됐든 동일하게 적용.

---

## 3. "Council 불일치도(Agreement Level)" 연동 감쇄 가중치 공식

**이 항목은 완전히 채우지 못했다 — 아래에서 이유를 설명한다.**

"Council"과 "Arbiter Signal"이라는 용어는 이 저장소의 코드(`analyze.py`, `analyze_lib.py`,
`backtest.py`, `news_event_experiment.py`, `news_event_calibration_analysis.py`)나 이번
세션에서 지금까지 만든 어떤 문서(`CLAUDE.md`, `Phase2_뉴스이벤트추출_인수인계.md`,
`post_mortem_template.md`)에도 등장하지 않는다. 현재 이 프로젝트의 판단 구조는:

- **라이브**: `ask_claude_decision` 단일 호출 하나가 시장 요약 + 종목별 결정을 낸다 — 여러
  판단 소스가 투표/합의하는 구조(Council)나 그걸 중재하는 별도 계층(Arbiter)이 없다.
- **Phase 2 실험**: `ask_news_event_judgment` 역시 헤드라인 하나를 보고 단일 판단(사건유형/
  방향/confidence)을 낸다 — 마찬가지로 다중 판단자 구조가 아니다.

그래서 "Council 불일치도"가 정확히 무엇을 가리키는지(예: 여러 AI 판단 소스 간의 불일치? 가격
신호와 뉴스 신호 간의 불일치? 아니면 이 저장소 밖의 별도 계획 문서에 있는, 아직 이 코드베이스에
반영 안 된 미래 설계?) 확인 없이 "연동 방식"을 구체적으로 쓰면 존재하지 않는 시스템과의 연동을
지어내는 셈이 된다 — 그건 하지 않는 게 맞다고 판단했다.

### 대신 정리한 것: 일반적인 "불일치도 기반 감쇄" 개념

용어의 정확한 정의와 무관하게, "여러 판단 소스 간 의견이 갈릴수록 비중을 줄인다"는 아이디어
자체는 일반적인 앙상블/멀티시그널 사이징 기법이다. 이 프로젝트에 적용 가능한 형태로 일반화하면:

```
weight_adjusted = base_weight × f(agreement_level)
```

- `agreement_level`: 0~1(또는 0~100) 스케일의 판단 소스 간 일치도
- `f(agreement_level)`: 감쇄 함수 — 예를 들어 선형(`f(a) = a`), 또는 특정 임계값 아래에서만
  감쇄하는 계단식 함수
- 이 프로젝트에서 "판단 소스 간 불일치"에 해당할 수 있는 실측 후보는 현재 딱 하나 있다: Phase
  2의 `confidence` 필드(0-100, `news_event_calibration_analysis.py`가 이미 고확신/저확신
  분리 임계값 70으로 쓰고 있음) — 이걸 "단일 판단자의 자기 확신도"가 아니라 "여러 신호원 간
  일치도"로 재해석해 쓰려는 것이라면 방향은 이해되지만, 이게 실제로 "Council"이 의도한 것인지는
  확인이 필요하다.

**확인 요청**: "Council"/"Arbiter Signal"이 가리키는 게 (a) 이 저장소 밖의 별도 계획
문서(CLAUDE.md가 언급하는 "저장소 밖 로드맵 문서")에 있는 개념인지, (b) 여러 AI 호출을 병렬로
돌려 투표시키는 새 아키텍처를 이번에 처음 제안하는 것인지, (c) 위에서 추측한 것처럼 confidence
필드를 재해석하는 것인지 알려주시면 이 섹션을 마저 채우겠습니다.
