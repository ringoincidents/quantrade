# QuanTrade A1 Dashboard Wireframe — Step 4·5 (2026-08-29)

> **코드 구현 없음 — Wireframe/Migration Plan 문서만.** 코드 구현은 이 문서
> PM 승인 후 별도 지시로 진행한다. 이 문서는 `A1_Dashboard_Wireframe_Step1-2.md`
> (A1-1/A1-2/A1-3)를 전제로 하며, 그 문서의 13개 블록 번호(#1~#13)를 그대로
> 참조한다.

**전제 4건(PM 확정, 그대로 반영)**:
1. ORDER는 UI 스켈레톤만 — Preview/Risk Check 연동·API 호출은 Phase C(§11), 이번엔 빈 상태/더미 데이터.
2. TODAY는 레이아웃/필드만 — 4개 생성기(`market_indicators.py`/`news_event_cards.py`/`post_trade_review.py`/`portfolio_report.py`) 합성 로직은 A2(Intelligence Layer) 완료 후 연결, 이번엔 목업 데이터.
3. Alpha Lab 3블록(#5/#11/#12)은 메인에서 제거 → 별도 "Research" 탭/뷰로 이동(삭제 아님, 코드 레벨 태그 PR#36 유지, UI 진입경로만 분리).
4. §16.1 로깅 훅: TODAY 요약 카드 각 항목 "확인함" / EVENTS 리스트 각 항목 "관련있음·관련없음" / WATCHLIST·STOCK DETAIL·MARKET·MY PORTFOLIO는 훅 없음(조회 전용). 실제 로깅 구현은 A5, 이번엔 자리만.

---

## A1-4: Wireframe (모바일 우선, 데스크톱 보조)

### 0. 내비게이션 구조 (제안)

7영역(+Research)을 전부 동급 탭으로 두면 모바일 하단 탭바가 감당하기 어렵다(일반적으로 5개 안팎이 한계). 아래처럼 **1차 탭 5개 + 드릴다운 2개 + 별도 진입점 1개**로 나누는 안을 제안한다 — 이는 이 문서의 제안이며 확정 사항이 아니다.

```
┌ 1차 탭(하단 탭바, 상시 노출) ─────────────────┐
│  TODAY · MY PORTFOLIO · MARKET · EVENTS · WATCHLIST │
└───────────────────────────────────────────────┘
┌ 드릴다운(탭 아님, 상위 화면에서 종목 탭하면 진입) ─┐
│  STOCK DETAIL(종목 하나) → ORDER(매수/매도 버튼)  │
└───────────────────────────────────────────────┘
┌ 별도 진입점(하단 탭바 밖, 상단 아이콘/링크) ───────┐
│  Research (Alpha Lab, PM 전제 3)                  │
└───────────────────────────────────────────────┘
```

근거: STOCK DETAIL은 "화면"이라기보다 특정 종목의 드릴다운이라 탭바에 상시 노출할 대상이 아니다(MARKET/WATCHLIST/EVENTS/MY PORTFOLIO 어디서든 종목 하나를 탭하면 진입). ORDER는 STOCK DETAIL에서만 진입 가능하고 Phase C 전까지 비활성이라 탭바에 놓으면 "눌러도 안 되는 탭"이 상시 보이게 된다. Research는 PM 전제 3("메인에서 제거")의 취지가 "덜 눈에 띄게"이므로 하단 탭바보다 상단의 보조 진입점이 그 취지에 맞는다.

전체 프레임(모든 화면 공통):
```
┌─────────────────────────────────┐
│ QuanTrade                [Research →] │ ← 상단바: 타이틀 + Research 진입점
├─────────────────────────────────┤
│                                   │
│         (활성 탭 콘텐츠)          │
│                                   │
├─────────────────────────────────┤
│ [TODAY][PORTFOLIO][MARKET][EVENTS][WATCH] │ ← 하단 탭바
└─────────────────────────────────┘
```
데스크톱 보조안: 640px 고정폭 단일 컬럼 대신 좌측 사이드 내비(세로 탭 목록, 위 5개+Research)+ 우측 콘텐츠 영역의 2컬럼으로 확장. 콘텐츠 카드 구조 자체는 모바일과 동일해 재사용 가능(현재 CSS의 `.page{max-width:640px}` 제약만 데스크톱에서 풀어주면 됨).

---

### 1. TODAY (레이아웃/필드만, 목업 데이터, PM 전제 2)

```
┌─────────────────────────────────┐
│ 오늘 2026-08-29                   │
│ 각 소스 최신 기준시각 요약 배지 4개 │ ← market_indicators/news_event_cards/
│ [시장 09:xx][사건 18:xx]          │   post_trade_review/portfolio_report
│ [포트폴리오 주1회][사후점검 -]     │   각자 갱신 주기 다름(기존 사실 유지)
├─────────────────────────────────┤
│ 🛑 시스템 상태                    │ ← autoexec_state.json (A2 합성 대상 아님,
│ 킬스위치: 해제됨 · 예측경로: 비활성 │   단순 passthrough라 A1에서 바로 표시 가능)
├─────────────────────────────────┤
│ 📐 오늘의 규칙/리스크           [전체보기→MY PORTFOLIO] │
│ ┌───────────────────────────┐   │
│ │ (mock) 집중도 규칙 해당 1건  [✓확인함]│ ← §16.1 훅 자리(항목별)
│ │ (mock) ...                          │
│ └───────────────────────────┘   │
├─────────────────────────────────┤
│ 📈 오늘의 시장 상태             [전체보기→MARKET] │
│ ┌───────────────────────────┐   │
│ │ (mock) 종목A 변동성 급등     [✓확인함]│
│ └───────────────────────────┘   │
├─────────────────────────────────┤
│ 📄 오늘의 사건                  [전체보기→EVENTS] │
│ ┌───────────────────────────┐   │
│ │ (mock) 종목B [공시] 요약 한줄  [✓확인함]│
│ └───────────────────────────┘   │
├─────────────────────────────────┤
│ 🗒️ 최근 활동 기록                [전체보기→MY PORTFOLIO] │
│ (mock) 최신 사후점검 리포트 1건 발췌 [✓확인함]│
└─────────────────────────────────┘
```

**필드 목록(목업 단계에서 확정할 것)**: 각 카드는 `{항목 요약 텍스트, 발생/기준 시각, 원본 블록 링크, 확인함 상태(bool, 로컬만)}` 4개 필드로 통일 제안. 실제 값 채우기(4개 생성기 출력 합성)는 A2 완료 후.

**PM 전제 2 관련 사실 기록**: A1 Step1-2(§1의 #6)에서 "규칙위반점검"을 `analyze.py`의 `check_risk_guardrails()`(`last_report.json.guardrail_violations`) 기반으로 TODAY에 배정했으나, 이번 PM 전제가 TODAY 합성 소스를 `portfolio_report.py` 등 4개 생성기로 명시하면서 이 배정을 **`portfolio_report.py`의 `rule_matches`(#3에서 이미 쓰이던 필드) 기반으로 정정**한다. 이 정정에는 기술적 근거가 있다 — 2026-08-28 세션에서 `analyze.py`의 크립토 모의포지션 로직을 전부 제거하면서 `portfolio.json`의 `positions`가 항상 빈 배열이 됐고, `check_risk_guardrails()`의 비중 상한 검사는 순회할 포지션이 없어 상시 통과, 현금비율 검사도 `cash / total_assets`가 포지션 없이는 항상 100%라 상시 통과한다 — 즉 **`analyze.py` 쪽 가드레일은 지금 사실상 "위반 없음"으로 고정된 상태**다(코드는 살아있고 삭제되지 않았으나 출력이 무의미해짐). `portfolio_report.py`의 `rule_matches`(실계좌 기반)가 TODAY의 "오늘의 규칙" 카드로는 더 의미 있는 소스다.

---

### 2. MY PORTFOLIO (#1+#2+#3+#4 통합, 훅 없음)

```
┌─────────────────────────────────┐
│ 💰 총자산 XX원 · 현금 XX원        │ ← #1 real_portfolio.json
│ [도넛차트: 보유비중]              │
├─────────────────────────────────┤
│ [현황] [배분] [리스크] [손익추이] │ ← 내부 서브탭 4개
├─────────────────────────────────┤
│ (현황 탭 선택 시)                 │
│ 종목별 보유 목록 + 수익률          │ ← #1, 탭하면 STOCK DETAIL
│ ┌───────────────────────────┐   │
│ │ 종목A  ₩XXX,XXX   +X.XX%    │ →│
│ └───────────────────────────┘   │
│                                   │
│ (배분 탭 선택 시)                 │
│ 자산군 배분 갭 표                 │ ← #3 asset_class_gap
│ 포트폴리오 역할 계층 표            │ ← #3 role_gap
│ 배분 로드맵 표                    │ ← #3 roadmap
│                                   │
│ (리스크 탭 선택 시)                │
│ 신규매수 권장금액 범위             │ ← #4 position_sizing
│ MDD 예산 소진율                   │ ← #4 mdd_budget
│ 집중도 / 상관관계(0.7↑) / 비용반영후│ ← #4
│                                   │
│ (손익추이 탭 선택 시)              │
│ [총손익(정적)+평가손익(자동) 차트] │ ← #2 historical_pnl_manual +
│                                   │   portfolio_pnl_history
└─────────────────────────────────┘
```

**PM 전제 4 적용**: 훅 없음(조회 전용) — 그대로 유지.

**A1 Step1-2 §1 각주 반영**: #1의 "⏳ AI 매매 제안" 서브카드(`loadRetiredPending`, `badge-retired`)는 이 화면에 옮기지 않는다 — 제안(§1 및 A1-5 삭제 목록) 참고.

---

### 3. MARKET (#8, 훅 없음)

```
┌─────────────────────────────────┐
│ 시장 상태 (보유+관심종목 한정)     │ ← market_indicators.json/state_board
│ ※ "시장 전체"가 아니라 보유+관심   │   (A1 Step1-2 §2에서 이미 기록한
│   종목으로 스코프가 좁혀져 있음    │    이름-범위 불일치 사실 재확인)
├─────────────────────────────────┤
│ 종목      20일변동성  백분위  ADX │
│ 종목A       X.XX%     XX.X   XX.X│ → (탭하면 STOCK DETAIL)
│ 종목B       X.XX%     XX.X   XX.X│ →
├─────────────────────────────────┤
│ 보유종목간 평균 상관계수: 0.XXX    │
│ (X쌍, 최근 60거래일)              │
└─────────────────────────────────┘
```

---

### 4. EVENTS (#7, 훅 있음 — 관련있음/관련없음)

```
┌─────────────────────────────────┐
│ 오늘의 사건 카드 (N건)             │ ← news_event_cards.json
│ 사실 설명이며 매매 판단이 아닙니다  │
│ [전체][뉴스][이상행동]            │ ← 필터 칩
├─────────────────────────────────┤
│ ┌───────────────────────────┐   │
│ │ 종목A          [공시]        │   │
│ │ 요약 문장...                 │ → │ (탭하면 STOCK DETAIL)
│ │ [관련있음] [관련없음]         │   │ ← §16.1 훅 자리(항목별)
│ └───────────────────────────┘   │
│ ┌───────────────────────────┐   │
│ │ 종목B          [이상행동]     │   │
│ │ 거래량 20일평균 대비 X.X배    │ → │
│ │ [관련있음] [관련없음]         │   │
│ └───────────────────────────┘   │
└─────────────────────────────────┘
```

---

### 5. WATCHLIST (신규, 훅 없음)

```
┌─────────────────────────────────┐
│ 관심종목 (X개)              [+추가]│ ← watchlist.json (현재 symbols: [])
├─────────────────────────────────┤
│ ┌───────────────────────────┐   │
│ │ 종목A(005930)                │   │
│ │ 목표가: ₩XX,XXX (진입일 YYYY)│ →│ ← target_prices.json (현재 targets: {})
│ │                       [삭제] │   │
│ └───────────────────────────┘   │
│ (비어있을 때) "관심종목이 없습니다.│
│  + 버튼으로 추가하세요."          │
└─────────────────────────────────┘
```

**사실 기록**: `watchlist.json`/`target_prices.json` 둘 다 현재 비어있다(A1 Step1-2 §3에서 이미 확인한 사실). 이 화면은 신규 UI일 뿐 아니라, 완성되면 처음으로 이 두 파일에 실제 데이터가 쌓이기 시작하는 계기가 된다 — `news_event_cards.py`/`market_indicators.py`의 `build_universe()`(보유+관심종목)가 지금은 사실상 "보유종목만"으로 동작 중이라는 뜻이기도 하다(사실만 기록, 판정 아님).

---

### 6. STOCK DETAIL (신규, 훅 없음)

```
┌─────────────────────────────────┐
│ ← 종목A (005930)                  │
├─────────────────────────────────┤
│ (보유 중이면) 보유 XX주 · 평가손익 +X.XX%│ ← real_portfolio.json (symbol 매칭)
│ (관심종목이면) [관심종목 해제]      │ ← watchlist.json
│ (둘 다 아니면) [관심종목 추가]      │
├─────────────────────────────────┤
│ 지표                              │ ← market_indicators.json/indicator_board
│ PER: 데이터 소스 미연결            │   (symbol 매칭)
│ 20일 변동성 / 모멘텀 / ADX(14)     │
├─────────────────────────────────┤
│ 시장 상태                          │ ← market_indicators.json/state_board
│ 변동성 백분위: XX.X                │   (symbol 매칭)
├─────────────────────────────────┤
│ 목표가                             │ ← target_prices.json (symbol 매칭)
│ ₩XX,XXX 도달 시 XX% 매도 (설정됨)  │
├─────────────────────────────────┤
│ 관련 사건 (X건)                    │ ← news_event_cards.json (symbol 필터)
│ (카드 목록, EVENTS와 동일 렌더러)   │
├─────────────────────────────────┤
│           [매수]  [매도]           │ → ORDER (Phase C 전까지 비활성 배지)
└─────────────────────────────────┘
```

**A1-3 재확인**: 이 화면이 필요로 하는 "여러 JSON을 종목코드로 조인"하는 로직은 현재 어디에도 없다(A1 Step1-2 §3에서 이미 기록한 사실) — 이 화면 자체가 그 조인 계층의 최초 소비처가 된다.

---

### 7. ORDER (신규, 스켈레톤만 — PM 전제 1, Phase C 전까지 비활성)

```
┌─────────────────────────────────┐
│ ← 주문                [비활성 배지]│ ← "Phase C(§11) 전까지 비활성" 고정 표시
├─────────────────────────────────┤
│ 종목: 종목A (005930)               │ ← STOCK DETAIL에서 전달됨
├─────────────────────────────────┤
│ 주문 종류                          │
│ ○ 매수   ○ 매도                   │ ← 더미 라디오, 선택 가능하나 이후 단계 없음
├─────────────────────────────────┤
│ 수량                               │
│ [        ] 주                     │ ← 더미 입력란(비활성 or 입력은 되나 저장 안 됨)
├─────────────────────────────────┤
│ 가격                               │
│ ○ 시장가   ○ 지정가 [        ]    │ ← 더미
├─────────────────────────────────┤
│ 예상 금액                          │
│ (빈 상태) "수량/가격 입력 후 표시"  │ ← Phase C 전까지 계산 없음
├─────────────────────────────────┤
│ Risk Engine 표시 자리              │ ← MY PORTFOLIO #4 렌더러 재사용 예정
│ (빈 상태) "Preview 연동 전 — 표시  │   (자리만, 이번엔 연동 안 함)
│  없음"                             │
├─────────────────────────────────┤
│         [승인 버튼 자리]            │ ← 비활성(disabled), 클릭 불가
│  "이 화면은 Phase C(§11) 전까지    │
│   실제 주문을 실행하지 않습니다."   │
└─────────────────────────────────┘
```

---

### 8. Research (Alpha Lab, #5/#11/#12, PM 전제 3)

```
┌─────────────────────────────────┐
│ ← Research (Alpha Lab)            │
│ 참고용 — Core 판단에 사용 안 함    │ ← 기존 코드 주석(c3afea4)과 동일 문구
├─────────────────────────────────┤
│ 📚 118티커 통계 검증 결과           │ ← #5, indicator_significance_report.json
│ (기존 렌더러 그대로 재사용)         │
├─────────────────────────────────┤
│ 📊 게이트 판정 요약                │ ← #11, backtest_report.json
│ (기존 렌더러 그대로 재사용)         │
├─────────────────────────────────┤
│ 📰 데이터수집 진행상황              │ ← #12, news_event_calibration_log.json
│ (기존 렌더러 그대로 재사용)         │
└─────────────────────────────────┘
```

내용/렌더러는 기존 그대로, **진입 경로만** 메인 하단 탭바에서 상단 보조 링크로 옮긴다(PM 전제 3 — "코드 레벨 태그는 그대로 두고 UI 레벨에서만 진입경로 분리"). `loadIndicatorLearning`/`loadGate`/`loadPhase2` 세 함수는 수정 없이 그대로 옮겨 붙이면 된다(A1-5 §"재사용" 참고).

---

## A1-5: Migration Plan (파일 단위)

전제: 이 저장소는 빌드 스텝 없는 정적 페이지(GitHub Pages, `index.html` 그대로 서빙)라는 원칙이 있다(CLAUDE.md). 아래 계획은 이 원칙을 유지한다는 전제로 짰다 — 프레임워크/번들러 도입은 제안하지 않았고, `index.html` 하나에 7+1개 화면 컨테이너를 전부 두고 탭 전환은 클라이언트 사이드에서 `display` 토글로 처리하는 방식을 제안한다(기존에도 각 블록이 독립적으로 비동기 로드되는 패턴을 그대로 확장). 파일을 여러 개로 쪼갤지(예: 화면별 JS 분리)는 유지보수성 문제이지 이번 지시의 필수 요구사항이 아니라 **제안으로만** 적어둔다.

### 삭제할 코드

| 파일 | 대상 | 사유 |
|---|---|---|
| `index.html` | `real-pending-section` DOM 블록 + `loadRetiredPending()` 함수 | "⏳ AI 매매 제안" — 이미 `badge-retired`로 상호작용 불가 표시된 죽은 UI. 기반 데이터(`pending_actions.json`의 `target_account:"real"` 항목)는 손대지 않음 — **화면 표시 코드만** 삭제 대상. 백엔드 `analyze.py`의 `REAL:` 제안 큐잉 로직 자체는 v3.2 보존 원칙 대상이라 미포함. |

**그 외 삭제 후보 없음** — v4.0 §15/CLAUDE.md 보존 원칙에 따라 Alpha Lab 3블록은 "이동"이지 "삭제"가 아니며, 나머지 10개 블록은 전부 재배치 대상이지 삭제 대상이 아니다.

### 유지할 코드 (백엔드, 변경 없음)

| 파일 | 비고 |
|---|---|
| `real_portfolio_sync.py` | Core, 조회 전용 |
| `portfolio_report.py` | Core, MY PORTFOLIO 대부분 + TODAY 규칙매치 소스 |
| `market_indicators.py` | Core, MARKET/STOCK DETAIL 소스 |
| `news_event_cards.py` | Core, EVENTS/STOCK DETAIL 소스 |
| `post_trade_review.py` | Core, TODAY 활동기록 소스 |
| `analyze.py`/`analyze_lib.py` | Core, 가드레일·킬스위치 판정 유지(단, 위 TODAY 절에서 기록한 대로 가드레일 출력이 현재 사실상 상시-통과 상태) |
| `check_updates.py`/`autoexec.py`/`autoexec_stop_fast.py` | Core, 텔레그램/킬스위치 |
| `backtest.py`/`indicator_significance_test.py`/`news_event_experiment.py`/`news_event_backtest.py`/`news_event_prompt_experiment.py`/`news_event_calibration_analysis.py` | Alpha Lab, PR#36 태그 유지, 물리적 이동 없음(PM 전제 3) |
| 전체 JSON 상태파일 | 스키마 변경 없음 |

### 재사용할 코드 (프런트엔드, 그대로 옮겨 붙임)

| 파일(현재) | 함수/요소 | 새 위치(제안) |
|---|---|---|
| `index.html` `<script>` | `fmtPct`/`fmtBasis`/`esc`/`pick`/`assertNoPredictiveFields`/`PREDICTIVE_FIELDS` | 공통 유틸 — 화면 분리 여부와 무관하게 전 영역에서 그대로 재사용 |
| 〃 | `renderGuardrailCard`/`renderRuleMatch` | MY PORTFOLIO(배분) / TODAY(규칙 카드) |
| 〃 | `renderAssetClassGap`/`renderRoleGap` | MY PORTFOLIO(배분) |
| 〃 | `renderPositionSizing`, `loadRiskEngine` | MY PORTFOLIO(리스크) |
| 〃 | `loadRealAccount`(파이차트 포함), `loadPnlHistory` | MY PORTFOLIO(현황/손익추이) |
| 〃 | `renderNewsInfoCard`, `loadNewsCards` | EVENTS 전체 + STOCK DETAIL(종목 필터만 추가) + TODAY(발췌만 추가) |
| 〃 | `loadMarketIndicators`(현재 한 함수가 두 섹션을 채움 — MARKET/STOCK DETAIL로 갈라지면 두 화면이 같은 fetch 결과를 나눠 써야 함, 함수 분리 검토 필요) | MARKET(state_board 부분) + STOCK DETAIL(indicator_board 부분) |
| 〃 | `renderReviewCard`, `loadPostTradeReview` | MY PORTFOLIO(활동 로그) 또는 TODAY(최신 1건 발췌) — 로직은 동일, 호출부만 두 곳 |
| 〃 | `renderLearningRow`, `loadIndicatorLearning` | Research |
| 〃 | `loadGate` | Research |
| 〃 | `loadPhase2` | Research |
| 〃 | `loadAutoexecStatus` | TODAY(시스템 상태 스트립) |

### 새로 작성할 코드 (신규)

| 영역 | 신규 작성 내용 | 비고 |
|---|---|---|
| 내비게이션 셸 | 하단 탭바 5개 + 상단 Research 링크 + 탭 전환(컨테이너 display 토글) | 기존 index.html엔 내비게이션 자체가 없음(단일 스크롤 페이지) — 완전 신규 |
| TODAY | 4개 카드 컨테이너 + 항목별 "확인함" 훅 UI(클릭 시 로컬 상태만 토글, 서버 전송 없음 — A5에서 구현) + 시스템 상태 스트립 | 목업 데이터 렌더링까지만(PM 전제 2), 실합성 로직은 A2 이후 |
| WATCHLIST | `watchlist.json` 렌더 + 추가/삭제 UI, `target_prices.json` 목표가 표시/입력 UI | 오늘 이 파일들을 읽어 그리는 코드가 전혀 없음(A1 Step1-2 §3 확인 사실) — 완전 신규 |
| STOCK DETAIL | 종목코드 기준 여러 JSON 조인 계층(신규 함수, 예: `buildStockDetail(symbol)` 성격) | A1 Step1-2 §3에서 "조인 로직 자체가 없음"으로 확인된 공백을 메우는 신규 코드 |
| EVENTS | 필터 칩(전체/뉴스/이상행동) + 항목별 "관련있음/관련없음" 훅 UI | 기존 `loadNewsCards`는 재사용, 필터/훅 UI만 신규 |
| ORDER | 스켈레톤 폼 전체(종목표시/주문종류/수량/가격/예상금액 빈상태/Risk Engine 자리/승인버튼 자리) | 전부 신규, "Phase C 전까지 비활성" 고정 배지 포함. `autoexec.place_sell_order()`/매수 함수는 여전히 미구현 상태로 둠(건드리지 않음) |
| Research | 진입 링크 + 컨테이너 라우팅만 | 내부 렌더러 3개는 재사용(위 표), 이 부분만 신규 |

### 열린 질문 (이 문서가 결정하지 않음)

- `index.html` 단일 파일 유지 vs 화면별 파일 분리 — "빌드 스텝 없음" 원칙 안에서도 둘 다 가능. 이 문서는 단일 파일 유지 쪽으로 기울여 제안했으나 확정은 아니다.
- `loadMarketIndicators()`를 MARKET/STOCK DETAIL 두 화면이 나눠 쓸 때 fetch를 한 번만 하고 캐시할지, 화면 진입마다 새로 fetch할지 — 성능/신선도 트레이드오프이며 A1-4/5 범위 밖 세부 구현.
- TODAY의 "확인함"/EVENTS의 "관련있음·관련없음" 훅이 로컬(브라우저)에만 남는지, 서버(파일)에 기록되는지 — §16.1 실제 구현(A5)의 결정 사항. 이 문서는 자리만 표시했다.

---

## 부록 — A1 Step1-2 대비 변경/정정 사항

- TODAY의 "규칙" 카드 소스를 `analyze.py`(가드레일) → `portfolio_report.py`(`rule_matches`)로 정정(위 §1 참고, PM의 4-생성기 지정과 2026-08-28 크립토 로직 제거의 결과가 맞물린 것).
- 그 외 A1 Step1-2의 13→7 매핑, Data Dependency Map, 유지/통합/이동 판정은 이번 지시와 상충하지 않아 그대로 유지.
