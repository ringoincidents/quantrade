# QuanTrade 상태 보고서 — 2026-08-09

> 세션 종료 시 갱신. 이 문서는 **현재 상태 요약**이다. 결정을 내리지 않으며,
> 판정성 서술은 담지 않는다(CLAUDE.md 세션 시작 체크리스트 "State facts,
> don't render verdicts"). 이번 세션은 `세션보고서_2026-08-08.md` 이후
> 방향성 세션이 순서대로 지시한 작업들을 하나로 묶은 보고서다 — 킬스위치
> 타이밍조사 병합, GitHub Actions 신뢰성 이슈 기록, 뉴스 레이어 확장(스코프
> 축소 + 이상행동 흡수 + 죽은 코인 출력 제거), 시장 상태/지표 표시판 신설.

---

## 1. 한눈에

| 항목 | 상태 | 근거 |
|---|---|---|
| 킬스위치 타이밍조사 문서 병합 | ✅ 완료 | PR#18 |
| GitHub Actions 스케줄 신뢰성 이슈 기록 | ✅ 완료 (방향성 안건으로, 판정 아님) | PR#19 |
| 세션 상태 요약 문서화 | ✅ 완료 | PR#20 |
| 뉴스 카드 스코프 축소(보유+관심종목만) | ✅ 완료 | PR#21 |
| 이상행동 감시(거래량/갭/급등락) 뉴스 카드에 흡수 | ✅ 완료, 같은 스키마 재사용 | PR#21 |
| 죽은 코인 모의투자 출력 제거 | ✅ 완료 (텔레그램/보고서 텍스트만, 시뮬레이션 상태는 유지) | PR#21 |
| 시장 상태 수치판 ("국면 판별"의 재정의) | ✅ 완료 | PR#22 |
| 지표 병렬 표시판 ("종목 스코어링"의 재정의) | ✅ 완료 | PR#22 |
| 공통 규칙(점수/순위/색상신호등/결론문장 금지) 위반 | 0건 (자체 감사 + 저장거부 경로 self-test로 확인) | PR#22, §4 참고 |
| `place_sell_order()` | 미구현 유지 | 토스 주문 API 엔드포인트/필드명 미확보 |
| `RULE_BASED_AUTOEXEC_ENABLED` | `false` 유지 | 전환 안 함 |
| 킬스위치 응답 타이밍 최종 확인 | ⏸️ 여전히 미확정 | `세션보고서_2026-08-08.md` §3 그대로 승계 |

---

## 2. 이번 세션 타임라인 (사실만)

1. **킬스위치 타이밍조사 병합 + 다음 단계** — `킬스위치_타이밍조사_2026-08-08.md`를 사실 기록 문서로 병합(코드 변경 없음). GitHub Actions 스케줄 신뢰성을 CLAUDE.md에 방향성 안건으로 등록: "poll.yml/autoexec_stop_fast.yml이 스케줄대로 실행되지 않는 사례 확인됨(2026-08-08, 69분 및 22분 무실행 구간). 원인은 GitHub Actions 플랫폼 레벨 지연으로 추정, 코드 문제 아님." `docs.github.com` 직접 열람은 이번에도 정책 차단으로 막혀 1급 소스 확인은 못 했고, 검색으로 확인한 문서 요지 + GitHub 스태프의 공개 커뮤니티 발언을 정황 증거로 기록. `workflow_dispatch`를 킬스위치 보조 경로로 병행하는 방안은 가능성만 조사(미구현) — 자동화하려면 결국 "서버 없음" 원칙과 부딪힘. → **PR#19**
2. **세션 상태 요약 문서화** — 한국어 응답 전환 지시에 따라, 방향성 세션이 불러준 5개 항목 상태 요약을 `세션보고서_2026-08-08.md`로 작성. → **PR#20**
3. **뉴스 레이어 확장 — 이상행동 감시 흡수 + 스코프 축소** (방향성 세션 지시, 2026-08-09):
   - **스코프 축소**: "오늘의 사건카드" 대상을 보유종목(`real_portfolio.json`) + 관심종목(`watchlist.json`, 신설 — 사람이 수기 입력)으로만 한정. 삼성전자·SK하이닉스 같은 비보유 대형주 헤드라인 제거. `news_event_cards.py`의 `build_universe()`가 이 유니버스를 만든다.
   - **이상행동 흡수**: 별도 기능이 아니라 같은 카드 스키마(`CARD_FIELDS`)에 `event_type: "이상행동"` 카드로 추가. 거래량 급변(20일 평균 대비 배수) / 가격 갭(전일 종가 대비 시가 %) / 급등락(최근 20일 변동성 대비 배수) 세 가지를 `detect_anomalies()`가 순수 산술로 판정 — AI를 거치지 않는다(autoexec.py의 세 규칙과 같은 성격). "위험 신호"/"매도 검토" 같은 판단 문구는 self-test가 금지어로 걸러 확인.
   - **죽은 코인 출력 제거**: `analyze.py`/`check_updates.py`의 텔레그램·보고서 텍스트에서 크립토(`asset_class` 기본값 `"crypto"`) 관련 출력만 억제. 현금 계산, 하드손절 실행, 거래 기록 등 시뮬레이션 상태 자체는 건드리지 않음(보수적 스코프 해석).
   - **실사용 검증**: `workflow_dispatch`로 `news_event_cards.yml`을 직접 실행 — 실제 대상 종목 `['196170', 'NMR', 'NVDA', 'RIG', 'SCHD']` 5건, 뉴스 카드 5건 생성 확인(`news_event_cards.json`, 커밋 `cb19044`).
   → **PR#21**
4. **시장 국면 판별 + 종목 스코어링 (프레이밍 주의)** (방향성 세션 지시, 2026-08-09) — 아래 §4에서 상세.
   → **PR#22**

---

## 3. 미해결 항목 (다음 세션 승계)

- **킬스위치 응답 타이밍 최종 확인** — PR#15 적용(2026-08-08 14:42:59 KST) *이후* 시점의 정상 응답 사례가 아직 관측되지 않음. 확인되는 대로 CLAUDE.md "킬스위치 검증 완료" 기록.
- **11:30→11:31 페어링 불일치** — `킬스위치_타이밍조사_2026-08-08.md` §4-1, 사용자 측 원본 로그 재대조 필요.
- **엔드포인트/필드명 확보 방법론** — 문서 호스트 차단 우회 또는 대체 검증 경로. 별도 세션 미결정.
- **GitHub Actions schedule 신뢰성 방향성 안건** — `RULE_BASED_AUTOEXEC_ENABLED` 전환 논의 시 함께 다룰 것.
- **`watchlist.json`이 비어 있음** — 사람이 수기로 채워야 뉴스 카드/이상행동 감시/시장 상태·지표 표시판 모두 관심종목 쪽 대상이 늘어난다. 현재는 보유종목 5개(`196170`/`NMR`/`NVDA`/`RIG`/`SCHD`)만 대상.

---

## 4. 이번 작업 상세 — 시장 상태 수치판 + 지표 병렬 표시판

### 4.1 지시와 재정의

방향성 세션은 "국면 판별"과 "종목 스코어링"이라는 표현 자체가 종합판단으로
읽힐 수 있다는 점을 지적하며, 공통 규칙(위반 시 전체 반려)을 먼저 못박았다:
합산 점수·순위 정렬·색상 신호등 금지, "그래서 사도 됨/팔아야 함" 결론 문장
금지. 이에 따라 두 기능을 다음처럼 재정의해 구현했다(`market_indicators.py`,
신규):

- **현재 시장 상태 수치판**: 종목별 20일 변동성의 과거 분포 내 백분위,
  ADX(14) 원값(해석 없이 숫자만), 그리고 **보유 종목 간**(지시 원문 그대로,
  관심종목 제외) 일간수익률 평균 쌍별 피어슨 상관계수.
- **지표 병렬 표시판**: 종목별로 PER(데이터 소스 미연결 — 숫자를 지어내지
  않음, `rule_trigger_report.py`와 동일 원칙)/20일 변동성/20일 모멘텀/ADX를
  표로만 나열. **출력 순서는 항상 원본(보유+관심종목) 순서를 보존** — 어떤
  계산값으로도 프로그램이 먼저 재정렬하지 않는다.

### 4.2 위반 시 전체 반려의 구현

`audit()`가 결과 JSON 전체를 재귀 검사해 금지 필드(`score`/`rank`/`ranking`/
`phase`/`regime`/`signal`/`grade`/`rating`/`color`/... )나 금지 문구
("상승장"/"매수"/"1위"/"국면 전환"/...)를 찾으면, `save_if_clean()`이
**파일을 저장하지 않고 `SystemExit(1)`로 종료**한다. `rule_trigger_report.py`의
`audit()`는 위반을 `_audit_violations`로 태그만 달아 그대로 저장하지만, 이
작업은 지시 원문이 "위반 시 전체 반려"를 명시했으므로 더 엄격한 동작을 썼다.
GitHub Actions 워크플로(`market_indicators.yml`)에도 self-test 선행 → 계산 →
독립적인 감사 재확인 단계를 넣어 이중으로 걸었다.

**구현 중 실제로 걸린 사례**: 최초 구현에서 상단 `note` 필드에 "국면 판별의
재정의"를 설명하며 "순위·색상 신호등을 만들지 않습니다"라고 썼는데, 이 설명
문장 자체가 금지 문구 "순위"를 문자 그대로 담고 있어 로컬 실행 시 감사에
걸려 저장이 거부됐다(`news_event_cards.py` self-test가 이전에 겪은
자기지시적 함정과 같은 종류 — "이걸 안 한다"고 말하는 문장이 금지어를 그대로
포함). 문구를 "우열을 매겨 배열하지 않으며"로 다시 써서 해결했고, 이 실패
사례 자체가 감사 로직이 실제로 작동한다는 증거이기도 하다. 재발 방지로
self-test에 `build_report()`가 만드는 고정 문구 자체를 감사하는 케이스를
추가했다.

### 4.3 검증

- `python market_indicators.py --self-test` — 통과 (변동성 백분위 방향성,
  피어슨 상관계수 경계값(±1), 지표 표시판 순서 보존(계산값으로 재정렬되지
  않음을 의도적으로 뒤바뀔 상황을 만들어 확인), 상태판/지표판 필드
  화이트리스트, 오염된 리포트에서 감사가 실제로 걸리는지, 위반 시
  `save_json`이 호출되지 않고 `SystemExit`이 발생하는지, `build_report()`의
  실제 고정 문구가 감사를 통과하는지까지 8개 케이스).
- `python news_event_cards.py --self-test` — 회귀 없음 확인(재사용한
  `build_universe`/`fetch_candles_for_anomaly`에 영향 없음).
- `index.html`의 `<script>` 블록을 Node `new Function`으로 구문 검증.
- **실사용 검증**: 병합(PR#22, `main` `ffa3e79`) 후 `workflow_dispatch`로
  `market_indicators.yml`을 실제 실행. 결과: 보유종목 5개 전부 정상 계산됨
  (예: 알테오젠 20일 변동성 6.06%/백분위 89.6/ADX 56.7, 보유종목 간 평균
  상관계수 0.120·10쌍·60거래일). 커밋된 `market_indicators.json`을 로컬에서
  독립적으로 다시 `audit()`에 통과시켜 위반 0건 재확인(워크플로 내부 검증에만
  의존하지 않음).
- **자체 점검 결과 (지시 원문 요구사항)**: 공통 규칙 위반 0건 — 합산 점수·순위
  정렬·색상 신호등·결론 문장 어디에도 없음, `per`/`volatility_20d_pct`/
  `momentum_20d_pct`/`adx_14`/`correlation` 모두 값의 나열이며 라벨이나 해석이
  붙지 않음, 지표 표시판 순서는 원본 순서 그대로(계산값 재정렬 없음).

### 4.4 대시보드 화면 캡처

로컬 HTTP 서버 + Playwright(Chromium)로 실제 `market_indicators.json`
데이터를 렌더링한 두 블록을 캡처했다(별도 첨부 이미지 참고):

- **시장 상태 수치판**: 종목명/코드, 20일 변동성, 변동성 백분위, ADX(14),
  하단에 "보유 종목 간 평균 상관계수: 0.120 (10쌍, 최근 60거래일)".
- **지표 병렬 표시판**: 종목명/코드, PER("데이터 소스 미연결"), 20일 변동성,
  20일 모멘텀, ADX(14). 각주: "나열 순서는 보유+관심종목 원본 순서이며,
  합산해 하나의 값으로 만들거나 종목 간 우열을 매기지 않습니다."

### 4.5 변경 파일

| 파일 | 변경 |
|---|---|
| `market_indicators.py` | 신규, 412줄 |
| `.github/workflows/market_indicators.yml` | 신규, 57줄 |
| `index.html` | +99/-1줄 — 새 블록 2개, `PREDICTIVE_FIELDS`에 rank/ranking/phase/regime/grade/rating/color/colour 추가 |

---

## 5. 여전히 금지/보류 중

- `place_sell_order()`/`place_buy_order()` 구현
- `RULE_BASED_AUTOEXEC_ENABLED` → `true` 전환
- CLAUDE.md "킬스위치 검증 완료" 기록
- 방향/확신도 예측 재개(§2.1 게이트 재통과 없이는 불가)

---

## 부록 — 이번 세션 PR (2026-08-08 이후 누적)

| PR | 내용 |
|---|---|
| #15 | 킬스위치 상태 미영속화 버그 수정, `poll.yml` 5분 단축, `autoexec_stop_fast.yml` 신설 |
| #16 | `toss_order_endpoint_probe.py` 신설(조회 전용) |
| #17 | 조회 엔드포인트 검증 결과 기록 + "same-session sign-off" 정정 |
| #18 | 킬스위치 타이밍조사 문서 |
| #19 | GitHub Actions 스케줄 신뢰성 이슈를 방향성 안건으로 기록 |
| #20 | 세션보고서 2026-08-08 |
| #21 | 뉴스 카드 스코프 축소 + 이상행동 흡수 + 죽은 코인 출력 제거 |
| #22 | 시장 상태 수치판 + 지표 병렬 표시판 (이번 문서 §4) |
