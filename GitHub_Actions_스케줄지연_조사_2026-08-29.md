# GitHub Actions `schedule` 트리거 지연 조사 (2026-08-29)

## 배경

`킬스위치_타이밍조사_2026-08-08.md`(2026-08-08)에서 `poll.yml`이 72분간 한 번도 실행되지 않은 구간을 발견하고, GitHub Actions의 `schedule` 트리거가 플랫폼 부하 시 지연될 수 있다는 사실을 기록한 바 있다. 이번 조사는 그 이후(`*/5 * * * *`로 단축된 이후) 실측 데이터로 지연 규모를 다시 확인한 것이다.

**방법**: GitHub API(`mcp__github__actions_list` / `list_workflow_runs`, `event=schedule` 필터)로 `poll.yml`과 `autoexec_stop_fast.yml`의 최근 스케줄 실행 이력을 각각 100건씩 조회하고, 연속된 두 실행의 `created_at` 타임스탬프 차이를 계산했다. 두 워크플로 모두 cron이 `*/5 * * * *`(5분 간격)이므로, 정상이라면 연속 실행 간격은 5분 안팎이어야 한다.

## 발견: 두 워크플로에서 동시에 12시간+ 공백

| 워크플로 | 직전 실행 (`created_at`) | 다음 실행 (`created_at`) | 간격 | run id |
|---|---|---|---|---|
| `poll.yml` (#798→#799) | 2026-08-28T05:17:32Z | 2026-08-28T17:28:56Z | **12시간 11분 24초** | [33144319549](https://github.com/ringoincidents/quantrade/actions/runs/33144319549) → [33194876888](https://github.com/ringoincidents/quantrade/actions/runs/33194876888) |
| `autoexec_stop_fast.yml` (#647→#648) | 2026-08-28T04:08:22Z | 2026-08-28T16:37:36Z | **12시간 29분 14초** | [33140934247](https://github.com/ringoincidents/quantrade/actions/runs/33140934247) → [33190859512](https://github.com/ringoincidents/quantrade/actions/runs/33190859512) |

두 워크플로의 공백 구간이 **거의 같은 시간대(2026-08-28 04:08~17:29 UTC, KST로 13:08~다음날 02:29)에 겹친다** — 서로 다른 워크플로 파일, 다른 job 내용인데도 같은 시간대에 동시에 멈췄다는 것은 이 저장소의 코드나 특정 워크플로의 로직 문제가 아니라 **GitHub Actions 플랫폼 쪽 스케줄링 문제**임을 시사한다(둘 다 `schedule:` 트리거를 쓰고, 둘 다 이 조사 시점 기준 `main`에서 실행되고 있었다).

**cron 설정이 이 공백의 원인이 아님을 확인**: 두 파일의 cron 표현식은 이 조사 시점에도 `*/5 * * * *`이고, 마지막으로 수정된 커밋(`5aed32e0b`, 2026-08-23T02:08:03Z)은 공백 구간(2026-08-28)보다 5일 이상 앞선다 — 즉 공백 발생 시점에 이미 5분 간격 cron이 적용된 상태였다.

## 대조: 평상시 간격

같은 조회에서 2026-08-25~08-27 구간(공백 전후)의 연속 실행 간격은 대부분 30분~2시간 사이였고(`poll.yml` 기준), 5분 정간격은 아니지만 12시간에 근접하는 값은 이 구간에서 관측되지 않았다. 즉 **12시간+ 공백은 상시 발생하는 정상 패턴이 아니라, 관측 기간 중 한 번 발생한 극단값**이다 — 다만 "한 번도 안 일어난다"가 아니라 "일어날 수 있고, 일어나면 이 정도 규모"라는 사실이 이번에 처음으로 실측됐다.

## 결론

- 2026-08-08 조사(72분 공백)는 GitHub Actions `schedule` 트리거가 수분~1시간대 지연될 수 있음을 보였다.
- 이번 조사(2026-08-29)는 같은 메커니즘이 **최소 12시간대까지 벌어질 수 있음**을, 서로 다른 두 워크플로의 동시 공백이라는 교차 증거로 확인했다.
- 이 지연/누락은 워크플로가 도는 러너의 종류(GitHub-hosted vs self-hosted)와 무관하다 — `schedule:` 이벤트 자체가 GitHub 쪽 스케줄러에서 큐잉되는 단계의 문제이므로, self-hosted runner로 바꿔도 "이벤트가 언제 큐에 올라가는가"는 그대로다. 근본 해결은 텔레그램이 즉시 push하는 웹훅 방식뿐이며, 이는 상시 대기 서버가 필요해 이 저장소의 "GitHub Actions가 유일한 런타임" 설계 원칙과 부딪힌다(2026-08-08 조사에서 이미 지적된 트레이드오프, 이번 조사로 그 대안이 더 시급해졌을 뿐 새로 생긴 문제는 아니다).
- **`RULE_BASED_AUTOEXEC_ENABLED`를 켜는 시나리오에서 이 사실이 갖는 의미**: 킬스위치(`/autoexec_stop`)가 `schedule:` 워크플로(`poll.yml`/`autoexec_stop_fast.yml`)에만 의존하는 한, 최악의 경우 사용자가 정지 명령을 보낸 뒤에도 12시간 이상 그 명령이 처리되지 않을 수 있다는 뜻이다 — 이는 실거래 자동실행에 요구되는 "즉시 정지 가능"이라는 전제와 정면으로 충돌한다.

## 조사 범위와 한계

- GitHub API로 확인 가능한 범위(최근 실행 이력)만 조사했다 — GitHub 자체의 인시던트/스테이터스 페이지 등 1급 소스는 이 환경에서 접근하지 못했다(2026-08-08 조사와 같은 제약).
- 표본이 1회의 12시간대 공백(교차 확인 2건)이라 "12시간+가 얼마나 자주 발생하는가"의 빈도까지는 이 조사로 알 수 없다 — "일어날 수 있다"는 사실과 그 최소 규모를 확인한 것이지, 발생 확률 분포를 구한 것은 아니다.
