# QuanTrade 상태 보고서 — 2026-08-28

> 세션 종료 시 갱신. 이 문서는 **현재 상태 요약**이다. 결정을 내리지 않으며,
> 판정성 서술은 담지 않는다(CLAUDE.md 세션 시작 체크리스트 "State facts,
> don't render verdicts"). 이번 세션은 세 부분으로 구성된다: v4.0 Phase A0
> Gap Analysis(지시문 1, 조사·분류만) → 그 결과에 따른 후속 구현(크립토
> 모의포지션 로직 폐기, 뉴스신호 실시간 코호트 cron 중단) → 인프라 조사
> (지시문 2, VPS/GitHub Actions 스케줄/시크릿/저장소 설정/킬스위치).

---

## 0. 브랜치 상태 (중요 — 세션 중 발견된 사실)

이 세션은 `claude/v4-phase-a0-gap-analysis-esot9d` 브랜치에서 작업했다.
인프라 조사(§5) 도중 로컬 `main` 참조가 `origin/main`보다 15개 커밋 뒤처져
있다는 사실을 발견했다 — `git fetch --unshallow`로 shallow clone(50커밋
제한)을 전체 히스토리(447커밋, 원격 브랜치 8개)로 전환하며 드러남. 이
브랜치가 분기된 지점(`476f806`) 이후 `origin/main`에는 **별도 브랜치
(`claude/toss-api-proxy-integration-b83ydz`)의 git 히스토리 시크릿 스캔 +
Anthropic API 키 재발급 기록(PR#33/#34/#35)**이 이미 병합돼 있었다(§5-3
참고). 이 사실을 반영해 **이 브랜치를 `origin/main`(`a611f47`) 기준으로
리베이스**했다 — `portfolio.json` 1건만 충돌(bot이 갱신한 크립토 가격
데이터 vs 이 세션의 "포지션 전부 삭제" 커밋)했고, 삭제 의도를 그대로
유지하는 쪽으로 해결했다. `analyze.py`/`news_event_experiment.yml`/
`CLAUDE.md`는 겹치는 부분 없이 자동 병합됐다.

---

## 1. 한눈에

| 항목 | 상태 | 근거 |
|---|---|---|
| v4.0 Phase A0 Gap Analysis (지시문 1) | ✅ 완료 | §2, 커밋 없음(조사·분류만) |
| 4분류표(이미구현/수정필요/새로필요/폐기) | ✅ 완료 | §2-1 |
| Intelligence Layer 6단계 매핑 | ✅ 완료 | §2-2 |
| audit() 현황 조사(금지어 목록, 오탐 5건) | ✅ 완료 | §2-3 |
| 죽은 코드 조사(크립토 잔재/Phase2 격리대상/미실행 워크플로) | ✅ 완료 | §2-4 |
| 뉴스신호 실시간 코호트 cron 중단 | ✅ 완료 | §3-1 |
| 크립토 모의포지션 로직 전체 제거 | ✅ 완료 | §3-2/3-3 |
| CLAUDE.md 사실 기록 2건 추가 | ✅ 완료 | §3-4 |
| 인프라 조사 (지시문 2 — VPS/Actions/시크릿/저장소/킬스위치) | ✅ 완료(조사만, VPS 직접 접근분 제외) | §5 |
| 브랜치를 origin/main 기준으로 리베이스 | ✅ 완료 | §0 |
| 지시문 3(백테스트 세션 — Alpha Lab 이관, Core 오염 확인) | — | 이번 세션 범위 아님 |

---

## 2. Phase A0 Gap Analysis 요약 (지시문 1)

**전제**: `QuanTrade_종합계획서_v4_0.md`는 이 저장소에 커밋돼 있지 않아 원문을 확인하지 못했다. 아래는 지시문 본문에 기술된 내용(6단계 Intelligence Layer, 공통 스키마 필드셋 등)을 근거로 수행한 조사이며, 코드 수정 없이 조사·분류만 했다.

### 2-1. 4분류 (요약)

- **Python 18개**: `analyze_lib.py`/`analyze.py`/`check_updates.py`/`autoexec.py`/`autoexec_stop_fast.py`/`rule_trigger_report.py`/`market_indicators.py`/`news_event_cards.py`/`post_trade_review.py`/`portfolio_report.py`/`real_portfolio_sync.py`는 이미구현 또는 수정 필요(Core), `toss_order_endpoint_probe.py`는 새로 필요(조사용), `backtest.py`/`indicator_significance_test.py`/`news_event_experiment.py`/`news_event_backtest.py`/`news_event_prompt_experiment.py`/`news_event_calibration_analysis.py` 6개는 폐기(보존) — Alpha Lab 이관 후보.
- **JSON 상태파일 27개**: Core 상태파일 다수 이미구현. `autoexec_log.json`은 2026-08-04 단일 날짜(24건, `fired:true` 0건)만 있고 이후 갱신 없음. `autoexec_state.json`/`autoexec_reports.json`은 파일 자체가 아직 없음(§5-5에서 origin/main 최신 기준으로 재확인 — 여전히 없음). `backtest_report.json`류 6개 파일은 폐기(보존, 삭제 금지 대상).
- **GitHub Actions 워크플로 14개**: 8개는 schedule 활성(`daily`/`poll`/`sync_real`/`market_indicators`/`news_event_cards`/`post_trade_review`/`portfolio_report`/`autoexec_stop_fast`), 5개는 workflow_dispatch만(`backtest`/`indicator_significance_test`/`news_event_backtest`/`news_event_prompt_experiment`/`toss_order_endpoint_probe`). `news_event_experiment.yml`은 조사 시점엔 schedule 활성이었으나 이번 세션 중 dispatch-only로 전환(§3-1). `autoexec.yml`은 애초에 존재하지 않음.
- **`index.html`**: 현재 `<h2>` 블록 13개 확인 — CLAUDE.md의 "2026-08-02 리빌드, 3블록" 서술보다 실제 파일이 앞서 있음(문서 드리프트, 수정 안 함).

### 2-2. Intelligence Layer 6단계 매핑 (요약)

| 단계 | 현재 담당 | 상태 |
|---|---|---|
| Normalization | 없음(단일 담당 부재) | 각 모듈이 자기 전용 스키마(`schema: "<모듈>_v3.2"`)를 따로 정의 |
| Event Detection | `news_event_cards.py`의 `ask_explanation()` | 활성 |
| Change Detection | `news_event_cards.py`의 `detect_anomalies()` + `market_indicators.py` 상태판 | 출력 형태가 서로 다름(자연어 문장 vs 평평한 숫자행) |
| Event Prioritization | 없음 | `audit()`의 `FORBIDDEN_FIELDS_BASE`가 rank/score/grade류를 기본 금지 |
| Portfolio Relevance | `post_trade_review.py`의 `connect_recent_news()` + `market_indicators.py` 상관계수 | 부분적으로만 존재 |
| AI Briefing | 없음(단일 합성 단계 부재) | `rule_trigger_report.py`가 가장 근접하나 규칙 발동 시 1종목 단위 |

제시된 공통 스키마 필드(`timestamp`/`asset`/`source`/`event_type`/`observed_value`/`baseline`/`change`/`reliability`/`related_assets`) 중 실제 코드에 그대로 쓰이는 이름은 `event_type` 하나뿐.

### 2-3. audit() 현황 (요약)

단일 공용 함수가 아니라 `analyze_lib.py`의 `FORBIDDEN_FIELDS_BASE`/`FORBIDDEN_PHRASES_BASE`를 6개 파일이 각자 확장/축소해 쓰고, 재귀검사 `audit()` 로직 자체는 6벌이 복붙돼 있음. 오탐 처리 이력 5건 확인:

1. `indicator_significance_test.py` — `signal` 필드(지표 이름) 충돌 → 이 파일에서만 목록 제외
2. `portfolio_report.py` — `rank`/`ranking`(계급) 충돌 → 이 파일에서만 목록 제외
3. `market_indicators.py` — 자기지시적 문구("순위를 매기지 않습니다") → 재작성으로 해결(제외 없음)
4. `portfolio_report.py` — 자기지시적 문구("권장 금액") → 재작성으로 해결(제외 없음)
5. `news_event_cards.py` — 다른 두 파일 주석/세션보고서에서 교차 인용만 확인, 원 사례 자체는 shallow clone 한계로 특정 못함(§5-0에서 full clone 전환됐으므로 재조사하면 특정 가능할 수 있음 — 이번 세션엔 하지 않음)

### 2-4. 죽은 코드 조사 (요약)

- **크립토 모의투자 잔재**: 조사 시점엔 완전히 제거되지 않은 상태였음(portfolio.json에 크립토 포지션 6건 존재, 하드손절/거래기록 로직은 계속 작동, 텔레그램 출력만 억제돼 있었음) → **이번 세션에서 §3-2로 완전히 제거**.
- **`autoexec.run()`**: 이를 스케줄로 도는 워크플로가 없음(`autoexec.yml` 자체가 없고, `daily.yml`은 `analyze.py`만 실행) — index.html/poll.yml 주석/CLAUDE.md 세 곳에서 독립적으로 일치 확인. 킬스위치 관점의 추가 확인은 §5-5.
- **Phase2/promptB 관련 4개 파일**(`news_event_experiment.py`/`news_event_backtest.py`/`news_event_prompt_experiment.py`/`news_event_calibration_analysis.py`) — Alpha Lab 이관 후보로 표시.

전체 조사 내용은 이전 대화 턴의 조사 응답에 상세 표로 남아 있음(이 문서는 요약).

---

## 3. 크립토 시뮬 폐기 + 뉴스신호 cron 중단 (구현)

Gap Analysis에서 확인된 사실("크립토 모의투자 잔재가 완전히 제거되지 않았음")에 대한 후속 조치로, PM 승인 하에 아래 구현을 진행했다.

### 3-1. `news_event_experiment.yml` — schedule 트리거 제거

`on:` 블록에서 `schedule: cron: '30 9 * * *'`를 삭제하고 `workflow_dispatch`만 남김 — 다른 폐기 워크플로(`backtest.yml` 등)와 동일 패턴. `news_event_experiment.py` 코드, `news_event_calibration_log.json`(759건) 기존 데이터는 손대지 않음. 재개 시 이 schedule 항목만 되돌리면 된다.

### 3-2. `analyze.py` — 크립토 모의포지션 로직 전체 제거

- 삭제한 것: 포지션별 가격 갱신 루프, 하드손절 자동실행, `trade_history.json` 적재 로직, 그리고 이 세 가지에 얽혀 있던 "크립토 출력만 숨긴다"는 `is_dead_crypto` 분기 전부.
- 그대로 둔 것: `check_risk_guardrails()`(비중 상한/현금 비율 — 크립토 전용이 아니라 v3.2의 세 가드레일 중 둘), `PREDICTION_ENABLED` 게이트 뒤에 보존된 예측 경로 연구 코드(삭제 금지 대상), 실계좌 `REAL:` 제안 큐잉 로직.
- **주의 기록**: 하드손절 자동실행 메커니즘 자체가 `analyze.py`에서 없어졌다. `PREDICTION_ENABLED`가 나중에 재활성화돼 가상 주식/KRX 후보가 다시 매수되더라도, 지금 코드에는 그 포지션을 하드손절할 경로가 없어 재개 시 다시 만들어야 한다.
- **검증**: `python3 -m py_compile`로 문법 확인, 격리된 임시 디렉터리에 파일을 복사해 `analyze.run()`을 실제로 실행 — 기존 크립토 데이터가 있는 상태(가드레일 위반 1건 정상 검출)와 크립토 제거 후 상태(위반 0건, `positions: []`, `cash` 불변) 양쪽 모두 정상 동작 확인. 이후 리베이스(§0)로 origin/main의 최신 bot 갱신 데이터와 충돌한 부분도 "포지션 삭제" 의도를 유지하는 쪽으로 재해결·재검증함.

### 3-3. `portfolio.json` — 크립토 포지션 6건 제거

`asset_class:"crypto"` 5건(`KRW-PENGU`/`KRW-WIF`/`KRW-LAYER`/`KRW-ANKR`/`KRW-USD1`) + 필드 자체가 없어 기본값으로 크립토 취급되던 1건(`KRW-USDE`) 제거. `cash`(48,351.70원)는 그대로 둠 — 포지션 청산에 따른 현금 환입 처리는 지시에 없어 하지 않았다.

### 3-4. `CLAUDE.md` — 사실 기록 2건

- "뉴스신호 실시간 코호트 cron 중단 (2026-08-28) — Alpha Lab 재개 절차(§15) 전까지 비활성" (Phase 2 두 트랙 설명 문단 뒤에 추가)
- "크립토 시뮬 로직 폐기 (2026-08-28, PM 승인)" (analyze.py 아키텍처 설명 뒤에 추가, 하드손절 경로 소실 사실 포함)

### 3-5. 건드리지 않은 것

`backtest_report.json`/`backtest_report_experiment_b.json`/`indicator_significance_report.json`/`news_event_backtest_log*.json`/`news_event_calibration_log.json` 등 과거 백테스트·실험 데이터는 전혀 손대지 않았다. `check_updates.py`의 크립토 필터(`/status` 출력)도 이번 지시 범위 밖이라 그대로 뒀다 — 크립토 포지션이 더 이상 없으므로 이 필터는 이제 사실상 공회전이지만, 남아 있어도 무해하다.

---

## 4. Alpha Lab 격리 (지시문 3) — 이번 세션 범위 아님

지시문 3(백테스트 세션: Alpha Lab 이관 목록, Core 오염 여부, 연구 데이터 보존 확인)은 이 세션에서 다루지 않았다 — 별도 세션 대상.

---

## 5. 인프라 조사 (지시문 2)

VPS/GitHub Actions 스케줄/시크릿/저장소 설정/킬스위치를 조사·분류만 했다(코드 수정 없음). §0에서 이미 다룬 브랜치 상태 확인 과정에서 얻은 부수 발견(git 히스토리 스캔, 키 재발급 기록)도 여기 정리한다.

### 5-1. 실행 환경 조사

- **Vultr VPS(Seoul) 가동 여부 / questproxy 인증 — 이 세션에서 직접 확인 불가.** VPS에 대한 SSH/콘솔 접근 수단이 이 세션에 없다. 대신 간접 증거: 최근 30건의 `sync_real.yml` 실행(2026-08-20~08-28)이 전부 `conclusion: success`이고 `real_portfolio.json`의 `synced_at`(`2026-08-28T11:35:53Z`, origin/main 기준)이 최신 실행 시각과 정확히 일치 — 프록시·인증이 죽어 있었다면 이 호출들이 실패했을 것이므로, 최소 그 시점까지는 정상 동작했다는 정황이 있다.
- **Toss API 화이트리스트 IP vs 현재 VPS IP — 코드에 기록된 기대값만 확인, 실시간 대조는 불가.** `real_portfolio_sync.py`의 `check_proxy_ip()` docstring과 `토스_주문API_스펙조사_2026-08-05.md`에 **Vultr 프록시 141.164.41.178:3128**이 명시돼 있다(2026-08-01 확인 기록). 이 값이 지금도 유효한지는 Toss 화이트리스트 관리 화면이나 VPS 접근이 필요해 이 세션은 확인하지 못했다.
- **VPS 비용/스펙(CPU/RAM/디스크) — 확인 불가.** Vultr 계정/청구 정보 접근 도구가 이 세션에 없다.

### 5-2. GitHub Actions 스케줄 신뢰성 조사

`poll.yml`/`autoexec_stop_fast.yml`의 최근 30건씩 실제 실행 시각(`created_at`)을 GitHub API로 직접 조회해 연속 실행 간 간격을 계산했다(선언된 주기는 둘 다 5분).

| 워크플로 | 관측 구간 | 최소 간격 | 중앙값 | 최대 간격 | 5분 이내 비율 |
|---|---|---|---|---|---|
| `poll.yml` | 2026-08-25T23:12 ~ 2026-08-28T17:28 (29개 간격) | 19.1분 | 53.0분 | **731.4분(약 12.2시간)** | 0/29 (0%) |
| `autoexec_stop_fast.yml` | 2026-08-26T01:39 ~ 2026-08-29T00:41 (28개 간격) | 1.5분 | 53.8분 | **749.2분(약 12.5시간)** | 1/28 (3.6%) |

- **인수인계 §4-7 "최대 72분 지연"은 재현되는 정도가 아니라 이미 훨씬 초과됐다.** 관측된 최근 구간(2026-08-27~28)에는 500~750분(8~12.5시간) 간격이 여러 번 나타난다. 5분 간격이 지켜진 사례는 사실상 없다(0~3.6%).
- `autoexec_stop_fast.yml`에는 2026-08-26 15:06~15:24 사이에 `cancelled` 2건, `failure` 2건이 몰려 있는 구간이 있다 — 원인은 조사하지 않았다(로그 미확인).
- **저장소는 Public이므로 GitHub Actions 사용 시간은 무료(무제한)다** — `get_workflow_run_usage` 조회 결과 `billable: {"UBUNTU": {"total_ms": 0}}`로 나왔는데, Public 저장소가 표준 러너 사용 시간에 과금 대상이 아니기 때문으로 보인다. 즉 **"Actions 분당 사용량 초과"가 이 지연의 원인일 가능성은 낮다** — 확정은 아니지만 사실관계상 그렇다.

### 5-3. 시크릿/환경변수 조사

- **GitHub Secrets — 실제 값 열람 도구는 이 세션에 없음. 워크플로 YAML에서 참조하는 키 이름을 전수 나열(대리 확인):** `TELEGRAM_TOKEN`, `TELEGRAM_CHAT_ID`, `CLAUDE_API_KEY`, `PROXY_URL`, `TOSS_CLIENT_ID`, `TOSS_CLIENT_SECRET` — 6개. (참고: "`ANTHROPIC_API_KEY`"/"`TELEGRAM_BOT_TOKEN`"이라는 이름은 실제 워크플로에서 쓰이는 이름과 다르다 — 실제로는 `CLAUDE_API_KEY`/`TELEGRAM_TOKEN`이다.)
- **.env 사용 여부/gitignore**: `.gitignore`에 `.env`/`.env.*` 등록돼 있음. 현재 워킹트리에 `.env` 파일 없음. 전체 히스토리(447커밋, 8브랜치) 어디에도 `.env` 또는 `secret`/`credential`/`.pem`/`.key` 이름의 파일이 추가된 적이 없음(git log 전수 확인).
- **하드코딩 재확인 — 이미 별도 브랜치(`claude/toss-api-proxy-integration-b83ydz`, PR#33으로 병합, §0 참고)에서 gitleaks v8.21.2 + trufflehog v3.83.7로 전수 스캔이 수행돼 있었다(`git_history_scan_result.md`, origin/main에 존재, 이번 리베이스로 이 브랜치에도 포함됨).** 결과:
  - gitleaks 기본 규칙셋: 0건 / trufflehog: **3건(고유 값 2종)** — Telegram Bot Token 1개, Anthropic API 키 형태 문자열 1개(2개 커밋에 등장) — 전부 `analyze.py`의 2026-07-29~30 커밋(`3aa4116`/`9656148`/`c291b566`)에 하드코딩, 2026-07-31 `analyze_lib.py` 신설 커밋에서 환경변수 방식으로 교체되며 현재 워킹트리에는 없음.
  - **Anthropic 키는 이미 재발급 완료·검증됨**(PR#35, CLAUDE.md 기록): 사용자가 console.anthropic.com에서 폐기 후 재발급, `CLAUDE_API_KEY` 시크릿 교체, 1차 identity-linked 키가 HTTP 400으로 실패해 legacy 키로 재발급 후 실제 workflow_dispatch 실행으로 정상 동작 확인.
  - **Telegram 토큰은 아직 재발급되지 않음** — PM 판단 대기 상태로 명시적으로 남아 있음.
  - **저장소 Public/Private 전환도 아직 결정되지 않음** — 저장소가 생성 시점(2026-07-28)부터 지금까지 계속 Public이었다는 사실만 기록돼 있음.
  - 이 세션은 **독립적으로 추가 확인**을 시도했다: 전체 히스토리(모든 브랜치)에 대해 AWS 키(`AKIA...`), GitHub PAT(`ghp_...`), PEM 개인키 블록, `PROXY_URL`/`TOSS_CLIENT_ID`/`TOSS_CLIENT_SECRET`의 하드코딩된 대입, `Authorization: Bearer <literal>` 패턴을 추가로 grep했다 — **전부 0건**. 이미 발견된 2종 외의 다른 하드코딩된 값은 이 세션의 스캔 범위 내에서 발견되지 않았다.

### 5-4. 저장소 설정 조사

- **Public/Private**: `visibility: "public"`, `private: false`(GitHub API 직접 조회). 생성 시점(2026-07-28)부터 계속 Public.
- **협업자 수**: 1명(`ringoincidents`, role: admin) — outside collaborator 없음.
- **GitHub 플랜(Free/Pro)**: 확인 불가 — 청구/플랜 조회 도구가 이 세션에 없음. 다만 Public 저장소는 플랜과 무관하게 Actions 사용 시간이 무료이므로, 플랜 정보가 §5-2의 지연 원인 판단에 미치는 영향은 제한적이다.
- **Actions 분당 사용량(무료 티어 2,000분 대비 %)**: 직접 조회 도구 없음. 다만 Public 저장소는 애초에 2,000분 무료 티어(Private 저장소 대상)의 적용을 받지 않으므로 "%" 자체가 성립하지 않는 개념일 가능성이 높다(§5-2 참고).
- **Pages 배포 방식**: `has_pages: true`이고, 워크플로 목록에 사용자가 만든 배포 워크플로는 없이 GitHub가 자동 생성하는 `pages-build-deployment`(경로: `dynamic/pages/pages-build-deployment`)만 존재한다 — "브랜치에서 배포"(Legacy) 방식일 때 나타나는 패턴이다. 정확한 배포 브랜치/폴더 설정 값 자체는 재확인하지 못했다.

### 5-5. 킬스위치 조사

- **트리거 조건**: `autoexec_stop_fast.py`가 `telegram_offset.json`의 마지막 오프셋+1부터 `getUpdates`를 폴링해, 텍스트가 정확히 `/autoexec_stop`(앞뒤 공백만 허용)인 메시지가 있으면 즉시 `autoexec.engage_kill_switch()`를 호출한다. 오프셋은 절대 전진시키지 않음(poll.yml 전용 소유).
- **상태 저장 파일/방식**: `autoexec_state.json`의 `{"stopped": bool, "stopped_at": ISO시각, "first_enabled_at", "last_fired": {규칙명: 날짜}}` 구조. `release_kill_switch()`(`/autoexec_start`)만 해제 가능, 재시작·실행 실패로 자동 해제되지 않음.
- **실제 이 파일이 아직 존재하지 않음** — origin/main 최신 기준으로도 `autoexec_state.json` 자체가 없다(§2-1과 동일 사실, 여기서는 킬스위치 관점에서 재확인). 즉 킬스위치가 실거래 상황에서 실제로 작동된 적이 없다 — `autoexec_log.json`도 2026-08-04 하루치(24건, `fired:true` 0건)만 있고 그 이후 갱신이 없다는 사실과 일치.
- **차단까지 걸리는 시간 — 설계값 vs 실측**: 설계상 5분 간격 폴링으로 최대 5분 내 반영을 노리지만, §5-2에서 확인했듯 이 워크플로 자체가 최근 최대 12.5시간까지 지연되는 사례가 있어 설계값과 실측값의 간극이 이전 조사(2026-08-08, 40~90분/72분 무실행)보다 더 벌어진 상태다. `/autoexec_stop`이 실제로 발화된 사례 자체가 없어 종단간 "명령 전송→킬스위치 작동" 실측 지연시간은 계산하지 못했고, 워크플로 자체의 폴링 간격 지연만 확인했다.

---

## 6. 미해결/승계 항목

- 지시문 3(백테스트 세션: Alpha Lab 이관 목록, Core 오염 여부, 연구 데이터 보존 확인) — 별도 세션 대상, 이 세션에서 다루지 않음.
- **Telegram Bot Token 재발급 여부** — PM 판단 대기(§5-3).
- **저장소 Public → Private 전환 여부** — PM 판단 대기(§5-3, §5-4).
- **git 히스토리에서 노출된 값 자체를 제거할지(`git filter-repo` 등, 강제 푸시 필요) 여부** — PM 판단 대기(§5-3).
- **GitHub Actions 스케줄 신뢰성 저하(§5-2) 대응 여부** — `RULE_BASED_AUTOEXEC_ENABLED` 전환 논의 시 함께 다룰 안건으로 CLAUDE.md에 이미 등록돼 있음, 이번 세션에서 수치를 갱신만 함.
- PM 세션의 다음 단계(세션 보고 취합 → Private 전환 결정 → Alpha Lab 격리 실행 지시 → A1 Dashboard Wireframe)는 이 보고서 범위 밖.
- `check_updates.py`의 크립토 필터 정리는 요청받지 않아 보류 — 필요 시 별도 지시 요망.

---

## 부록 — 커밋 · 브랜치

| 커밋 | 내용 |
|---|---|
| `125b087` | `news_event_experiment.yml` cron 제거, `analyze.py` 크립토 로직 삭제, `portfolio.json` 정리, `CLAUDE.md` 사실 기록 2건 (origin/main 리베이스 후 해시) |
| (이 커밋) | 세션보고서_2026-08-28.md에 인프라 조사(§5) 결과 병합, §0/§1/§4/§6/부록 갱신 |

브랜치: `claude/v4-phase-a0-gap-analysis-esot9d` — `origin/main`(`a611f47`) 기준으로 리베이스 완료 후 강제 push. PR 미생성 — 요청 시 생성.
