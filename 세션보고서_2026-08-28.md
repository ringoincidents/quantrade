# QuanTrade 상태 보고서 — 2026-08-28

> 세션 종료 시 갱신. 이 문서는 **현재 상태 요약**이다. 결정을 내리지 않으며,
> 판정성 서술은 담지 않는다(CLAUDE.md 세션 시작 체크리스트 "State facts,
> don't render verdicts"). 이번 세션은 v4.0 Phase A0 Gap Analysis(지시문 1,
> 조사·분류만)와, 그 결과에 따른 후속 구현(크립토 모의포지션 로직 폐기,
> 뉴스신호 실시간 코호트 cron 중단) 두 가지로 구성된다.

---

## 1. 한눈에

| 항목 | 상태 | 근거 |
|---|---|---|
| v4.0 Phase A0 Gap Analysis (지시문 1) | ✅ 완료 | §2 참고, 커밋 없음(조사·분류만, 코드 수정 금지 지시) |
| 4분류표(이미구현/수정필요/새로필요/폐기) | ✅ 완료 | §2-1 |
| Intelligence Layer 6단계 매핑 | ✅ 완료 | §2-2 |
| audit() 현황 조사(금지어 목록, 오탐 5건) | ✅ 완료 | §2-3 |
| 죽은 코드 조사(크립토 잔재/Phase2 격리대상/미실행 워크플로) | ✅ 완료 | §2-4 |
| 뉴스신호 실시간 코호트 cron 중단 | ✅ 완료 | `36e5398`, §3-1 |
| 크립토 모의포지션 로직 전체 제거 | ✅ 완료 | `36e5398`, §3-2 |
| CLAUDE.md 사실 기록 2건 추가 | ✅ 완료 | `36e5398`, §3-3 |
| 지시문 2(프록시/인프라)·지시문 3(백테스트/Alpha Lab) | — | 이번 세션 범위 아님(각각 별도 세션 대상) |

---

## 2. Phase A0 Gap Analysis 요약 (지시문 1)

**전제**: `QuanTrade_종합계획서_v4_0.md`는 이 저장소에 커밋돼 있지 않아 원문을 확인하지 못했다. 아래는 지시문 본문에 기술된 내용(6단계 Intelligence Layer, 공통 스키마 필드셋 등)을 근거로 수행한 조사이며, 코드 수정 없이 조사·분류만 했다.

### 2-1. 4분류 (요약)

- **Python 18개**: `analyze_lib.py`/`analyze.py`/`check_updates.py`/`autoexec.py`/`autoexec_stop_fast.py`/`rule_trigger_report.py`/`market_indicators.py`/`news_event_cards.py`/`post_trade_review.py`/`portfolio_report.py`/`real_portfolio_sync.py`는 이미구현 또는 수정 필요(Core), `toss_order_endpoint_probe.py`는 새로 필요(조사용), `backtest.py`/`indicator_significance_test.py`/`news_event_experiment.py`/`news_event_backtest.py`/`news_event_prompt_experiment.py`/`news_event_calibration_analysis.py` 6개는 폐기(보존) — Alpha Lab 이관 후보.
- **JSON 상태파일 27개**: Core 상태파일 다수 이미구현. `autoexec_log.json`은 2026-08-04 단일 날짜(24건, `fired:true` 0건)만 있고 이후 갱신 없음. `autoexec_state.json`/`autoexec_reports.json`은 파일 자체가 아직 없음. `backtest_report.json`류 6개 파일은 폐기(보존, 삭제 금지 대상).
- **GitHub Actions 워크플로 14개**: 8개는 schedule 활성(`daily`/`poll`/`sync_real`/`market_indicators`/`news_event_cards`/`post_trade_review`/`portfolio_report`/`autoexec_stop_fast`), 5개는 workflow_dispatch만(`backtest`/`indicator_significance_test`/`news_event_backtest`/`news_event_prompt_experiment`/`toss_order_endpoint_probe`). `news_event_experiment.yml`은 조사 시점엔 schedule 활성이었으나 이번 세션 중 dispatch-only로 전환(§3-1). `autoexec.yml`은 애초에 존재하지 않음.
- **`index.html`**: 현재 `<h2>` 블록 13개 확인 — CLAUDE.md의 "2026-08-02 리빌드, 3블록" 서술보다 실제 파일이 앞서 있음(문서 드리프트, 수정 안 함).

### 2-2. Intelligence Layer 6단계 매핑 (요약)

| 단계 | 현재 담당 | 상태 |
|---|---|---|
| Normalization | 없음(단일 담당 부재) | 각 모듈이 자기 전용 스키마(`schema: "<모듈>_v3.2"`)를 따로 정의 |
| Event Detection | `news_event_cards.py`의 `ask_explanation()` | 활성 |
| Change Detection | `news_event_cards.py`의 `detect_anomalies()` + `market_indicators.py` 상태판 | 출력 형태가 서로 다름(자연어 문장 vs 평평한 숫자행) |
| Event Prioritization | 없음 | `audit()`의 `FORBIDDEN_FIELDS_BASE`가 rank/score/grade류를 기본 금지 |
| Portfolio Relevance | `post_trade_review.py`의 `connect_recent_news()` + `market_indicators.py` 상관계수 | 부분적으로만 존재 |
| AI Briefing | 없음(단일 합성 단계 부재) | `rule_trigger_report.py`가 가장 근접하나 규칙 발동 시 1종목 단위 |

제시된 공통 스키마 필드(`timestamp`/`asset`/`source`/`event_type`/`observed_value`/`baseline`/`change`/`reliability`/`related_assets`) 중 실제 코드에 그대로 쓰이는 이름은 `event_type` 하나뿐.

### 2-3. audit() 현황 (요약)

단일 공용 함수가 아니라 `analyze_lib.py`의 `FORBIDDEN_FIELDS_BASE`/`FORBIDDEN_PHRASES_BASE`를 6개 파일이 각자 확장/축소해 쓰고, 재귀검사 `audit()` 로직 자체는 6벌이 복붙돼 있음. 오탐 처리 이력 5건 확인:

1. `indicator_significance_test.py` — `signal` 필드(지표 이름) 충돌 → 이 파일에서만 목록 제외
2. `portfolio_report.py` — `rank`/`ranking`(계급) 충돌 → 이 파일에서만 목록 제외
3. `market_indicators.py` — 자기지시적 문구("순위를 매기지 않습니다") → 재작성으로 해결(제외 없음)
4. `portfolio_report.py` — 자기지시적 문구("권장 금액") → 재작성으로 해결(제외 없음)
5. `news_event_cards.py` — 다른 두 파일 주석/세션보고서에서 교차 인용만 확인, 원 사례 자체는 shallow clone 한계로 특정 못함

### 2-4. 죽은 코드 조사 (요약)

- **크립토 모의투자 잔재**: 조사 시점엔 완전히 제거되지 않은 상태였음(portfolio.json에 크립토 포지션 6건 존재, 하드손절/거래기록 로직은 계속 작동, 텔레그램 출력만 억제돼 있었음) → **이번 세션에서 §3-2로 완전히 제거**.
- **`autoexec.run()`**: 이를 스케줄로 도는 워크플로가 없음(`autoexec.yml` 자체가 없고, `daily.yml`은 `analyze.py`만 실행) — index.html/poll.yml 주석/CLAUDE.md 세 곳에서 독립적으로 일치 확인.
- **Phase2/promptB 관련 4개 파일**(`news_event_experiment.py`/`news_event_backtest.py`/`news_event_prompt_experiment.py`/`news_event_calibration_analysis.py`) — Alpha Lab 이관 후보로 표시.

전체 조사 내용은 이전 대화 턴의 조사 응답에 상세 표로 남아 있음(이 문서는 요약).

---

## 3. 이번 세션 구현 — 크립토 시뮬 폐기 + 뉴스신호 cron 중단

Gap Analysis에서 확인된 사실("크립토 모의투자 잔재가 완전히 제거되지 않았음")에 대한 후속 조치로, PM 승인 하에 아래 구현을 진행했다.

### 3-1. `news_event_experiment.yml` — schedule 트리거 제거

`on:` 블록에서 `schedule: cron: '30 9 * * *'`를 삭제하고 `workflow_dispatch`만 남김 — 다른 폐기 워크플로(`backtest.yml` 등)와 동일 패턴. `news_event_experiment.py` 코드, `news_event_calibration_log.json`(759건) 기존 데이터는 손대지 않음. 재개 시 이 schedule 항목만 되돌리면 된다.

### 3-2. `analyze.py` — 크립토 모의포지션 로직 전체 제거

- 삭제한 것: 포지션별 가격 갱신 루프, 하드손절 자동실행, `trade_history.json` 적재 로직, 그리고 이 세 가지에 얽혀 있던 "크립토 출력만 숨긴다"는 `is_dead_crypto` 분기 전부.
- 그대로 둔 것: `check_risk_guardrails()`(비중 상한/현금 비율 — 크립토 전용이 아니라 v3.2의 세 가드레일 중 둘), `PREDICTION_ENABLED` 게이트 뒤에 보존된 예측 경로 연구 코드(삭제 금지 대상), 실계좌 `REAL:` 제안 큐잉 로직.
- **주의 기록**: 하드손절 자동실행 메커니즘 자체가 `analyze.py`에서 없어졌다. `PREDICTION_ENABLED`가 나중에 재활성화돼 가상 주식/KRX 후보가 다시 매수되더라도, 지금 코드에는 그 포지션을 하드손절할 경로가 없어 재개 시 다시 만들어야 한다.
- **검증**: `python3 -m py_compile`로 문법 확인, 격리된 임시 디렉터리에 파일을 복사해 `analyze.run()`을 실제로 실행 — 기존 크립토 데이터가 있는 상태(가드레일 위반 1건 정상 검출)와 크립토 제거 후 상태(위반 0건, `positions: []`, `cash` 불변) 양쪽 모두 정상 동작 확인.

### 3-3. `portfolio.json` — 크립토 포지션 6건 제거

`asset_class:"crypto"` 5건(`KRW-PENGU`/`KRW-WIF`/`KRW-LAYER`/`KRW-ANKR`/`KRW-USD1`) + 필드 자체가 없어 기본값으로 크립토 취급되던 1건(`KRW-USDE`) 제거. `cash`(48,351.70원)는 그대로 둠 — 포지션 청산에 따른 현금 환입 처리는 지시에 없어 하지 않았다.

### 3-4. `CLAUDE.md` — 사실 기록 2건

- "뉴스신호 실시간 코호트 cron 중단 (2026-08-28) — Alpha Lab 재개 절차(§15) 전까지 비활성" (Phase 2 두 트랙 설명 문단 뒤에 추가)
- "크립토 시뮬 로직 폐기 (2026-08-28, PM 승인)" (analyze.py 아키텍처 설명 뒤에 추가, 하드손절 경로 소실 사실 포함)

### 3-5. 건드리지 않은 것

`backtest_report.json`/`backtest_report_experiment_b.json`/`indicator_significance_report.json`/`news_event_backtest_log*.json`/`news_event_calibration_log.json` 등 과거 백테스트·실험 데이터는 전혀 손대지 않았다. `check_updates.py`의 크립토 필터(`/status` 출력)도 이번 지시 범위 밖이라 그대로 뒀다 — 크립토 포지션이 더 이상 없으므로 이 필터는 이제 사실상 공회전이지만, 남아 있어도 무해하다.

---

## 4. 미해결/승계 항목

- 지시문 2(프록시/인프라 세션: 저장소 Private 전환 조사, git history 시크릿 스캔, credential 분리 현황, tossinvest-cli MCP 재확인)와 지시문 3(백테스트 세션: Alpha Lab 이관 목록, Core 오염 여부, 연구 데이터 보존 확인)은 각각 별도 세션 몫 — 이 세션에서 다루지 않음.
- PM 세션의 다음 단계(3개 세션 보고 취합 → Private 전환 결정 → Alpha Lab 격리 실행 지시 → A1 Dashboard Wireframe)는 이 보고서 범위 밖.
- `check_updates.py`의 크립토 필터 정리는 요청받지 않아 보류 — 필요 시 별도 지시 요망.

---

## 부록 — 커밋

| 커밋 | 내용 |
|---|---|
| `36e5398` | `news_event_experiment.yml` cron 제거, `analyze.py` 크립토 로직 삭제, `portfolio.json` 정리, `CLAUDE.md` 사실 기록 2건 |

브랜치: `claude/v4-phase-a0-gap-analysis-esot9d` (원격에 push 완료, PR 미생성 — 요청 시 생성)
