# QuanTrade A5 사용기록 로깅 영속화 — Step 1 조사 결과 (2026-08-30)

> **코드 구현 없음 — Step 1(저장 구조) 조사 결과만.** PM 지시대로 "구현 전
> 방식 확정 필요" — 이 문서는 방식을 조사·비교하고 하나를 제안할 뿐,
> 확정은 PM 몫이다. Step 2(스키마)/Step 3(최소 구현)는 이 조사 결과가
> 승인된 뒤 진행한다.
>
> 조사 근거: 이 저장소의 실제 배포 방식(GitHub Pages, "Deploy from
> branch"), 실제 워크플로 구조(`.github/workflows/*.yml`), 실제
> `check_updates.py`/`poll.yml`의 텔레그램 명령 처리 코드, CLAUDE.md에
> 이미 기록된 보안 원칙(공개 저장소, git history 비밀값 노출 대응, "서버
> 없음 — GitHub Actions가 유일한 런타임" 설계 원칙과 그 원칙 때문에
> 웹훅 전환이 보류된 전례).

---

## 0. 문제의 본질 — 왜 이게 A5에서 가장 먼저 풀어야 하는 문제인가

`index.html`은 순수 정적 페이지다(빌드 스텝 없음, GitHub Pages가 HTML/CSS/JS를
그대로 서빙, 서버 사이드 실행 코드 없음). 브라우저에서 일어난 클릭을
**영구적으로** 남기려면 결국 이 저장소의 git 커밋 이력에 들어가야 한다
(이 저장소의 유일한 "데이터베이스"는 커밋된 JSON 파일들이라는 기존
관례 — CLAUDE.md "State files" 절). 그런데:

- git에 커밋하려면 **인증된 쓰기 권한**이 필요하다.
- 이 저장소는 **Public이다**(2026-07-28 생성 시점부터 지금까지, CLAUDE.md
  보안 절에서 이미 확인된 사실). 정적 사이트의 클라이언트 JS 소스는
  전부 공개돼 있다 — 브라우저 개발자 도구로 누구나 그대로 읽는다.
- 이 저장소는 "서버 없음, GitHub Actions가 유일한 런타임"이라는 설계
  원칙을 이미 갖고 있고, 이 원칙 때문에 웹훅(텔레그램이 즉시 push하는
  방식) 전환이 2026-08-08에 실제로 보류된 전례가 있다(CLAUDE.md "킬스위치
  반영 지연" 절 — "상시 대기 서버가 필요해... 설계 원칙과 정면으로
  부딪힌다").

**따라서 이 문제는 "어디에 JSON을 저장할까"가 아니라 "인증 없는 공개
클라이언트에서 나온 이벤트를, 새 서버 없이, 크리덴셜을 노출하지 않고,
어떻게 인증된 쓰기로 바꿀 것인가"다.** PM 지시문의 표현을 그대로 쓰면:
"AI가 직접 커밋하지 않는다"는 것과 같은 급의 원칙 — 여기서는 "클라이언트가
직접 커밋 권한을 가지면 안 된다"는 문제가 핵심이다.

---

## 1. PM이 제시한 3개 방식 조사

### (a) 클라이언트가 GitHub API를 직접 호출

**막히는 지점**: GitHub API로 커밋을 만들거나 워크플로를 트리거하려면
(REST `contents` API든 `workflow_dispatch`/`repository_dispatch`든) 인증된
토큰이 필요하다 — Personal Access Token이든 Fine-grained PAT든, "이
저장소에 쓸 수 있는 자격증명"이라는 본질은 같다. 이 토큰을 클라이언트
JS 어디에라도 박아 넣는 순간, 공개 저장소이므로 **전 세계 누구나 그
토큰을 복사해 임의로 커밋하거나 워크플로를 실행할 수 있는 상태**가
된다 — 스코프를 아무리 좁혀도("이 저장소, 이 파일만") "공개된
크리덴셜"이라는 사실 자체는 없어지지 않는다.

이건 가정이 아니라 이 저장소가 이미 한 번 실제로 겪은 문제와 같은
종류다 — git history 비밀값 노출 대응(2026-08-28, Telegram Bot Token/
Anthropic API 키가 과거 커밋에 하드코딩됐다가 재발급까지 간 사건)이
"우연히 노출된" 크리덴셜이었다면, 이 방식은 **의도적으로 노출시키는**
크리덴셜이라 더 나쁘다.

**판정: 불가.** 이 저장소가 이미 갖고 있는 보안 원칙과 정면 충돌하고,
완화책(스코프 최소화)도 근본 해결이 아니다.

### (b) 별도 로깅 엔드포인트

기술적으로는 된다 — 서버리스 함수(Cloudflare Workers/Vercel Functions
등)나 소형 VPS가 클릭 이벤트를 받아, 서버가 보관한(클라이언트에
노출되지 않는) 토큰으로 GitHub에 커밋하는 구조는 흔한 패턴이다.

**문제는 원칙 충돌이다.** 이 저장소는 "GitHub Actions가 유일한 런타임,
서버 없음"을 명시적 설계 원칙으로 삼고 있고, 정확히 이 이유로 웹훅
전환이 보류된 전례가 있다(위 §0 인용). 새 서버는:
- 새로 관리해야 할 시크릿(그 서버가 쓸 GitHub 토큰)이 생긴다.
- 새로운 유지보수 대상·비용·공격 표면이 생긴다.
- "서버 없음"이라는, 이 저장소가 여러 번 재확인해 온 설계 결정 자체를
  뒤집는 일이다.

**판정: 기술적으로는 가능하지만, 이 저장소의 아키텍처 원칙을 깨는
결정이다.** 이건 코드 세션이 조사 결과에 곁들여 슬쩍 채택할 사안이
아니라 — 이 저장소가 실계좌 코드 변경에 방향성 세션 승인을 요구하는
것과 같은 급의, **원칙 자체를 바꾸는 결정**이라 별도로 명시적 승인이
필요하다고 본다(이 문서는 판정만 내리고 승인 여부는 PM 몫으로 남긴다).

### (c) 클라이언트 로컬 저장 후 주기적 배치 업로드

`localStorage` 자체는 문제없이 된다 — 브라우저에 값을 남기는 것뿐이라
어떤 인증도 필요 없다. 하지만 **"배치 업로드"가 결국 (a)나 (b) 중
하나로 귀결된다** — 로컬 저장은 목적지가 아니라 중간 버퍼일 뿐이고,
"결국 저장소에 어떻게 쓸 것인가"라는 원래 문제는 그대로 남는다.

게다가 이 방식은 **이 프로젝트의 실제 사용 패턴과 특히 안 맞는다.**
사용자는 아침 6~9시 사이버지식정보방 PC와 그 외 시간 모바일 앱을
오가며 이 대시보드를 본다(CLAUDE.md에 이미 기록된 사실). `localStorage`는
브라우저/기기별로 완전히 분리된다 — PC에서 "확인함"을 누른 기록이
모바일에서 안 보이고, 그 반대도 마찬가지다. §16.1이 원하는 게 "행동
변화를 나중에 추적"하는 것이라면, 기기가 바뀔 때마다 기록이 끊기는
저장 방식은 그 목적 자체를 못 채운다.

**판정: 그 자체로는 영속화 솔루션이 아니다.** 클릭 즉시 서버(또는
그에 준하는 무언가)로 보내지 못했을 때의 **재전송 대기열**이나
**중복 전송 방지 캐시**로는 유용하지만, (a)/(b)/아래 (d) 중 하나와
반드시 결합해야 한다.

---

## 2. 조사 중 발견한 4번째 방식 — 텔레그램 딥링크 릴레이(기존 인프라 재사용)

PM이 제시한 3개 방식을 조사하다가, **이 저장소가 이미 이 문제를 한 번
풀어놓은 상태라는 사실**을 발견했다. 텔레그램의 `/approve`, `/keep`,
`/autoexec_approve` 같은 명령이 정확히 같은 형태의 문제다 — "인증
안 된 곳(사용자의 텔레그램 앱)에서 나온 사람의 행동을, 새 서버 없이,
크리덴셜 노출 없이 git 커밋으로 바꾸는" 문제. 이미 실제로 동작하는
해법이 있다:

- `poll.yml`이 5분마다 실행되며, `TELEGRAM_TOKEN`(저장소 시크릿, 서버가
  아니라 GitHub Actions 실행 환경에만 존재)으로 `getUpdates`를 조회한다.
- `check_updates.py`의 `run()`이 명령을 파싱해 상태 파일을 갱신하고
  커밋한다 — 실제 코드를 확인했다(`check_updates.py:254-`):
  ```python
  parts = text.split()
  cmd = parts[0].lower()
  ...
  elif cmd == "/keep" and len(parts) > 1:
      if handle_keep(parts[1], portfolio):
          changed = True
  ```
  `/keep <market>`처럼 인자 1~2개짜리 명령을 처리하는 패턴이 이미
  6종(`/approve`/`/reject`/`/keep`/`/unkeep`/`/autoexec_report`/
  `/autoexec_approve`/`/autoexec_reject`) 존재한다.

**아이디어**: TODAY/EVENTS의 "확인함"/"관련있음"/"관련없음" 버튼을
누르면, GitHub API를 직접 부르는 대신 텔레그램 딥링크
(`https://t.me/<봇 사용자명>?text=...`)를 연다 — 이건 **사용자 자신의
텔레그램 앱이 자신의 계정으로 봇에게 메시지를 보내는 것**이라 클라이언트
어디에도 시크릿이 필요 없다. `check_updates.py`에 새 명령
(예: `/log <event_id> <action>`) 핸들러 하나만 추가하면, 기존 5분
주기 폴링 인프라가 그대로 받아서 `usage_log.json`에 append한다(Step 2
스키마 대상).

**장점**:
- 새 서버·새 시크릿·새 인프라가 전혀 없다 — "서버 없음" 원칙을 그대로
  지킨다.
- 클라이언트에 노출되는 건 봇 사용자명(공개 정보, 텔레그램 자체가
  이미 공개하는 것)뿐이다.
- 5년 넘게 검증된 게 아니라 이 저장소 안에서 이미 매일 실제로 도는
  코드 경로를 재사용하는 것이라, "새로 만든 코드라 어디서 깨질지
  모른다"는 리스크가 다른 방식보다 작다.

**단점(정직하게 기록)**:
- **원클릭이 아니다.** 딥링크가 텔레그램 앱을 열고, 사용자가 전송을
  최소 한 번 확인해야 한다(텔레그램 클라이언트 설정에 따라 다름).
  데스크톱 브라우저에서 텔레그램 데스크톱 앱이 안 깔려 있으면 웹
  텔레그램으로 넘어가거나 동작이 어색할 수 있다 — 이 환경(사이버지식
  정보방 공용 PC)에서 실제로 어떻게 동작할지는 이 조사에서 확인하지
  못했다(확인 불가 사실로 남김 — 이 세션은 실제 텔레그램 클라이언트를
  띄워 테스트할 수 없다).
- **즉시 커밋이 아니다.** 최대 5분 지연(poll.yml 주기) — 버튼을 누른
  순간이 아니라 다음 폴링 사이클에 커밋된다.
- 사용자가 딥링크를 열고도 전송을 취소하면 로그가 안 남는다(반대로
  말하면, 이건 "정말 보낼 의도가 있었는지"를 한 번 더 확인시키는
  효과도 있다 — 좋고 나쁨을 이 문서가 판정하지 않는다).

---

## 3. 방식 비교 요약

| | (a) 클라이언트 직접 GitHub API | (b) 별도 서버 | (c) 로컬 저장만 | (d) 텔레그램 딥링크 릴레이 |
|---|---|---|---|---|
| 크리덴셜 노출 | **있음(치명적)** | 없음(서버가 보관) | 없음 | 없음 |
| "서버 없음" 원칙 | 해당 없음(애초에 서버가 없어서 발생하는 문제) | **깨짐** | 유지 | 유지 |
| 새 인프라 | 없음 | **필요(서버 1개)** | 없음 | 없음 |
| 기기 간 일관성 | 되면 됨(문제는 보안) | 됨 | **안 됨**(기기별 분리) | 됨(텔레그램 계정 기준) |
| 즉시성 | 즉시(가능했다면) | 즉시 가능 | 즉시(로컬만) | **최대 5분 지연 + 사용자 확인 1탭** |
| 실현 가능성(이 제약 하에서) | **불가** | 가능하나 원칙 위반 | 부분적(버퍼로만) | **가능** |

## 4. 이 문서의 제안

**(d) 텔레그램 딥링크 릴레이를, (c) localStorage를 "중복 전송 방지
캐시"로 보조 삼아 함께 쓰는 조합을 제안한다.** 근거: 이게 유일하게
"서버 없음" 원칙을 지키면서 크리덴셜도 노출하지 않고 실제로 동작하는
방식이고, 새로 만드는 부분이 이 저장소가 이미 검증된 방식(텔레그램
명령 파싱 + 폴링 커밋)의 자연스러운 확장이라 리스크도 작다.

**localStorage의 역할(보조, 그 자체로 해법 아님)**: 버튼을 누르면
로컬에 `{event_id, action, sent: false}`를 먼저 남기고, 딥링크를 연다.
사용자가 실제로 텔레그램에서 전송했는지는 클라이언트가 확인할 방법이
없으므로(텔레그램 앱과 웹사이트 사이에 응답 채널이 없음), 완벽한 확인
장치는 아니지만 "같은 버튼을 실수로 두 번 누르는" UI 중복은 막을 수
있다.

**이 문서가 PM 확인이 필요하다고 보는 지점(부록 B로 정리)**:

1. **(d)의 UX 트레이드오프(5분 지연 + 텔레그램 앱 확인 1탭)를 받아들일지.**
   즉시성이 필요 없는 로그 성격(§16.1 자체가 "나중에 집계 분석"하는
   데이터라고 명시)과는 맞아 보이지만, 이건 판단이 필요한 지점이라 이
   문서가 대신 정하지 않는다.
2. **아니면 (b)로 가서 "서버 없음" 원칙 자체를 바꾸는 방향성 결정을
   내릴지.** 이 경우 이 저장소의 기존 관례상 방향성 세션 승인이
   필요한 급의 결정이라고 본다.
3. **사이버지식정보방 공용 PC에서 텔레그램 딥링크가 실제로 잘 동작하는지**
   — 이 세션은 확인할 수 없다. Step 3(최소 구현) 전에 실제 환경에서
   한 번 손으로 확인이 필요할 수 있다.

## 부록 A. 이 문서가 다루지 않은 것

- Step 2(스키마: `event_id`/`action`/`timestamp`/`event_priority_score`,
  append-only)는 이 문서가 방식을 확정하지 않아 함께 확정하지 않았다 —
  스키마의 `event_id`는 어느 방식을 택하든 동일하게 A2의
  `changeEventHookId()`를 재사용하면 되므로 방식 선택과 독립적이다.
- Step 3(최소 구현: "관련있음/없음"부터)의 구체적 코드는 방식이
  확정된 뒤 진행한다.
- 텔레그램 봇 사용자명, 딥링크 URL 형식(`t.me/...?text=` vs
  `t.me/...?start=`)의 정확한 스펙은 Step 3에서 다룬다.

## 부록 B. PM 확인 필요 목록

1. 방식 확정: **(d) 텔레그램 딥링크 릴레이(권장)** vs (b) 별도 서버
   (원칙 변경 필요, 별도 승인 급) vs 그 외 대안.
2. (d)를 택할 경우, "확인함"/"관련있음"/"관련없음" 버튼 클릭 시 딥링크를
   여는 UX(예: 새 탭으로 텔레그램 열기)에 대한 사용자 경험 확인 —
   사이버지식정보방 공용 PC 환경에서의 실제 동작은 이 세션이 검증
   불가.
